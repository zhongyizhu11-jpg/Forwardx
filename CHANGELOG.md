# Changelog

## [未发布]

### 修复

- **侧边栏改头像，要等服务端顶回来才知道今天改不了了**（易用性）。账户面板做了两份 —— 侧边栏的账户菜单和个人资料页，各自定义同名的 **7 个 mutation 和 5 个处理函数**，一百多行几乎一样，而且已经漂了。
  - 个人资料页点保存前先查当天额度，直接说「今日头像修改次数已用完」；侧边栏**没有这个前置判断**，用户挑完、裁完、点了保存，才被服务端顶回来。
  - **额度本身服务端一直在管**（`userRepository` 里会抛「头像每天最多修改 N 次」），所以侧边栏那条路只是体验更差，**不是绕过了限制** —— 查过服务端才敢这么说。
  - 额度算法收进 `lib/avatarQuota.ts` 两边共用，侧边栏补上查询、前置判断和成功后的失效刷新。默认值照搬个人资料页的「查询没回来时按 3 次算」：改成 0 会在查询回来前把保存按钮拦死。
  - **另一处漂移只报不修**：`handleMobileUpdateCheck` 发现新版本时，侧边栏弹更新对话框并记下版本号，个人资料页只弹一句 toast。同一个「检查更新」，按哪儿的结果不一样。该统一成哪种是产品决定（对话框现在挂在 `DashboardLayout` 的 JSX 里，个人资料页开不了它），所以留给你定，没有替你猜。
  - 新增 `accountPanelDuplication.test.ts` 把这笔账记住：两个文件里同名的处理函数名单**只许变短**，再多抄一个就红。反向对照两条都验过。

### 说明

- **重复逻辑扫描器修好了。** 上一版漏两类：不到 6 行的函数被过滤掉（`agentAssets` / `pluginRepository` 的 `normalizeVersion` 就是这么漏的，后来被守卫测试抓出来），以及只认 `function f(` 不认 `const f = () => {}`。修好后扫到 **1092 个函数体**（原来只看得到一小部分），并加了相似度排序 —— 同名且高度相似的是真重复，相似度低的多半只是撞名（比如插件版本号清洗 vs 版本比较）。这一轮的账户面板重复就是修好之后才浮出来的。


- **IPv6 换个写法，界面认、服务端报错**（链路管理）。多跳链路每一跳的连接地址，界面判「是不是这台主机配好的地址」用的是归一化比较（大小写、方括号都不算数），服务端用的是**精确字符串相等**。6 个样本里 3 个结论不同：
  - `2001:DB8::1` 对 `2001:db8::1`、`[2001:db8::1]` 对 `2001:db8::1`，界面都认为是同一个地址，服务端保存时报「连接地址只能使用入口地址、已配置的内网IP或IPv6地址」—— 而它明明就是。
  - 走界面时两边一般对得上（界面会把值换成主机上存的那一份再提交），**对不上的是存量数据和 API/Telegram 这类直接提交的路径**：主机的 IPv6 字段改过大小写、或者当初带方括号存进去，之后再保存这条链路就会撞上。
  - 判定收进 `shared/ipAddress.ts` 的 `sameNetworkAddress`，两边共用。**只统一「两种写法算不算同一个地址」，不放宽「哪些地址被允许」** —— 允许的仍然只有主机的入口地址、内网 IP 和 IPv6。
  - 顺带说明：同名的 `normalizeHopConnectHostsForHosts` 前后端各有一份，但那两个**不是重复** —— 一个是界面的表单归一（不合规就退回空），一个是服务端的校验（不合规就报错）。分层本该如此，没有强行合并。真正该共用的只有「算不算同一个地址」这一条。


- **写着「全站唯一一份」的版本比较，其实还有三份漏网**（结构）。`shared/version.ts` 的文件头写着这句话，并列出了当初合并掉的四处 —— 但当时漏了：
  - `client/src/lib/mobileNotifications.ts` 自己写了一份 `compareVersions`。差别在后缀：它按 `"."` 切，`2.3.364-beta.1` 的第三段成了 `"364-beta"`，`Number()` 得 NaN 归 0，于是这个版本被当成 `2.3.0` —— **比 `2.3.100` 还旧**。共用那份按 `[.-]` 切，得 364。11 对样本里 **4 对结论不同**，全在带后缀的版本上。
  - **今天还碰不到**：`ANDROID_APP_VERSION` 一直是干净的 `x.y.z`，所以这是潜伏问题，哪天发一个 rc 版才会发作（顺带一提，`android/app/build.gradle` 算 `versionCode` 时也是按 `"."` 切的，同一个坑）。
  - `server/agentAssets.ts` 的 `normalizeVersion` 和共用那份一字不差，换掉。
  - `server/repositories/pluginRepository.ts` 也有个 `normalizeVersion`，但它**不是同一件事** —— 那是插件版本号入库前的字段清洗，多了 64 字符上限和「空就当 0.0.0」的兜底。改名成 `normalizePluginVersionField` 并写明别顺手换掉：名字撞在一起，最容易被人「顺手统一」掉，把长度上限和兜底一起弄丢。
  - 新增一条守卫盯住这件事。**它一上来就抓出了上面后两处** —— 那两处是我自己前一轮的重复扫描漏掉的（那个脚本把不到 6 行的函数过滤掉了）。


- **端口策略界面和服务端说法不一致**（结构 / 易用性）。这套逻辑在 `client/src/pages/Rules.tsx` 里另抄了一份，5 个真实场景跑下来 **7 处判定不一致**：
  - **服务端的策略支持多段 `ranges`，界面那份完全不认。** 套餐发的端口段就是这么下来的（`portRanges: planRange.ranges`）。一个只有多段、没有单段的策略，在界面眼里等于「没有限制」。更糟的一处：策略同时有单段和多段时，界面把服务端会放行的端口判成**不可用** —— 那是功能少了一块，不只是提示不准。
  - **「不限制」时两边说法不同**：服务端说「不限制」，界面说「1-65535」—— 听着像配了个限制。现在统一成「不限制」。
  - **合并两条策略，界面那份逐个端口试 1..65535**，实测 **2.042 ms/次**；服务端是区间求交，**0.007 ms/次**（约 290 倍）。这是对话框里的 `useMemo`，换主机、换隧道都要重算。
  - `server/portPolicy.ts` 整个移到 `shared/portPolicy.ts`（它本来就没有任何 import，是纯逻辑），7 个服务端文件改导入，界面删掉自己那份 6 个函数加一个本地类型。
  - **今天还碰不到的那部分说清楚**：主机表没有 `portRanges` 列，所以多段策略目前只在服务端内部流转，界面对 `ranges` 的失明是**潜伏的** —— 一旦有接口把套餐端口段发给前端就会立刻发作。合并之后这个雷拆了。
  - 顺带记下一个**还没修的**：创建转发对话框显示的端口范围只算主机策略，不含套餐端口段（套餐段是服务端独有数据，没有任何接口把它发给前端）。租户会看到一个比实际可用范围更宽的提示，填了之后要等一次往返才被 `rules.checkPort` 拒掉。要修得先加一个查询接口，单独做。
  - 新增一条**防再抄一份**的守卫，反向对照验过（在 Rules.tsx 里重新定义一个同名函数，测试立刻指名该文件）。


- **环回地址换个写法就能绕过 SSRF 守卫**（安全）。拦内网/保留地址的判断在三个地方各写了一份，而且三份都不一样 —— 30 个样本地址里 **13 个三方判定不一致**。
  - `server/ssrf.ts`（插件、迁移等对外请求的守卫）用的是 `ip === "::1"` 加一串 `startsWith`。**`0::1` 和 `0:0:0:0:0:0:0:1` 是同一个环回地址的等价写法，两个都放行。**
  - `server/routers/lookingGlass.ts`（网络测试，拿用户的 Agent 主机发探测）同样是字符串前缀判断，除了上面那两个，还放行 `192.0.0.1`、TEST-NET 三段（`192.0.2.x` / `198.51.100.x` / `203.0.113.x`）和 `2001:db8::`。
  - **`::ffff:10.0.0.1` 在网络测试和归属地那两份里一路放行。** 这是 IPv4-mapped 写法，Linux 上连它就是连 `10.0.0.1` —— 这不是漂移，是两份都有的洞。
  - 三份合成 `shared/ipAddress.ts` 一份。规则取三份的**并集**，IPv6 先展开成 8 组再按位比（不靠字符串前缀，等价写法一律认得出），IPv4-mapped 拆出里面的 IPv4 再判。按用途给了两个出口：`isPrivateOrReservedAddress`（归属地、网络测试）和 `isRestrictedOutboundAddress`（SSRF，带 `allowPrivate`）。
  - **只收紧不放松，而且是验过的**：拿 1558 个地址（IPv4 全段抽样 + 各种 IPv6 等价写法）逐个比对新旧三份 —— **14 个地址由放行变拦截，0 个由拦截变放行**。有几段（`192.0/16`、`192.2/16`、`198.51/16`、`203.0/16`）比 RFC 的保留段宽，是原来那三份就这么写的，这次照旧保留：收紧一个安全守卫随时能做，放松要有确凿理由，「更贴 RFC」不算理由。
  - `allowPrivate` 的语义也写明白了：只放开内网段（RFC1918、CGNAT），环回和保留段一律不放 —— 包括 `169.254.169.254` 这个云元数据地址。「允许内网」的本意是让自建服务能连，不是连回自己。
  - 7 条测试，外加一条**防再抄一份**的守卫：全仓扫地址分类的特征写法（判 RFC1918 段、按前缀认 IPv6），`shared/ipAddress.ts` 之外再出现就红。两条反向对照都验过（把环回判定退回 `=== "::1"`；在 `ssrf.ts` 里重新抄一段分类）。


- **同一次面板升级，侧边栏和设置页报不同的进度**（界面 / 结构）。这段逻辑有**两份实现** —— 侧边栏一份（`DashboardLayout` 的 `getLayoutUpgradeProgress`），设置页一份（`Settings` 的 `getUpgradeProgress`），而且已经漂了。
  - **能同时看见**：Docker 构建必然打出 `transferring context` 这行，那一刻侧边栏说「**52%　下载或拉取资产**」，设置页说「**74%　安装并重启**」—— 你在设置页点的升级，侧边栏就在旁边。`pnpm install` 的 `Packages:` / `node_modules` 两条特征也一样只有设置页认。
  - 差异不止数字：完成时一个说「升级完成，正在等待面板恢复」，另一个说「升级完成」；空闲时一个说「等待确认升级」，另一个说「等待升级」。
  - 合成一份放进 `client/src/lib/panelUpgrade.ts`。日志特征取并集（设置页那份是超集，多认的三条都是真实构建输出），文案取更准的那一版。两个调用处各删掉 56 / 66 行重复实现。
  - 动手前先把分歧跑出来：把两份实现原样摘出来喂同一个任务对象，**4 个场景 4 个不一致**。不是推断，是跑出来的。
  - 补了 6 条测试（含回退文案、`job` 为空、`logs` 不是数组）外加一条**防复发守卫**：步骤文案「下载或拉取资产」在源码里只许出现一次，再抄一份就红。反向对照验过 —— 在设置页放一份同样的文案数组，守卫立刻指名 `pages/Settings.tsx`。
  - 顺带核了手册「不要假进度」这条：这里的百分比是从四个真实日志里程碑映射出来的，不是定时器凭空爬的，不冲突。


- **设置页 11 个开关，读屏里一个名字都没有**（无障碍）。开关在读屏里念出来是「switch, checked」—— 名字不会自动来自旁边那行字，除非显式关联。设置页一整页开关全无名称：用户连着听到十一声「switch, checked」，分不清哪个是「开放注册」哪个是「启用 HTTPS」。手册把这条列为硬性要求（读屏：图标按钮要有文字说明）。
  - 补了 **29 处**：设置页 23 个、用户/套餐/个人资料/流量计费 6 个。用的是**可见标题原文**，保证读屏念出来的和屏幕上写的一致（WCAG 2.5.3）。列表里的开关额外带上对象名 —— 「允许 张三 添加转发」，否则一屏十个开关全叫「允许添加转发」。
  - 侧边栏菜单那一组直接引用同一个常量 `SIDEBAR_MENU_LABELS[key]`，**零重复**，改菜单名时名称自动跟着走。
  - 真面板逐页验过，6 个页面 **47 个可见开关全部有可访问名称**（之前 /settings 是 11 个全无、/rules 是 12 个全有 —— 这个对照证明检测本身没问题）。
  - 新增一条**源码级**回归测试，它立刻抓出了真面板够不着的另外 **50 处** —— 藏在对话框、移动端设置、主机管理这些要交互才渲染的地方。
  - 那 50 处**没有一次补完**，列成了明账（棘轮）：新写的开关漏名字直接红，名单里的补好了不删也红。**为什么不一次补完**：脚本按「最近的中文」自动提名试过，一半提出来的是说明文字而不是名字（「低于阈值时提醒。」这种做可访问名称是错的）。硬套上去比没有更糟 —— 读屏用户听到一句和界面对不上的话，反而更难定位。剩下的要一处一处看上下文定。
  - 两个反向对照都验了：拿掉一个已补的名字 → 红在「新写的开关漏了」；给名单里的一处补上却不删名单 → 红在「请从待补名单里删掉」。
  - **后续把那 50 处补完了，待补名单已清空**。一处一处看上下文定的名字，没用自动提名：支付页 6 个（启用易支付 / 启用支付宝官方 …）、邮件页 5 个、转发组 11 个、用户页 5 个转发方式（「允许使用 iptables」，可见文字只有 `iptables`，光念协议名说不清是允许还是禁止）、主机页 3 个拦截开关（「拦截 HTTP」，同理）、隧道页 3 个「当前协议不支持」的禁用占位。
  - 插件表单那三处是**动态字段**，直接写 `aria-label={field.label}` —— 名称跟着字段定义走，零重复。转发组成员那一处复用了本文件已有的 `memberLabel(member)`，和同排输入框一个写法。
  - **测试自己也有个误报要修**：wouter 的路由组件也叫 `<Switch>`，第一版把 `App.tsx` 里的两处路由当成了没名字的开关。现在只认从 `components/ui/switch` 导入的那个。
  - 真面板 11 个页面复验：可见开关全部有可访问名称。

- **更正：上一条的「待补名单已清空」是拿一把瞎的尺子量出来的**（无障碍）。那条测试按行扫 `<Switch`，正则是 `/<(Switch|OptimisticSwitch)[\s/>]/` —— `<Switch` 后面直接换行、属性写在下面几行的写法，行尾没有第三个字符可配，一个都匹配不到。全站 **31 个**这样写的开关因此从未被看见，名单「清空」是假的。改成整文件的标签栈扫描后它们全部现形。
  - 同时补上两条**同样实测过**的名称来源，之前的检查一概不认，所以那 31 个里有一部分其实是误报：`title=`（Chrome 里来源为 attribute）、以及被 `<label>` **包着**（来源 labelwrapped）。去掉误报后，全站真正没名字的是 **87 处**：开关 21、下拉 50、输入框 16。
  - 还更正一条**我先前说反了的结论**：`<label for>` 对 `<button>` **是有效的**。Radix 的开关和下拉渲染出来都是 button，照样能被 label 命名。反过来，下拉（`role=combobox`）**不从内容取名** —— `<SelectTrigger><SelectValue /></SelectTrigger>` 里显示的当前值，读屏一个字都念不到。这两条都在 Chrome 的 `Accessibility.getPartialAXTree` 里单独验过，不是照规范推的。

### 变更

- **层级改用令牌，两个「随手写的大数字」清掉**（结构）。手册第五节给了六档，并要求「用变量统一管理，禁止随手写大数字」。
  - **先普查，结果第一次就漏了**：按 CSS 类名和 `z-index:` 声明搜，全站找出 1 / 30 / 70 / -1 四个越界值。**漏掉了最大的那个** —— toast 的 `zIndex: 200` 写在 JS 内联样式里，既不是类名也不是 CSS 声明，两种搜法都碰不到。补上 `zIndex:` 才看见。
  - 换掉的是真正没道理的那两个：toast 的 `200` 和日期面板的 `z-[70]`，现在走 `shared/design-tokens.css` 的令牌。
  - **写明了一件之前没人写下来的事**：对话框、下拉、菜单、气泡、日期面板、toast 全都 portal 进 `#forwardx-overlay-root`，而那个容器是 `isolation: isolate` —— **容器内的 50/70/200 只和彼此比较，跟页面上的 0~60 不是一套坐标系**。所以令牌分了两组，名字写明「容器内」，手册文档里也补了那张表。以前这套次序只存在于几个魔数里，新加一个浮层只能靠猜。
  - 手机底栏从 30 抬到手册的吸顶档 40 —— 原来它和表格冻结列同为 30，谁在上全看 DOM 顺序。
  - **证明没改动行为**：改之前把 6 个页面（含打开对话框）里每个有层叠值的元素抓了一份快照，改完再抓一份，**每页的层叠值分布完全一致**。另外单测了三种机制真的解析得出原值：Tailwind 的 `z-[var(--fx-z-in-overlay-base)]` → 50、`z-[var(--fx-z-in-overlay-popover)]` → 70、React 内联 `zIndex: "var(--fx-z-in-overlay-toast)"` → 200。手机底栏实测 40，且在它自己的位置上是最上层。
  - 剩下的越界值没有硬改，而是**记账**：表格冻结列（30，只和同表格单元格比先后）、页面基座（背景视频 -1 配 `#root` 1）、看板娘浮窗（45，要压住吸顶头部又不能盖弹窗）。硬套档位会动层叠顺序，收益却是用户看不见的。
  - 新增 `zIndexTiers.test.ts`：写一个不在六档、也没记在案的数字就红；在案清单只许变短；另有一条钉住那两个令牌不许写回数字。三条反向对照都验过（随手写 `z-[999]`、把 toast 写回 `200`）。


- **30 个密码框补上「显示 / 隐藏」**（易用性 / 无障碍）。手册把这条列为硬性要求，而全站 32 个密码框里**只有登录页那两个有** —— 其余 30 个只能盲打。填 API Token、商户密钥、SMTP 密码、Cloudflare Token 这类又长又杂的串时，看不见就只能靠重填一遍来确认自己没敲错。
  - 新增 `ui/password-input.tsx`，图标、位置、文案、无障碍名称只有一份。登录页原来那两个**手写的**也合并进来了 —— 留着就是第二套行为，迟早分岔。
  - 合并时修掉了手写版的两个毛病：切换按钮原来写着 `tabIndex={-1}`，**键盘用户根本聚焦不到**「显示密码」；按钮也没有 `aria-label`，读屏只念一声「按钮」。现在可聚焦、有名称、有 `aria-pressed`（读屏能念出当前是显示还是隐藏）。
  - 真面板验的：个人资料页三个密码框各有一个「显示密码」按钮；点一下 `type` 从 `password` 变 `text`、名称变「隐藏密码」、`aria-pressed` 变 `true`；从输入框按 Tab **落在切换按钮上**（正是 `tabIndex={-1}` 挡掉的那一步）。手机视口下按钮 **44×44**，不溢出输入框。
  - 新增 `passwordReveal.test.ts`：密码框只许通过 `PasswordInput` 写，写死 `type="password"` 或自己拼明文/密文切换都会红；另一条钉住组件自己的 `aria-label`、`aria-pressed`、可聚焦和焦点环。反向对照验过。
  - **测试自己写错一次**：第二条先前红在组件的**说明注释**上 —— 那段注释正是在解释「登录页原来写了 `tabIndex={-1}`，所以这里不写」，而断言没剥注释。现在先剥注释再查。
  - 无障碍棘轮也跟着补了两处：认了 `PasswordInput`（否则 30 个控件突然从扫描范围里消失，底线断言 `total >= 500` 立刻红 —— 这条断言正是为这种情况留的），并且把透传 `{...props}` 的控件算作「名字由调用处给」，免得以后每加一个包装组件都要改名单。


- **100 个「改完要点保存」的开关改成复选框**（界面 / 易用性）。手册把分工定得很死：**Switch 用于「切换后立即生效、无需提交」，需要提交的用 Checkbox**。
  - 这不是外观偏好。一个亮着的开关在用户眼里就是「已经生效了」；如果它其实要等点保存，那它一直在说谎 —— 关掉对话框改动就没了，界面上看不出来。又是这个项目反复出现的那类毛病：**A 变了，B 没跟上，而界面上看不出来**。
  - 判定按 `onCheckedChange` 里实际干了什么：调 `.mutate()` / `onCheckedChangeAsync` 是立即生效；调 `setXxx` / `setForm` / `updateXxx` 是写进本地草稿、要提交。全站 132 个开关里，**95 个是要提交的**，另外 5 个是包装组件或只读布尔展示，一并处理；剩 26 个保持 Switch。
  - **更正一次报数**：第一版审计我只扫了对话框里的开关，报的是「49 处」。这个筛选条件本身就错了 —— 手册的规则是「要不要提交」，和在不在对话框里无关。按规则正确套用是 95 处，设置页一页就占 23 个。
  - 新增 `ui/checkbox.tsx`，用**原生 `<input type="checkbox">`**，不引第三方组件：键盘（空格切换）、`<label>` 关联、焦点、表单语义全是浏览器自带的，和手册「不要用 div 模拟按钮」是同一个道理。props 刻意和 `Switch` 对齐（`checked` / `onCheckedChange`），所以现场替换就是改个标签名，其余一个字不动。
  - **差点一次性发出 100 个几乎看不见的复选框**：默认 `border-input` 用的是 `--fx-stroke`，实测未选中状态的边框对比度只有 **1.31:1**（浅色）/ **1.56:1**（深色），远低于 WCAG 1.4.11 对非文本控件要求的 3:1 —— 一个 20px 的空方框全靠边框才看得见。项目里本来就有 `--fx-control-stroke` 专门干这个，输入框、文本域、下拉都在用，把复选框接进同一条规则后是 **3.37:1 / 4.26:1**。
  - 量这个对比度前后写坏了三版探针（正则拿 `oklab()` 当 rgb 解析、手写 PNG 解码器把浅灰读成纯黑），最后改成在页面里用 canvas 让浏览器自己解析颜色和叠加半透明，并给探针加了三条自检（黑白 21:1、#767676 对白 4.54:1、50% 黑叠白得中灰）。自检通过的那一版才采信。
  - 新增 `switchVsCheckbox.test.ts` 把这条规则钉住：要提交的用了 Switch 会红，立即生效的用了 Checkbox 也会红；判不了的（`disabled checked={false}` 那种「当前协议不支持」占位、回调透传的包装组件）放过，宁可漏不可误杀。反向对照验过：把设置页一个复选框退回成开关，测试红在那一行。
  - 无障碍棘轮也认了 `Checkbox`（它渲染出来是 `<input>`，但 placeholder 对它没意义，所以不给 placeholder 兜底）。真面板 13 页复查：67 个可渲染控件，无名 0。


- **补上手册点名的两个效果：边框光束、流动渐变背景**（界面）。手册第七节点了四个效果，毛玻璃和进度条项目里本来就有，这两个**一处都没有** —— 全站搜 `offset-path`、`animate-float`、`blur-3xl` 都是零。
  - **边框光束**用手册的新写法：一颗发光的点沿 `offset-path: rect(0 100% 100% 0 round 12px)` 跑边框。手册明确把「conic-gradient 旋转 + mask 只留边框」列为**旧方案**（理由是「那是渐变」），所以没走那条路。纯 CSS，零 JS。
  - 用在**真的有事在跑**的卡片上：网络测试结果卡（ping/mtr 动辄十几秒）和 iperf3 服务端卡的启动/停止过渡态。手册写的是「不做常驻装饰」，所以 iperf3 的 `running` 稳定态**不亮** —— 一直亮着的光束等于没有光束。
  - `offset-path: rect()` 是 Chrome 116+ 的语法。不支持的浏览器里那颗点会停在左上角不动，是个莫名其妙的小黑点，比没有更糟，所以整层裹在 `@supports` 里，命中才显示。
  - **流动渐变背景**放在登录页 —— 手册点名的用法之一（登录页 / Hero / 空状态），工具型页面一律不用。三团的**负 `animation-delay`（-1.2s / -2.4s）**是关键：同频是「呼吸」，错开相位才是「流动」。
  - 顺手清掉一个死元素：登录页原来有个 `.auth-grid-overlay`，CSS 里是 `display:none`，也就是说那个 div 从头到尾什么都没画。
  - 颜色进了 `shared/design-tokens.css`（浅深两套成对），不写死在组件里 —— 文档站共用这个文件，而且改一个忘一个是迟早的事。
  - **移动端降级**：`< 768px` 撤掉三团 `blur(64px)`，换成等效的 `radial-gradient`（手册点名的做法）。**减少动态效果**下光束整个撤掉，渐变团保留但不浮动。
  - 验证：真面板注入组件真实 DOM，四种条件各量一遍 —— 浅色/深色（`.dark` 类，不是 `prefers-color-scheme`）、390px、`prefers-reduced-motion`。光束的点位在 1 秒内确实移动；深色下令牌切到 `rgba(66,76,88,.62)`；390px 下三团 `display:none` 且背景变成 radial-gradient；减少动效下光束层 `display:none`、渐变团 `animation:none`。
  - 另外验了一件差点误改的事：光束层是卡片的第一个子元素，直觉上会被后面带底色的 `CardHeader` 盖住。**实际不会** —— 绝对定位元素在绘制顺序里排在静态兄弟之后。用像素对比确认（有光束/无光束两张图在该处不同），所以**没有**加多余的 z-index。这一条前后测错了三次（取样点在卡片外、`pointer-events:none` 的元素 `elementFromPoint` 永远取不到、`offset-distance` 要等一帧才落到 `getBoundingClientRect`），最后给探针加了自检才问出正确答案。


- **全站表单控件补齐可访问名称：87 处**（无障碍）。下拉占了 50 处 —— 这一类以前从没被查过，而它恰恰是「看得见、听不见」最严重的：筛选器上明明写着「最近 3 小时」，读屏只念一声「combobox」。
  - **优先走已有的机制，而不是再抄一遍文案**。项目里本来就有 `FormField`：它用 context 发一个 id，`Label` 拿去当 `htmlFor`，`Input` / `Textarea` / `SelectTrigger` 拿去当 `id`。旁边已经有可见 `<Label>` 的 **45 处**，直接把外层 `<div>` 换成 `<FormField>`（渲染出来还是同一个 `<div {...props}>`，DOM 一字不差），名字从此只有一个出处，不会漂。
  - `Switch` 也接进了 `FormField`（原先只有输入框和下拉接了）。接之前先扫过一遍：现有 `FormField` 里没有任何一个装着两个控件，所以不会撞 id。
  - 剩下 **42 处**界面上根本没有可见标签（筛选下拉、行内开关、「添加成员」这类），只能写 `aria-label`。列表里的一律带上对象名 —— `${node.name} 启用`、`${item.ruleName} 选择落地节点`，否则一屏十个开关全叫同一个名字。动态字段直接引用变量（`aria-label={field.label}`、`aria-label={title}`），零重复。
  - 支付页 38 个输入框共用的 `Field` 包装器**推倒重写了一次**：第一版自己用 `useId` + `cloneElement` 造了一套注入，写完才发现和 `FormField` 是同一件事。删掉，改成三行的 `FormField` 薄封装，38 个调用处一个字没动。
  - 测试合并成一个 `accessibleNames.test.ts`，开关、输入框、下拉一起管 —— 两个检查器各写一套正是这次漏检的根源。扫描器换成整文件标签栈，多行标签、包裹式 `<label>`、`FormField` 上下文都认得，并且为这三种写法各留了一条回归用例。
  - **反向对照两条**：拆掉 `Hosts` 一个 `aria-label`、把 `MobileAppSettings` 一处 `FormField` 退回 `<div>` —— 测试分别红在这两行，恢复后转绿。
  - **真面板 13 个页面复验**：67 个可渲染控件，无名 0。走查本身也做了反向对照 —— 在页面上把 `aria-label`/`for`/`placeholder` 全删掉，同一份脚本立刻报 23/23 无名，证明这个 0 不是脚本没在看。
  - 已知边界写进了测试注释：控件先存进变量、再塞进别处 `<label>` 里的写法（隧道页的 `renderTransportSwitch`）静态扫不出来，那几处直接写了 `aria-label`，没去教脚本追数据流。

- **只有图标的按钮补名称：20 处**（无障碍）。编辑、删除、复制这些按钮里只有一个 svg，读屏念出来就一声「按钮」。列表里的一律带对象名（`编辑 ${rule.name}`、`删除 ${plan.name}`、`复制${label}`），一排五个「编辑」按钮听不出编的是哪一条。
  - **先前那次统计虚高了 3.5 倍**：原始数字 177，其中 53 个图标按钮本来就有 `title=`、74 个输入框本来就被 `<Label>` 包着 —— 这两种都是有效的名称来源，我当时的脚本一个都不认。真实数字是 50。
  - 这条检查**故意做得比控件那条弱**：JSX 里「按钮内容是不是只有图标」只能按文本粗判，`{pending ? <Loader2 /> : <Send />}` 这种分支会被当成有文字而放过。宁可漏报不误报 —— 误报会逼人给本来就有文字的按钮再加一个 `aria-label`，两者一旦不一致，WCAG 2.5.3（名称须含可见标签）反而挂了。漏的那部分由真面板走查兜。
  - `ui/sidebar.tsx` 那个折叠按钮**没有动**：它里面有 `<span className="sr-only">切换导航栏</span>`，实测这就是它的名称来源。

### 说明

- 合并后的 UI 重新走查了一遍（v2.3.364 的全站 UI 重构 + 此前的移动端修复）：**320px 横向滚动 8 个页面全部为 0**，触控目标在触屏下全部 44px，桌面端尺寸未变。早前那批「33x18 开关」「43x43 按钮」经核实都是探针误报（前者量的是视觉框而非命中区，后者是逐像素外探的 off-by-one）。

## [2.3.364] - 2026-09-19

### 发布摘要

- 全站 UI/UX 统一重构：公开首页、工作台、登录部署、功能页和使用文档共用排版、灰阶表面、细边框与轻阴影，适配浅深色及移动设备。
- 参考 UI Handbook 源码完善公开使用流程；首页提供“使用服务 / 管理网络”角色引导、路径示意和常见问题，按真实注册状态显示入口。
- 工作台新增功能搜索（Ctrl / ⌘ K）与手机底部快捷导航；导航沿用当前用户权限，手机抽屉支持焦点管理、Escape 关闭及焦点恢复。
- 面板与 APK Release `2.3.364`，Android APP `2.3.99`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`。Agent 与 FXP 源码未变化，已安装的 Agent 无需升级。

### 界面与交互

- 统一业务页标题、主操作、统计、表格、表单和弹窗层级；转发与链路按节点呈现入口、中继、出口。
- 分类导航改为单行滚动，手机低频筛选按需展开；主机维护动作收纳到菜单，账单入口开关改为紧凑行。
- 补齐 193 处字段标签关联，统一触控尺寸、焦点提示、加载、空状态和错误重试；总览明确区分新工作空间、读取失败和运行正常。
- 登录页沿用站点名称和图标；VitePress 文档接入共享设计令牌。自定义 HTML 和第三方插件内容保持原有控制权。

### 验证

- 重构 PR #41 已合并；Node 22 全量 1461 项测试、TypeScript、应用与文档构建及 Agent/FXP 检查均已通过 GitHub CI。
- 浅深色正文与辅助文字对比度超过 4.5:1，输入边界超过 3:1；提供四档宽度的独立交互预览。
- 尚未完成 iOS 真机触控及完整线上业务验收，检查范围见 `docs/UI_REDESIGN_2026.md`。

## [2.3.363] - 2026-09-18

### 发布摘要

- 汇总上一正式版以来的重构：共享逻辑归一、首屏按需加载、查询性能修复、转发清理一致性及移动端界面优化。
- Phase 7 全量测试入口已纳入 CI 和面板/APK 发布检查；此前提交的 1455 项测试、Agent/FXP 测试与静态检查均在 GitHub runner 通过。
- 面板与 APK Release `2.3.363`，Android APP `2.3.98`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`。Agent 与 FXP 源码未变化，已安装的 Agent 无需升级。
- 验收边界：未进行真实 VPS 吞吐、实际付款或 iOS Safari 真机测试，详见 `docs/PHASE7_VALIDATION_CN.md`。

### 修复

- Phase 7：测试入口自动发现全部测试文件，补入原默认命令漏掉的 8 个文件；setupFlow 迁移测试不再依赖容器网卡。
- 主机与链路分类在窄屏自动换行，补齐图标按钮的无障碍名称与视图选中状态，完善触屏操作区尺寸和间距。验证范围与环境限制见 `docs/PHASE7_VALIDATION_CN.md`。

- **内核态转发那两支合并成一处**（重构，行为一字不改）。`iptables` 和 `nftables` 原来在心跳路由里各写一遍 else-if，加起来 35 行，而它们只差三处。现在收进 `buildKernelForwardCmds`。
  - 保留的那个真差别写在注释里：**计数链只有 iptables 这一路下**。nft 构造器自带计数器，再下一遍 iptables 计数链会把同一份流量数两次，而账面上看不出来 —— 这正是合并时最容易顺手抹平的一条，所以单独加了一条测试盯它。
  - 反向对照把那条差别抹平之后，**两张网都红了**：单元测试红在「nftables 不该再下一遍」，命令基线红在条数（53 ≠ 基线值）。
  - 访问限制那一段仍由调用方传进来 —— 它依赖每次请求各不相同的用户配额，不属于这一层。
  - 自我修正：我第一版顺手抽了个 `buildForwardApplyAction` 想统一六种的动作对象，注释里写「六种各拼一遍一模一样的字面量」。**核对下来是假的** —— 六种的字段差别很大（`tunnelId`/`statusType`/`fxp`、`svcName`/`unit`、`failover`、`backendPort` 各不相同），共有的只是七个核心字段。而且合并之后内核那一支只剩一处字面量，这个 helper 已经不去重了。撤掉，只留真正有价值的那半。

- **停用那条路径上手抄的六种清理分支，删掉了**（重构，行为几乎一字不改）。心跳路由里本来就有一个完整的 `buildDisabledRuleRemovalAction`，六种转发方式一应俱全；而「用户手动停用」那条分支又把同样六种各手抄了一遍 —— 只有 `nftables` 一支是调过去的，其余五支是复制品。**145 行换成 10 行。**
  - 上一条的 nginx 漏清守护后端就是这么来的：两份列表各改各的，改了一边忘了另一边。两份手抄的东西不可能长期保持一致，唯一的办法是只留一份。
  - 36 组指纹里 **33 组逐字节不变**。变的 3 组全是 realm 手动停用那一路，而且**新的顺序恰好等于关协议那一路一直在发的那一份** —— 不是新行为，是本来就装在真机上跑着的那个顺序。哈希对得上，不是我说它等价。
  - 合并之后两条路径的 36 组指纹**完全相同**。基线里那条「差异只剩 realm 的顺序」按计划红了，改成了「一处都不该差」—— 它现在防的不是「又抄漏了」（结构上已经不可能），而是**有人再开一条新的手抄路径**。
  - 两个反向对照：让合并后的构造器少清一样东西 → 两条路径的基线**同时**红（这正是「已经是同一段代码」的证据，而一致性那条保持绿也是对的，因为两边同样退化）；重新给 nginx 开一条手抄的简化清理 → 四条断言一起红。
  - 自我修正：第一次做对照 1 时它一条都没红，我没放过 —— 查出来是**对照本身是空操作**（匹配串末尾没有换行，带 `\n` 的替换压根没落地）。和之前 `String(value)` 那次同一类错：对照不红，先怀疑对照。

- **规则名称不再必填，留空按目标地址自动生成**（行为变更）。这是「创建转发极简」最后一块：名字既不影响转发怎么走，也不影响流量算在谁头上 —— 纯粹是列表里认人用的，为它拦住「填完端口和目标就能保存」不值得。
  - 生成规则优先用目标（`10.6.6.6:7171`），因为那最能说明这条转发是干嘛的；没有目标才退到 `转发 24666`。**占位符直接显示会生成成什么**，不用猜。
  - IPv6 要带方括号 —— `2001:db8::1:80` 分不清哪段是端口。
  - **校验从三处收敛成一处**。原来「缺什么」在三个地方各写一遍：按钮的 `disabled`、footer 的提示、`handleSubmit` 里那七段 if。改名称必填与否时我只改了前两处，结果按钮亮着、点下去弹「请填写所有必填字段（目标端口必须填写）」—— 自相矛盾。现在三处都读 `submitBlocker`。顺带把「没有可用转发链 / 转发组 / 隧道权限」也并进去：原来它们只在点击后弹 toast，按钮却是亮的，正是手册说的「点了没反应」。
  - **一处必须说的自我修正**：第一版我认定「只有一个落库点」，因为 `grep name: input.name` 只命中一处。**这个判断是错的** —— 另一条路径写的是 `db.createForwardRule({ ...input, ... })`，直接展开 input，grep 照不到。结果是：隧道模式建规则，**toast 报成功、对话框关闭、库里名字是空字符串**。
    - 是怎么抓到的：没有停在「toast 说成功」，而是回头查了库。界面上完全看不出这个 bug。
    - 修法不是再补一处，而是把兜底移进 `createDirectForwardRuleForActor` —— 它是导出的，面板和 Telegram 机器人都走它，搁在调用方就得每个调用方各补一遍。
  - 新增 5 条测试盯生成器（目标优先、IPv6 方括号、没端口不留光秃秃的冒号、源端口 0 表示随机不能拿来当名字、只有空白算没填）。

- **创建按钮点不了的时候，旁边说一句缺什么**（UI）。手册的规矩是「到达边界要 disabled 对应控件，而不是点了没反应」—— 但光 disabled 还不够：用户盯着一张看起来填满了的表，按钮灰着，无从下手。
  - 关键不是加一句提示，而是**判断只留一处**。原来那串九个条件的布尔表达式只长在按钮的 `disabled` 上；现在抽成 `submitBlocker`，返回「还差什么」那句话本身，`null` 表示可以提交。**按钮和提示读同一个值**，不可能再各说各话 —— 这正是这一轮反复在修的那类毛病（A 变了，B 没跟上，而界面上看不出来）。
  - 按用户填表的顺序报，且**只报第一个缺口**：一次列三条缺失反而没人读。
  - 真面板逐步验过，七个状态全部跟得上：
    ```
    刚打开           还缺目标地址      禁用
    填了目标地址     还缺目标端口      禁用
    填了目标端口     还缺规则名称      禁用
    填了名称         （空）            可点
    改成已占用端口   源端口已被占用    禁用
    改回可用端口     （空）            可点
    ```
  - 提交中不显示 —— 那时候按钮自己写着「处理中...」。提示挂 `aria-live="polite"`，读屏用户也能听到缺口变化。

- **操作结果提示统一成「已 X」，并修掉一处我自己上一轮标错的必填**（文案）。
  - 先量后改：全站中文串 2599 条，其中**「已 X」式 168 处、「X 成功」式 13 处** —— 房规本来就是「已 X」，13 个是漏网的。改了 9 处（主机已更新 / 主机已添加 / 密码已修改 / 用户已创建 / 规则已创建 / 规则已更新 / 已兑换 / 已支付 / Agent 已升级）。
  - **四处故意不改**，机械替换会改坏：「升级成功 / 回退成功」是任务状态标签，和「正在升级」「升级出现异常」成组出现，改一个破坏对仗；**「注册成功」不能改成「已注册」** —— 后者读起来像「这账号早就注册过了」，是个错误提示；「部分成功」「动作执行未成功」本来就不是成功提示。
  - toast 句尾标点也量了：**476 条不带、8 条带句号**。把那 8 条的句号去掉。
  - **自我修正（要紧的一条）**：上一轮我把「规则名称」标成了「选填」，理由是「它是整张表单唯一不必填的字段」。**这话是假的** —— 创建按钮的禁用条件里明明写着 `!form.name`。照着我那个标签留空的用户，只会得到一个灰按钮和零解释，**比我改之前更糟**。现在和其余三个一样标星号。
    - 是怎么发现的：填完表去验 toast，创建按钮死活是灰的。没有去怪 Playwright 选择器，而是回头读了禁用条件。
  - 规范里补了这三条（「已 X」的三类例外、toast 不加句尾标点、标了选填就必须真能不填），把这次的教训写成前车之鉴。
  - 真面板验过：走完整条创建流程，toast 实际渲染为 `规则已创建，源端口 24777`。

- **手机上按钮点不满 44px，320px 宽还有 6px 横向滚动**（UI 走查，按 `docs/UI_HANDBOOK_CN.md`）。手册这两条是硬指标：可点面积 ≥ 44×44px（手指能舒服按中的最小尺寸），320px 宽不允许左右滚动。之前没量过。
  - **触控目标**：桌面那套高度是 32 / 36 / 40px，搬到手机上命中区只有 35~39px，头部图标按钮 35×35。现在在 `@media (pointer: coarse)` 下抬到 44px —— **桌面端一个像素都不动**，桌面用鼠标，32px 的密集工具栏是对的。改完真机量：触屏 12/12 都是 44px，桌面 1440px 的高度分布（24/32/36/40）原样不变。反向对照去掉这段 → 13/13 全部退回不达标。
  - **没有用「透明 ::after 撑命中区」那招**，虽然手册也提了这个做法：量过同一批控件之间只隔 4px（手册要求 ≥8px），撑出去会盖住旁边的按钮 —— 把「点不中」变成「点错个」，更糟。抬高实际高度不会让相邻元素靠得更近，是这里唯一安全的做法。
  - **320px 溢出**：只有系统设置页，6px。真凶是卡片的 `min-width: auto`（CSS Grid 子项默认值）在 296px 的格子里撑到 314px —— 经典的 grid 溢出。三处卡片网格加 `min-w-0`，八个页面复量全部归零。
  - 两处自我修正，都是**先怀疑量法再下结论**：
    - 第一版用 `getBoundingClientRect()` 量，报出「42 个可点元素 42 个不达标」。**这个数字是错的** —— 它量的是视觉框。列表里那个启用/停用开关视觉上 33×18，但 `.switch-compact::after` 早就把命中区撑到约 49×40 了。改用 `elementFromPoint` 从中心逐像素外探，真实数字是每页 2~15 个。
    - 第二版把设置页那个横向滚动的标签条当元凶，也是误报 —— 它外层本来就有 `overflow-x-auto`，在自己的滚动容器里。排除掉滚动容器内部的元素之后才找到真正越界的卡片。

- **创建转发的表单按「线路 → 端口 → 出口 → 名称」重排**（UI，按 `docs/UI_HANDBOOK_CN.md`）。
  - 先说没做什么：量下来这张表**本来就只有 7–8 个字段、四个标签页都不用滚动**。所以没有把它拆成多步向导 —— 「创建转发极简」这件事它已经基本做到了，硬拆只会多两次点击。真正的毛病在**顺序和视觉权重**，不在步数。
  - **「规则名称」原来排在第二**，而它是整张表单**唯一不必填**的字段。现在挪到最后，并标上「选填」—— 原来不标，用户只能靠试。
  - **必填星号只有「目标端口」有**，源端口和目标地址同样必填却不标。三个统一标上。
  - **每种线路配一种底色**（端口转发绿、隧道橙），整块占对话框约四分之一。手册的规矩是强调色约占 10%、只留给最重要的动作和信息 —— 一块背景色不属于。三处都换成中性卡片。转发方式徽章（`iptables` / `GOST`）同理去色：它标的是「用什么工具转」，是说明不是动作。
  - **异常 TG 提醒原来和「目标端口」并排同级** —— 一个是核心必填，一个是可选通知。而且没配机器人时它是禁用的，却只有开关点不动、整块看不出禁用。现在移出必填区，未配置时整块 60% 不透明度。
  - 真面板验过：四个标签页逐个截图，并**完整走了一遍隧道转发的创建**（源端口 24123 → 10.9.9.9:8080），存进库的是 `id=37 tunnelId=1 forwardType=gost`，字段一个不差。对话框关闭后列表里没立刻看到是分页（17 条、每页 12），不是没存成。

- **隧道列表页每行打三次库**（性能）。翻一页 12 行要打 41 次：每条隧道各查一次中继跳、一次额外落地节点，再逐台查主机。`pageSize` 上限是 100 —— 一次翻页三百多条，而这页还在轮询。
  - 批量版本**仓库里早就有**（`getTunnelHopsByTunnelIds`、`getTunnelExitNodesByTunnelIds`、`getHostsByIds`），心跳路由和可用性汇总都在用，只有这个列表页还在循环。
  - 改完 `5 + 3×行数` 变成**恒定 7 条**，不随页大小变。pageSize=12 时 41 → 7，pageSize=100 时 305 → 7。
  - 省了不等于对：拿改动前那个提交建了 worktree，同样的数据（多跳中继、额外落地节点、离线主机、停用节点都有）跑同一个请求，**整份响应逐字段完全相同**，包括三种可用性判定。
  - 新测试钉的第一件事是**条数不随页大小涨** —— 这才是真正拦 N+1 的那条；只订绝对值的话，一个「每行多查一次」的写法在小页上照样能过。
  - 另一条钉的是分组不能串台，还有一处**故意的不对称**：`hopHostIds` 会先滤掉 `hostId <= 0` 的坏跳再判「够不够两跳」，而 `hopConnectHosts` 不滤、原样保留每一跳的位置。两个列表长度不同看着像 bug，顺手抹平的话界面上「第二跳走哪个域名」会错位到第一跳去。
  - 三个反向对照各红在自己那条上：撤回修复 → 条数那条红并打印出增长曲线；分组串台 → 中继链那条红；把两个列表的过滤规则抹平 → 不对称那条红。
  - 顺带量了另外几个热列表，都干净、都不随规模涨：转发列表 5 条、转发地图 3 条、转发摘要 12 条、主机列表 6 条（10 台机器 50 条转发 → 40 台机器 200 条转发，条数一个没变）。首页那两个走缓存，这轮没量到。

- **给「停用/删除」那半边也铺上命令基线**（重构安全网，不改行为）。apply 那一路上一轮已经有基线了，这一组补的是另外半边：六种转发方式 × 三种协议 × 两条停用路径，一共 36 组指纹。
  - 为什么先铺网再动刀：这几条路径加起来是十几份手抄的清理命令列表，已经因此漏过一次（上一条的 nginx）。要把它们合并成一处，就得先能证明「合并之后一条不差、一条不多、顺序不变」—— 这些命令是要在真机上删 iptables 规则、停 systemd 单元、删配置文件的，靠读代码判断「应该一样」不算数。
  - 顺带把当下的分歧量了个准：修完 nginx 之后，**六种里五种两条路径完全一致**，只剩 realm 不一致 —— 而且 48 条命令集合完全相同，只是「删自己的配置文件」那一条排在不同位置（一个在清守护后端之前，一个在之后）。两条都在线上跑着，功能等价。
  - 这件事单独订了一条断言写在明面上，而不是把两条路径的哈希各记一份了事。合并做完之后它会红 —— 那是它该做的事：到时候改掉它，别绕过它。
  - 两个反向对照：少下一条清理命令 → 条数那条红；条数不变只改一条命令的文本 → 哈希那条红。

- **停用一条 nginx 转发，故障转移的守护进程留在机器上不走**。面板里有三条路径会下发「这条转发别跑了」：用户手动停用、管理员在设置里关掉整个协议、规则被删除。三条各写各的清理命令，六种转发方式各写一遍 —— 十八段手抄的列表，谁也不盯着谁。抄漏了一处。
  - 真库量过，六种方式两条路径的守护后端清理条数：iptables 0/0、nftables 0/0、realm 5/5、socat 5/5、gost 5/5，**只有 nginx 是 0/5**。手动停用不清，关协议才清；删除那条路径也是清的，连同一条 else-if 链上二十行开外的 `nginx-tunnel` 分支都是清的 —— 独独它自己漏了。这是抄漏，不是设计。
  - 会怎么坏：守护后端（`forwardx-realm-guard-<端口>` 这些）是故障转移的本地兜底进程，占着一个本地端口往真实落地转。一条配了故障转移的 realm 转发被改成 nginx 的同时停用（改类型那一步的 apply 压根不会跑），它的守护进程就永远留在机器上 —— 而面板上那条规则明明写着「已停用」。
  - 修法是补上漏掉的那一行。新加的测试不订绝对条数（那会跟着功能变），只订**同一种转发方式，两条停用路径必须处理得一样** —— 这才是能拦住下一次抄漏的那条。另一条订「四种用户态都要清、两种内核态不该清」（清理是按端口做的，所以不能只清自己会创建守护后端的那两种）。
  - 两个反向对照：撤回修复 → 两条断言都红，其中一条正是原来的症状；给内核态也加上清理 → 同样两条都红。

- **下发给落地机的 GOST 运行时配置，之前一行测试都没有**。一台机器上所有走 gost 的转发（直连的、走隧道的）最后压成同一个 JSON，Agent 拿它覆盖 `/etc/forwardx/runtime/gost.json` 再重启服务。它是全站最要害的一份生成物，而它错了**不会有任何人报错** —— GOST 照样起来、端口照样通，只是走错了路。
  - 命令基线那一组也盖不到它：那组只看每条规则各自的动作，而这份配置是跨全机规则聚合出来的，挂在一条 `gost-runtime-sync` 动作上。
  - 新加的这组钉两层。**结构断言**说得出「哪里错了」，每一条都对应一种真会发生、但界面上看不出来的坏法；**内容哈希**说不出哪里错，但什么都漏不掉，是重构这片时的安全网。
  - 六条结构断言里最要紧的一条：**走隧道的规则，handler 上必须挂着链**。链掉了，GOST 起得来、端口通、面板显示「运行中」，只是流量从入口机直奔目标，整条隧道被跳过 —— 用户完全看不出来，只有对端日志里的来源 IP 会变，而那正是买隧道要解决的事。反过来也钉了：直连规则不该挂链，否则白绕一趟还按隧道计费。
  - 其余几条：`both` 协议拆出的两个服务共用端口但名字不能撞（撞了 GOST 只保留一个，另一个协议无声地不转发）；UDP 监听的 `readQueueSize` / `backlog` / `ttl` 一项都不能少（漏了就退回 GOST 默认值，稍有并发就丢包，表现为「有时候连不上」）；隧道限速要落成 limiter 并且被服务引用（没引用就是不限速）；转发目标写在 forwarder 上，隧道走的是 chain 那一层。
  - 三个反向对照各红在自己那条结构断言**加**哈希上 —— 这正是两层设计想要的：结构说哪里，哈希说有事。

- **socat 那一支同样搬了出来，顺带补上安全网上的一个洞**（重构，行为一字不改）。
  - 先说洞：命令基线原来只盯 `commands` / `preCommands` / `postCommands` / `managedConfigs` 四个数组，而 **systemd 单元的正文和服务名走的是动作上的 `unit` / `svcName` 顶层字段**，一个都没盯。也就是说「把单元文件的拼接搬个地方，顺手改错一行」这类改动可以一路绿着过去 —— 而单元正文正是 realm / socat 这两支重构时唯一动到的东西。安全网的洞刚好开在要动刀的位置上。
  - 补法：指纹加上单元正文的哈希、以及两个服务名（服务名直接记名字，换了名字等于换了一个服务，旧的会留在真机上继续跑，值得在 diff 里一眼看见）。
  - 补完之后**拿重构前那个提交的产出重新对了一遍基准**：在 `682f818^` 上跑同一套指纹，和当前树逐项比，15 组全等。realm 和 socat 的单元正文没变，这才是有据的结论 —— 而不是拿改完之后的产出录一份基线自己对自己。
  - socat 这边搬出去的是 `buildSocatServiceUnit`。`both` 协议下原来两段几乎一样的单元字面量（只差 TCP/UDP 三处）合成一次调用；两个服务名也从手写字符串换成了和单协议模式同一个 `socatServiceNameForPort`。
  - 保留的差别写在类型注释里：**Description 里的协议名和拨号用的协议名不是一回事** —— `both` 拆出的两个单元写大写 `TCP`/`UDP`，单协议模式写规则上的原始小写值。统一成一种写法会改掉已经装在真机上的单元文件内容，触发一次没必要的重下发。
  - 顺带把 `cleanEndpointHost` / `isIpv6Literal` / `endpointHostPort` / `socatDialEndpoint` 四个纯地址格式化也挪了过来，它们本来就不依赖任何请求状态。
  - 新盯住的几条性质里最要紧的是：**监听的协议族和拨号的协议族是两件事**。监听一律 `TCP6-LISTEN` 加 `ipv6only=0`（一个双栈套接字同时收 v4 和 v6），拨号则得看目标长什么样 —— IPv6 目标要 `TCP6:` 加方括号，IPv4 目标两样都不要。把两者当成一件事，无论统一成哪边都会有一半目标连不上，而且是「服务起来了但流量不通」这种面板上看不出来的坏法。还有故障转移时 Description 仍写真实落地地址（否则机器上 `systemctl status` 只看得到「转到 127.0.0.1」，完全不知道这条转发本来要去哪）。
  - 五个反向对照各红在自己那条断言上：改单元正文、换掉 `both` 的第二个服务名、把监听协议族跟着目标走、拨号漏掉 IPv6 的 `6` 后缀、Description 改用拨号目标。
  - **nginx 和 gost 这两支没有可搬的东西**，核对过了：它们的 apply 分支里一行配置文本都不生成（nginx 只有清理和 `nginx -t`，gost 全是路由解析、限速、PROXY protocol、多路径腿这些请求级编排）。真正的 gost/nginx 配置生成在另一个区域的运行时同步里，那片是跨全机规则的聚合状态，且**不在现有基线的覆盖范围内** —— 下一步是先把安全网铺到那里，再动它。没有网就不动刀。

- **realm 那一支的配置生成搬出了心跳路由**（重构，行为一字不改）。原来 TOML 内容、systemd 单元内容、四个路径/服务名函数都长在那条三百多行的 if/else 链里，跟「查库、算签名、拼动作」混在一个闭包中，没法单独调用，也就一直没有测试。现在纯生成那部分进了 `server/forwardRuntimeConfigs.ts`，编排留在路由里。
  - **命令基线一字未变** —— 搬完之后下发给 Agent 的 15 组命令逐字节相同，不是靠读代码判断的。
  - 自我修正：我最初写这条时说「基线证明了单元文件也一字未变」，**这话当时是假的**。基线那会儿只盯四个命令数组，而 systemd 单元正文走的是动作上的 `unit` 顶层字段 —— 正好是这次唯一动到的东西。下一条补上了这个洞，并且是拿重构**之前**那个提交的产出当基准重新对的。
  - 搬出来之后才第一次能直接盯住它的几条性质：`use_udp` 跟着协议走（不是常开）、`send_proxy` 和 `accept_proxy` 各管各的（PROXY protocol 一头发一头收，混成一个开关会让链式转发两头都发或都不发）、IPv6 在 listen/remote 里要带方括号、服务名按协议分开（否则 TCP 和 UDP 抢同一个 systemd 单元）、只有指定了网卡才加 `--interface`。
  - 三个反向对照各红在自己那条断言上：把 `use_udp` 写死为真、把两个 proxy 开关合成一个、把协议从服务名里去掉。

- **给转发命令生成建了表征基线**（重构安全网，不改行为）。六种转发方式 × 三种协议，一共 15 组，钉住每组下发给 Agent 的命令条数和内容哈希。
  - 为什么需要它：真正生成这些命令的是 `agentHeartbeatRoute` 里一条**三百多行的 if/else 链**，埋在一个五千多行的路由注册闭包里 —— 没法单独调用。所以这一组从**真实心跳接口**整条打进去再看它吐出什么，这也是这条最关键的路径第一次有端到端覆盖。
  - 有了它，把那条链抽成按类型分派的策略表时，才能证明「一条命令都没丢、没多、没换顺序」。没有它，任何重构都是拿在跑的系统赌运气：这些命令要在真机上写 iptables/nftables 规则和 systemd 单元。
  - 两个反向对照都验了：**砍掉一条命令** → 条数那条红；**改一条命令的内容但条数不变** → 哈希那条红。（我第一次的对照写成了 `value = String(value)`，对本来就是字符串的值是空操作，什么也没测出来 —— 换成真的改文本才算数。）
  - 顺带确认两件看着像 bug 的事都不是：**nginx 不产出动作**是因为 `DEFAULT_FORWARD_PROTOCOL_SETTINGS.nginx = false`（另加一条断言盯着这个默认值别被无声改掉）；**socat 的 `commands` 是 0** 是因为它把命令放在 `preCommands` / `postCommands` 里，是我第一版探针看错了字段。

- **首页顶上那句「系统在线」是写死的**。一个绿点加两个字，后面没有任何数据 —— 掉多少台机器它都是绿的。整个首页唯一该回答「现在系统是否正常」的地方，是个装饰。
  - 现在它有据可依：**有异常就说异常，没有才说正常**，而且说得出是哪三类 ——「1 台主机掉线 · 1 条线路异常 · 8 条转发未运行」。只给一个总数等于让人去三个页面挨个找。
  - 两类「看着像异常但不是」故意不报：**从没收过心跳的主机**是「还没装 Agent」，一步没做完不是故障，混进来会让新加的机器天天报异常；**转发组的 unknown** 是「还没测过」，不是坏了。停用的转发和隧道同理 —— 那是人主动关的。
  - 「转发未运行」这个数是上一轮修完掉线状态才第一次不说谎的：在那之前，掉线主机上的转发一直写着 `isRunning = true`。
  - 三个反向对照各红在自己那条断言上：把「从没连过」算成掉线、把 unknown 算成异常、把转发组子规则也数进转发总数。

- **同一个管理员，主机页 3 台、首页 1 台**。仪表盘一律按 `ctx.user.id` 过滤，而主机页给管理员的是全部。同一个人同一时刻，两个页面两个答案，看不出哪个是对的。
  - 现在首页按角色取范围（管理员全部、租户自己的），和主机页、转发页同一个口径。真库复现过：修前 3 vs 1，修后都是 3。

### 变更

- **首页顶部重做**。四张带渐变背景的统计卡（`bg-gradient-to-br`、`backdrop-blur`）换成一块不带装饰的状态区：一行判定 + 四项指标（主机 / 线路 / 转发 / 近 24H 流量）。
  - 按你说的「高级感不是渐变阴影动画」来的：层级靠字号和留白拉开，**状态色只用在真正表示状态的那一个图标上** —— 四张卡各配一种渐变色，会让人分不清哪个颜色是有含义的。
  - 主机和转发这两个数已经在顶上那一行里，下面不再用卡片重复一遍；只留「累计入站 / 累计出站」，它和顶上的「近 24H」是两个口径，放一起才说得清。
  - 那一格叫 **「近 24H 流量」不叫「今日流量」**：数据是最近 24 小时的滚动窗口，不是从零点算起，两者在下午三点能差出大半天的量。页面下方那张图本来就叫「近 24H」，顶上写「今日」会让同一份数据在同一屏里有两个名字。
  - 不新增请求：这个数直接汇总下面那张图本来就要取的 series。

- **把流量并发那三条性质钉死**（防回退，不是修 Bug）。真库压过了：200 次并发累加正好 200MB、同一周期边界重复触发只生效一次、重置与 100 次上报撞在一起也不串周期。
  - 之所以要钉：这三条靠的是具体写法，而那几种写法看上去**都能顺手改得更好读**。累加是一条 `trafficUsed = trafficUsed + ?` 的原子 SQL，改成「读出来、加一下、写回去」会更清楚，也会在并发下丢更新；重置那条带 `lastAutoTrafficReset < boundary` 的条件才幂等，而这个任务**每小时跑一次**，去掉之后重置日当天会清零二十几次。
  - 两个反向对照分别改成读-改-写、去掉幂等条件，各红在自己那条断言上。

- **「不同步已有订阅者」只兑现了一半**。管理员改套餐时可以勾掉那个开关，面板会把套餐当时的内容冻进订阅行的快照。主机、隧道、转发组一直认这份快照 —— **只有落地节点不在里面**，它跟着套餐当前内容走。
  - 后果最难发现：老客户身上随便发生一件小事（充值、流量重置、权益重算）就会触发一次同步，把他买的时候送的那个节点悄悄收走，落地机上那份凭据一并删掉。管理员明明说了「不要动老客户」，而面板上没有任何一处提到这件事。
  - 现在快照里也记节点，授权计算优先认它。**老快照里没有这一列时退回按套餐当前内容算** —— 默认成空数组会把所有老订阅者的节点一次性收光，这一点和 `maxProxyInbounds` 当初的处理是同一个道理。
  - 顺带把这条查询改成「先取还生效的订阅，只给没冻过快照的那些去查套餐绑了什么」：冻结是管理员的常规操作，不该每次都白跑一条 join。
  - 三个反向对照各红在自己那条断言上：快照不记节点 / 无视快照 / 老快照缺列时当成空数组。

- **删掉一台机器，它跑过的流量永远算在租户头上**。`deleteHost` 清了整机计数、流量明细、分桶统计，唯独漏了**按规则记的那份计数**（`forward_rule_traffic_counters`）。
  - 这张是四张里唯一**没有按时间清理的**：明细和分桶有 72 小时保留期兜底，它是累计计数，没有时间戳可扫，只能靠删。
  - 后果不是多几行垃圾：重算用户总流量走的是**直接 SUM 这张表、不 join `forward_rules`**，于是一台已经删掉的机器跑过的量会一直算在这个租户头上，而管理员再也清不掉 —— 「重置这条转发的流量」要按规则 id 来，规则已经跟着主机一起没了。
  - 真库复现：租户名下 110G（其中 100G 在要删的那台上），删掉机器后重算仍是 110G；修完是 10G，另一台机器的计数没被带走。

- **删掉一个落地端口，它发出去的凭据永远留在库里**。`proxy_inbound_users` 存的是 uuid / password 本身，而删入站时只删了派生节点和入站行 —— 这一层谁都不管。
  - 派生节点跟着入站走，分享记录跟着节点走，唯独凭据这一层断在这里。管理员在弹窗里加的那些（`sharedUserId = 0`）尤其彻底：删完端口就成了永远不会被回收的凭据，全项目没有任何一条路会再碰它们。
  - 说清楚不是什么：**不存在「新端口继承老凭据」的风险**，主键是自增且不回收的，实测删掉 1 号端口后新建的是 2 号。问题是「一个以发凭据为业的面板，删掉发凭据的那个东西之后，凭据还在」。
  - 为分享单独发的那些（`sharedUserId > 0`）本来会在下一次分享对账时被顺手清掉，但那要等那个人身上恰好发生一次对账，而且对账要先对着一个已经不存在的入站白跑一趟。
  - 顺带给 `cleanOldTcpingStats` 补了一句注释：它名字里只提了 tcping，实际上连转发组的延迟历史一起清。按名字搜那张表会得出「没人清」的结论，然后加一个重复的清理 —— 我自己就先这么误判了一次。

- **删掉资源，套餐里还绑着它 —— 四种资源只有主机做对了**。套餐能绑主机、隧道、转发组、落地节点，删除时**只有主机**会去清套餐里的绑定，另外三种把行留在原地。
  - 留下来的是一条指向不存在资源的授权，而三处都会照着它说话：商店页上的「本套餐含 3 个落地节点」多算一个，客户买完只拿到 2 个；管理端的套餐编辑里显示成「节点 #7」这样一个只有编号的空壳；授权计算照样把这个 id 发出去。
  - 三处补齐，写法照抄主机那一路。测试四种一起测，就是为了盯住「以后新增第五种资源时别再漏」，另外验了「只删被删那个的绑定，同套餐里别的不能被带走」。
  - 但清干净之后还有一件事：这一下是**静悄悄**的。管理员删的是一个节点，被改掉的是几个正在卖的套餐，商店页上的数量当场就变。所以删除接口现在把「几个套餐移除了该节点」一起交出来，和原有的「几条转发已解绑」同一个写法，在同一句提示里说清楚。

- **「这台机器算不算在线」有两套判定**。三个条件缺一不可：库里那个 `isOnline`（上一次写入的结论）、心跳还新鲜（那个结论会过期，而它不会自己变）、快速失联没有确认掉线（Agent 那条实时通道最新）。仓储读出来时折算一次，Telegram 状态通知那边又各写一遍。
  - 判的是同一件事，漂了就会出现「列表上是绿的、机器人刚发过掉线通知」，而这种不一致没人查得出来是哪一边错。`isFreshHostHeartbeat` 当初被抽出来就是为了防这个，抽完却只统一了其中一半。
  - 现在两处都调同一个 `isHostConsideredOnline`。反向对照：拿掉心跳新鲜那一条，「心跳过期那台被判错」红。

- **派生节点按 `inboundId` 查是整表扫描**。保存入站、删除入站、主机换地址后重算、界面上列出某个端口派生了哪几条 —— 全都按这一列过滤，而 `proxy_nodes` 上偏偏没有这个索引（同表的 `userId` 有，隔壁 `proxy_inbound_users.inboundId` 也有）。
  - 一个商家几百上千行节点时，删一个入站要扫一遍全表，而这条路上还挂着「删节点顺带解绑转发规则」，一次操作要扫好几遍。
  - 执行计划实测：加索引前 `SCAN proxy_nodes`，加之后 `SEARCH proxy_nodes USING INDEX idx_proxy_nodes_inboundId`。老库也会补上（建表流程里删掉索引再跑一遍，它会重新出现）。

- **面板和 Telegram 机器人，对同一条规则给两个答案**。面板一直有探测新鲜期（6 分钟），机器人没有 —— 它只看「上一次探测超没超时」，不问那是三分钟前还是三天前的事。
  - 于是一条正常跑着的规则：面板是绿的、写着「Agent 已确认规则运行」，机器人说「目标探测超时」。两个地方对同一件事给两个答案，比给错答案更让人不敢信 —— 人会两个都不敢信。
  - 根因是中间那一步**把探测时间戳丢掉了**：仓储明明查出了 `latestLatencyAt`，机器人这边组装汇总时只抄了「超没超时」和延迟数字。陈年的那个延迟数字同样有问题，它会跟在「运行中」后面被当成现况写出去。
  - 顺带把这条判定的五份拷贝收成一个 `isLinkProbeFresh`：链路可用性、节点健康、转发规则状态、规则探测缓存原来各写一遍一模一样的三个条件（隧道那条 SQL 仍留在 SQL 里）。
  - 两个反向对照都红了，而且红在不同的断言上：把时间戳重新丢掉 → 「新鲜期判定会永远为假，机器人从此再也报不出超时」；让机器人重新无视新鲜期 → 「同一条规则两个答案」。

- **机器掉线几个小时了，它的转发还写着「Agent 已确认规则运行」**。心跳超时只改了 `hosts.isOnline`，而 `forward_rules.isRunning` / `tunnels.isRunning` 是 Agent 上一次上报时写下的结论 —— 机器没了，没人再来改它。
  - 不是含糊的「状态未知」，是一句言之凿凿的假话，而转发这一页正是人出事时第一个打开的地方。
  - 清理函数本来就在（主机换地址那条路一直在用），这一处的问题从来不是「没有这个能力」，而是**掉线时没人调它**。现在三条掉线路径都调：定时扫描、启动时的补扫、Agent 快速失联判定。
  - 连带把这台机器参与的隧道（入口、出口、中间跳、入口组成员）一起清掉 —— 机器没了，经过它的那条链路当然也没在跑。
  - 清成「等待 Agent 上报」而不是「错误」：机器回来时下一次心跳就会重新写上，中间这段确实只是不知道。掉线本身另有状态点和 Telegram 通知在说。
  - 测的是**真正的掉线入口**（`sweepOfflineHostsAndNotify`），不是绕过它直接调清理函数 —— 绕过去等于把唯一出问题的那一环测掉了。反向对照：让掉线不再清理，「转发还在库里写着运行中」那条红。

- **能设总流量的一共三层，只有两层会告诉你到量了**。主机有邮件和 Telegram 提醒，粘进来的落地节点也有 —— **面板自己在主机上开的那个落地端口没有**。它有额度、有累加、行上到量了会变红，唯独没有任何人被告知。
  - 填了 500G 的人以为面板在替他看着，其实只有他自己盯着订阅管理那一页时才看得见。这一层恰恰是**按租户计量**用的那一层（独享端口），跑超了是要收钱或者停服的。
  - 顺手把重复的那一半也合了：邮件和 Telegram 原来各写一遍「查哪些节点 → 判到没到量 → 拼文案」，现在两路遍历同一个清单（`server/proxyTrafficReminders`），判定仍然共用 `shared/proxyNodeReminder`，也就是界面上那个仪表盘的同一个阈值 —— 三处各算一套，迟早出现「图标还是绿的、邮件说快满了」。
  - **去重键分了命名空间**。两张表各有各的自增 id，5 号端口和 5 号节点同一天都到量时，共用一个键会让后到的那条被当成「今天发过了」而永远发不出去。node 那一路沿用原来的键名 —— 换掉等于所有人当天再收一遍已经发过的提醒。
  - 到量**不停服**，只告诉主人：和节点那一层同一个决定，这个数多半是机房规格，真正会断的是机房那一刀。
  - 两个反向对照都红了：去掉端口那一层，「端口到量了却没进提醒清单」红；把两个键名合成一个，「后到的那条永远发不出去」红。

- **主机换了地址，订阅里那条节点还写着旧的**。主机改自定义入口、开 DDNS 域名、或者 Agent 报上来一个新 IP 之后，转发链、隧道、Agent 配置全都会被刷到新地址 —— **只有订阅漏了**。
  - 派生节点的地址是**保存入站那一刻**抄下来的，此后全项目只有一个地方会重写它：入站保存。主机层的地址变更压根不经过那里。
  - 后果是最难查的那一类：客户端拉到订阅连不上，而面板上转发绿的、隧道绿的、Agent 在线，没有一处指向真正的原因 —— 自建节点用的是**同一个入口地址**，却是唯一一处没人更新的。
  - 真库复现：建一个入站派生出节点（地址 `203.0.113.10`），把主机入口改成 `relay.example.com`，节点地址原地不动。
  - 修法是把这一层挂进 `refreshHostAddressRuntime`（自定义入口 / Agent 报新 IP 两条路都走它），DDNS 开关和换域名那条另外补一次 —— 那条不走整套刷新，因为转发链和隧道认的是域名本身，不该被一次配置变更重置。
  - **只重算地址真的变了的那些入站**：这个函数挂在每一条地址变更路径上，绝大多数调用应当什么都不做。两个反向对照都验过 —— 去掉「变了才重派生」的判定，「地址应当跟着改」那条红；去掉「没变就不动」的判定，「不该重写」那条红。
  - 主机地址算空时保持原样，不把订阅里的节点停掉：「主机暂时没地址」和「这个入站不该出现在订阅里」是两回事，而地址回来时没有人会再保存一次入站。

- **折扣码可以被任意登录用户全速试出来**。兑换码那条路早就有试错限流（按用户 8 次、按来源 20 次，超了封 15 分钟），而**折扣码预览一点限制都没有** —— 两条都是「拿一个码来问对不对」，门上了锁，旁边的窗户开着。
  - 为什么要紧：折扣码是**管理员手打的**（`SALE50` 这类），好猜；而这个接口对不存在的码直接回「折扣码不存在」，是个干净的探测口。任何登录用户都能全速遍历，把商家还没公布的、或者只发给某个客户的码翻出来，然后自己在结账时用掉。
  - 修法是**复用兑换码那一套计数器**，不是再抄一份 —— 但**各记各的账**：一个人在结账页试几次折扣码，不该把他兑换礼品码的额度一起吃掉，反过来也是。
  - **只有「折扣码不存在」计入失败**。我第一版把「码是对的但不适用于这个套餐」也算了进去，真面板一跑就看出理由站不住：要遍历套餐，前提是已经拿到一个有效码，而拿到有效码走的是「不存在」那条路、那条是计数的；挡住了前面，后面这点信息不值得。代价却是实打实的 —— 结账时手里有几张码、试到一张是别的套餐的就白扣一次额度，扣满八次人就被挡在付款外面十五分钟。**拦试码的人，不该拦正在掏钱的人。**（另外套餐适用性在结账时还有一道真校验，预览只是提示。）
  - 真面板两头都验了：连试七个不存在的码，第八次回「折扣码试得太频繁，请 900 秒后再试」；重启清掉计数后连问十二次「不适用于该套餐」，一次都不被封。

- **同一条订阅，租户页和管理员页又是两个名字**。上一轮统一 `subscriptionSourceLabel` 时漏了第三份：「我的套餐」页自己还有一个 `sourceLabel`，`source='admin'` 在那儿叫**「后台分配」**，而服务端通知和用户管理页叫「管理员分配」。
  - 说清楚证据边界：我看到的是**代码里两份文案不一致**；开发种子里造不出租户那一屏的订阅行（`plans.mySubscriptions` 对管理员返回空），所以没能在真面板上亲眼看到「后台分配」。合并后两页调的是同一个函数，管理员页上验到的是「管理员分配」。
  - 顺带把 `statusLabel`（生效中/已过期/已取消）和 `quotaSourceLabel`（手工额度/已购附加流量/管理员加赠）也收进同一处 —— 这两组同样是各存两份。
  - 新增 `shared/ledgerLabels.test.ts` 把口径钉死，并分开测两种兜底：**流水类型和订阅来源原样显示**（将来加了新类型，吞成一句「余额变动」等于把唯一线索抹掉，账务有疑问就查不下去），**额度来源回「套餐额度」**（这一列是面板自己定的枚举，出现新值只可能是版本没对齐）。反向对照两个都红了。

- **系统设置的 `?tab=` 深链接一直是坏的**。上一版修过七个带 tab 的页（从 wouter 的 `useLocation()` 里 `split("?")` 取查询串，而那个值只有路径），**系统设置这一页漏了** —— 它自己手写了一份一模一样的错法。
  - 真面板对照：`/settings?tab=email` 地址栏写着 email，选中的却是「系统配置」；`/settings?tab=telegram` 同样落回系统配置。同一时刻 `/tunnels?tab=groups` 正确选中「转发组」。
  - 系统设置有七个 tab，是最常被人发链接说「去 Telegram 那个 tab 设一下」的一页 —— 对方点开看到的却是另一屏。
  - 现在统一到 `useUrlTab`，全站最后一份手写 tab 判定也没了。顺带这一页也和其它页一样会记住上次停在哪个 tab（优先级仍是 **地址栏 > 记住的 > 默认**，所以发出去的链接永远赢）。

- **两个功能有页面、但全站没有任何入口**。`/forward-groups`（转发组）和 `/email-settings`（邮箱设置）在路由表里注册着、有权限门禁、独立打包，**侧边栏没有入口，全站也没有一处链接过去**。
  - 两边都不是重复实现（界面本体是共享组件，分别被链路管理的「转发组」tab 和系统设置的「邮箱设置」tab 渲染），问题是这两个地址是同一个功能的第二个入口，而且落在那儿是**光秃秃一块、切不到兄弟 tab** 的死胡同。
  - 另外两个并过去的页（`/proxy-inbounds`、`/traffic-billing`）早就改成跳转了，这两个漏了。现在补齐：`/forward-groups` → `/tunnels?tab=groups`，`/email-settings` → `/settings?tab=email` —— 老书签不吃 404，而且带着 tab 上下文落地。
  - 真面板逐条验过八种地址，深链接和四个跳转全部落在正确的 tab 上。

- **服务端顺手把转发救回来了，六条路一个字都没说**。余额充值、修改余额、重置流量、启用账户、给流量计费授权、兑换码 —— 这六条路服务端都会检查一遍「这个人的转发是不是因为超额/欠费被停了，现在够条件恢复吗」，恢复了就在返回值里带 `forwardAccessRestored: true`。**六个客户端一个都不读**。
  - 最扎眼的是兑换码：租户余额耗尽被停了转发，兑换一张码往往就是为了把转发救回来 —— 面板只说「兑换成功」，他不知道转发回来没有，只能自己去试，或者来问你。做了事不说，比没做更让人没底。
  - 现在接在原句后面说出来，并且**分得清对谁说**：管理员那边是「该用户被暂停的转发已恢复」，租户自己那边是「你被暂停的转发已恢复」。人称用错在余额这种事上很刺眼 —— 租户充完值看到「该用户」，会以为扣错了人的钱。
  - 真面板跑通了全链路：把一个租户设成余额不足被停，管理员点「余额充值」，toast 是「余额已充值 · 该用户被暂停的转发已恢复」；同一个人恢复之后再充一次，toast 就只有「余额已充值」——**没恢复就不加废话**。

- **弹窗内容超过屏幕高度就被裁掉，而且滑不动**。`DialogContent` 的默认样式是 `max-h-[92svh]` 配 `overflow-hidden` —— 内容一超过屏高直接切掉，没有任何办法滑到下面。真机上「添加用户」最后那个「取消」按钮就卡在面板边缘外，手指怎么划都不动。
  - 全站 **94 个弹窗里只有 1 个**自己加了滚动容器，剩下的在矮屏幕上都是这个下场。
  - 默认改成 `overflow-y-auto`。自己已经管好滚动的那 25 个弹窗不受影响：它们在 className 里显式传了 `overflow-hidden`，`tailwind-merge` 会把默认那一组整个丢掉（当场验过 twMerge 的仲裁结果）。
  - 反向对照用**真实滚轮**而不是脚本改 `scrollTop`：`overflow:hidden` 下脚本照样设得动，只有真滚才分得出来。改前滚轮滚不动、「取消」露不出来；改后滚到底「取消」完整露出。
  - 试过把「×」用一个 0 高的 sticky 壳子钉在滚动区顶上，量下来代价是**所有弹窗标题上方多出 17px 的缝**，而且「×」会浮到标题上面去 —— 那一行的 gap 吃不掉（负 margin 只能压扁行高，压不掉行间 gap）。为钉一个按钮把 94 个弹窗的间距改一遍不划算，所以维持原样：滚上去之后还有 ESC、点遮罩、底部「取消」三条路可以关。

- **手指按在主机卡上想滑动，浏览器判成长按，把「计费： 走套餐流量」整行连标签带徽章选中并弹出系统的「拷贝 / 搜索 / 翻译」菜单**，挡住半张卡。那一行根本没人想复制 ——「计费：」是标签，「走套餐流量」是状态徽章。
  - 卡片默认关掉文字选择，再把**真正值得复制的**单独放出来：机器名和 IP 地址。一刀切全关会把地址也锁死，而排障第一件事就是复制地址。
  - `-webkit-touch-callout: none` 是 iOS 专门那一道：光有 `user-select: none`，Safari 在图片和链接上仍会弹菜单。
  - 只改主机卡：逐页量过，其它列表页没有这种「标签：值」密集排布（转发规则和链路各只有一个「筛选：」在工具栏上），不做没有依据的横扫。

- **列表里的开关只有 18px 高，手机上点不准**。转发规则、链路、转发组这些密集列表把开关用 `scale-75` 压小以免撑高行高，但 `transform` 连**命中区**一起缩了：44×24 变成实测 33×18。而这是全站最常点的那个开关 —— 启用/停用一条转发，点偏了要么没反应，要么点到旁边那行去。
  - 现在把缩放和命中区拆开：视觉仍是 0.75，另外用一个透明的 `::after` 把可点范围撑回来。实测**命中区 34×19 → 50×41**，视觉一像素没动。
  - 撑多少是量出来的，不是拍的：先量了开关到最近的其它可点元素有 28~53px，据此定四周各撑 13~17px；撑完再逐页验一遍「有没有哪个控件被开关的热区盖住」—— 转发规则 12 个、链路 4 个、转发组 3 个开关，被盖住的控件 0 个。
  - 反向对照：只去掉 `::after`、保留缩放，命中区立刻掉回 34×19。
  - 这一轮验证踩了两个坑，都记下来：一是**开屏公告弹窗盖在页面上**，前几次量到的「开关」其实是弹窗里的那个，数字全是错的；二是做对照时用 `git checkout` 还原 CSS，把还没提交的新样式一起冲掉了，于是「改前改后一模一样」—— 那不是修复没用，是对照本身失效了。

### 变化

- **近似重复再降到 27 组**，这一轮四处，都是「两页说同一件事」：
  - **资源状态点的颜色判定两份**（流量计费配置页、套餐管理页）→ `client/src/lib/statusDot.tsx`，跟画点的那个组件放一起。两份的判断完全一样、只是分支顺序不同。这两页摆的是**同一批资源**，同一台机器显示两种颜色，人会以为是两个东西。配了单测钉死三档的语义，其中一条是「**资源不存在时是灰的、不是离线**」——「被删掉」和「掉线」是两回事，说成离线会让人去排查网络，而实际该做的是把那条配置删掉。反向对照红了。
  - **时间刻度「MM/DD HH:mm」的第三份**（链路延迟详情）→ 上一轮建的 `latencyTimeLabel`。
  - **两个仓库的拖拽重排各一份 371 token 的实现**（探测服务、主机分组），一字不差，只差表名和错误里的名词 → `reorderRowsBySortOrder`。
    - 这个改的是**批量写接口**，所以单独配了真库用例：拖拽排序看着无害，其实是「给一串 id、逐个改 sortOrder」，少了整体校验那一步，一个夹带了别人 id 的请求会把**别人的行也改掉**，而且悄无声息。用例守三条：正常排序生效、夹带别人的 id 整个拒绝且**一行都不许改**、重复 id 拒绝（有重复说明前端状态乱了，照着写会把两行挤到同一个位置）。反向对照两个都红了。

- **两张地图的悬停卡合成一份**（889 token、相似度 0.95，平面地图和 3D 地球各抄了一份）。差别只有两处：坐标点颜色一边是 deck.gl 的 `[r,g,b,a]` 数组、一边是 CSS 串；再就是宽度 330/320、底色透明度 .94/.92 —— 后两处不是设计意图，是两份在不同时间各自被改过的痕迹。
  - 合并后统一按平面地图那份（330px / .94），**地球上的卡片因此宽 10px、底色深 2%**。颜色交给调用方先转成 CSS 串。
  - 这段是**拼字符串塞 innerHTML** 的，而机器名是用户填的、系统信息和 Agent 版本是机器报的 —— 所以配的测试重点不是排版，是转义有没有漏、缺字段时说不说人话。7 条用例，反向对照两个都红了（机器名不转义、旗帜兜底丢失）。
  - **两张图都在真面板上验到了内容**。平面地图靠悬停：弹出「SG relay 03 · 离线 · IPv4 198.51.100.16 · Singapore / Singapore · AlmaLinux 9 · v2.2.195」。3D 地球悬停不上 —— 这个环境是 SwiftShader 软件渲染，扫了 900 个点都没落在那几个很小的点位上；改成从 React fiber 树里取出地球拿到的 `pointLabel` 直接调，输出是「US backup 04 · 离线 · IPv4 203.0.113.18 · US United States / Los Angeles · Rocky Linux 9 · v2.2.124」，且带着 `max-width:330px`（平面地图那份的值），确认走的就是合并后这一份。

- **近似重复从 47 组降到 33 组**，这一轮挑的是「漂了会出事」的那些。字节级完全相同的上一版已经清零，这次换了把更细的尺子：按归一后的 token 做相似度比对，并且**不归一字符串字面量** —— 归一了的话，两个内容完全不同的文案表会被误判成重复（`paymentTypeLabel` 和 `providerLabel` 就是这样被第一版误报的）。
  - **`parseFailoverTargets` 两份**（规则 CRUD 存的时候一份、Agent 心跳下发的时候一份）→ `shared/failoverTargets.ts`。两份的上限写法还不一样：CRUD 用 `MAX_FAILOVER_TARGETS`，心跳**写死 10**。今天两个数碰巧相等所以没出事，但这是一次改动就会静默分家的写法 —— 把上限调到 15，面板收下 15 个目标、Agent 只拿到前 10 个，剩下 5 个不报错、不提示、只是永远不生效。主备本来就是拿来兜底的，兜底自己悄悄少一半是最不该发生的坏法。
  - **`quote` / `quoteIdentifier` 三份**（建表 DDL、库切换迁移、运行时裸 SQL）→ 一份，放在 `dbRuntime`（`dbCompat` 本来就依赖它，反过来引会成环）。引号规则错了不会报「语法错误」那么清楚，更常见的是一个带保留字或大写的表名在 MySQL 上能跑、在 PostgreSQL 上找不到表。
  - **`normalizeTunnelLatencySeriesKey` 两份**（服务端写点、客户端取点）→ `shared/latencyProbe.ts`。这两边必须对同一个字符串达成一致，任何一边的规则变了，图上就凭空多一条空曲线、原来那条同时不见，而且不报错。
  - **`maskToken` 两份**、**转发组模式判定第三第四份**、**`sameNullableStringArray` 两份**、**Rules 自己那份切换动画**（并进上一轮建的 `SectionTransition`，给它加了可选 `className`）也一并收了。
  - 合并仍然一律照搬原实现。`sameNullableStringArray` 上差点栽一次：我按记忆写成 `?? null`，而原文是 `|| null` —— **原实现把空串也当成 null**，改了会让一批本来相等的表单突然被判成「改过了」。发现后换回原文，并把这个取舍写进注释。

- **给全站最热的那条路钉了一道打库次数的红线**。Agent 心跳每台机器每几秒打一次，一个商家几百台就是每秒几十上百次 —— 别处多一条查询只是慢一点，这里要乘以机器数再乘以频率。
  - 量下来现状很干净：**稳态两条**（按 token 取这台机器 + 写回在线状态和心跳时间），**冷启动 31 条**（算一遍下发给 Agent 的期望状态，之后靠签名缓存住），而且**机器数从 10 放大到 50、规则数跟着放大，条数纹丝不动**。
  - 干净归干净，没有东西拦着它变脏。新增 `server/agentHeartbeatQueryCost.test.ts`：稳态订到 6 条、冷启动订到 50 条，并且**规模放大四倍时条数必须原地不动** —— 后面这条才是真正拦 N+1 的，只订绝对值的话，一个「每台机器多查一次」的写法在小数据集上照样能过。
  - 用绝对值而不是「跟上一次比」：相对基准会跟着劣化一起漂，等于没订。
  - 反向对照：往心跳里塞一段按规则逐条查主机的典型 N+1，稳态从 2 条涨到 33 条，测试红了；撤掉恢复绿。（第一次插桩锚点没匹配上，那次「通过」不作数，重新插过才算。）
  - 另外两处扫描是干净的，一并记下免得重复劳动：40 个写入的设置 key 里没有真正的死开关（几个 `...BackfilledAt` 只是给人看的时间戳，真正的守卫是另一个 marker）；所有 `useMutation` 的成功回调都会刷新对应查询、或走 `mutateAsync` 由调用方处理，没有「改完不刷新」的，三处拖拽排序也都有乐观回滚。

- **首屏再少下 461 KB（gzip 少 116 KB）**：主包 **1877 KB → 1416 KB**，gzip **554 → 438 KB**。
  - 原因是 `recharts` 整个躺在主包里。仪表盘是所有人的落地页，上一轮路由分包刻意把它留成同步的，于是它 import 的图表库也跟着进了主包 —— 一个只想看一眼「几台在线」的人，得先把整个图表库下完。
  - 现在把仪表盘那两张图（流量饼图、24H 面积图）抽成独立模块按需加载。这两张图**本来就有不依赖 recharts 的加载态和空态**：数据没回来时显示骨架或转圈，那段时间正好够把图表库取回来，所以改完在观感上是免费的，占位沿用原来那个转圈，换过来看不出接缝。
  - 打包结果：recharts 落到 `generateCategoricalChart`（376 KB）等独立 chunk，点到用图表的页面才下。
  - 真面板复核：仪表盘 4 个图表容器、16 个饼图扇区、2 条面积曲线、2 条坐标轴全部正常渲染，无页面异常。
  - 顺带查了主包里还剩什么大件：只有 lucide 图标（按需摇树）和 motion，没有可以整块摘出去的了。加上上一轮，主包从最初的 3477 KB（gzip 960）降到 1416 KB（gzip 438），**少 59%**。



### 变更

- **把两个纯逻辑模块从 `client/src/lib/` 挪进 `shared/`**：`linkAvailability`（链路可用性判定，500 行）和 `timestamp`。服务端原来是**跨层反向引用** `../client/src/lib/linkAvailability` —— 两边都需要的判定却住在客户端里。两个文件都不碰 React 和 DOM，挪过去只是把它放回它本来该在的那一层。

## [2.3.362] - 2026-09-14

### 修复

- **同一条订阅记录，两个地方叫两个名字**。`subscriptionSourceLabel` 有两份：`source='payment'` 在服务端通知里叫「在线购买」，在用户管理页叫「在线支付」；认不出来源时的兜底文案一个是「套餐变更」、一个是「套餐记录」。`balanceTypeLabel` 更是三份（服务端一份、账单页一份、钱包页一份）。
  - 一笔钱在两个地方叫两个名字，比叫错更让人不敢信 —— 租户会开始怀疑是不是两笔。
  - 现在收敛成 `shared/ledgerLabels.ts` 一份，**以服务端那份的口径为准**（通知是推到人手机上的，改它代价最大），所以用户管理页的「在线支付」统一成「在线购买」。顺带把「认不出的类型」从吞成一句「余额变动」改成原样显示 —— 出现一个没见过的流水类型时，把它的原名摆出来才查得下去。

- **套餐管理换个 tab，页头就说「套餐数量 0」**。页头那三张统计卡（套餐数量、套餐资源，以及商店状态里「N 个套餐的购买入口不生效」那行）两个 tab 都看得见，可它们的 `plans.summary` 查询原来跟着 `activeTab === "plans"` 走 —— 点到「流量计费」那一侧查询直接不发，`planSummary` 变 `undefined`，卡片一路回落到 0。
  - 真面板对照：`/plans` 显示「套餐数量 2 / 套餐资源 12」，`/plans?tab=billing` 同一时刻显示「0 / 0」。库里没变，就是同一个页面自己跟自己对不上。
  - 跟上一版按量计费那张状态卡是同一个毛病：**页头的数字不归 tab 管**。现在这个查询无条件发，卡片的 loading 也改看它自己那条查询（原先看的是被 tab 门禁挡住的列表查询，所以连「加载中」都不显示，直接摆一个 0 出来）。

- **同一个数在不同页面显示得不一样**。`formatBytes` 全站有 **8 份各自定义、8 种实现**，其中三份是错的：
  - **系统设置和插件那两份封顶到 MB** —— 一个 5 GB 的备份写成「5120.0 MB」，单位爬不上去，大小就没法一眼判断。
  - **用户管理那份单位阶梯只到 TB**，超过 1 PB 会取到数组外，界面上直接显示「1.78 undefined」。
  - 转发规则那份保留尾零（「1.50 GB」），别处是「1.5 GB」。
  - 金额那边同样：`money` 7 份 + 用户管理的 `formatCurrencyCny` 1 份，共 **8 份**，有的写 `Number(cents) || 0`、有的直接 `cents || 0`、有的把币种写死。
  - 现在各收敛成唯一一份（`shared/formatBytes.ts`、`shared/formatMoney.ts`）并单独测：单位阶梯、夹住下标不吐 undefined、尾零、脏值、负数、字符串入参、不足一分的单价要给三位小数（固定两位会把 0.003 元/GB 显示成「¥0.00」，一个正在收钱的价钱看起来像免费）。
  - 一个数在两个页面长得不一样，比显示得不好看严重得多 —— 人会开始不确定该信哪个。

### 变化

- **一字不差的重复实现从 33 组清到 0 组**，共 36 个文件、净减 474 行。第一轮合并了后果最重的九处：
  - **`compareVersions` 三份、`normalizeVersion` 四份** → `shared/version.ts`。这把尺子同时管三件事：面板要不要显示「发现新版本」、后端敢不敢给某台 Agent 下发新指令、主机卡上的升级角标亮不亮。四份各自演化的话，界面说该升、后端说不用升，谁也说不清该信哪个。单独测了八条，含两条有方向的：**版本号缺失时一律判「不够」**（宁可少下发一个新能力，也不能对着一台不认识这条指令的 Agent 发过去），以及「够门槛」和「落后」必须互补。反向对照两个都红了（把缺失判成「够」、改成字符串比较）。
  - **`maskIdentifier` 两份** → `server/routers/helpers.ts`。打码规则是隐私口径，漂了意味着有一条路上比另一条露得多，而多露的那条不会报错，只会安安静静写进日志。
  - **`dbBool` / `databaseBool` 三份** → `repositoryUtils`。SQLite 存 0/1、PostgreSQL 存 true/false、裸 SQL 回来还可能是字符串。
  - **`normalizeProbeCounts` 两份** → `shared/latencyProbe.ts`。
  - **`hostGeoCoordinate` 四份、`escapeTooltipHtml` 四份、经纬度聚类距离两份** → `client/src/lib/hostGeo.ts`。同一台机器在四张图上该落在同一个点。
  - **`prefetchReactGlobe` 两份** → `client/src/lib/reactGlobeLoader.ts`。这一份不只是重复：两页各有一个模块级的「预取过了」标志，**互不知道对方**，先后打开链路管理和转发规则就会把地球组件预取两次 —— 那个包 gzip 后 500 KB 出头，手机上白下一遍不是小事。
  - 合并一律照搬原实现，**不顺手改行为**（比如经度环距那两份都没取模，就保持没取模，并在注释里写明为什么可以）。

  第二轮把剩下的 24 组清完，挑几处说：
  - **`bytes` 三份（套餐商店、我的套餐、套餐管理）** —— 这是上一版 formatBytes 收敛时**漏掉的第三种写法**：单位阶梯只到 TB，而且保留尾零（「1.50 GB」），跟全站其它地方的「1.5 GB」对不上。现在数字交给 `formatBytes`，`formatQuotaBytes` 只负责「0 当成不限」这一条额度语义。所以这三页的「1.50 GB」现在显示成「1.5 GB」。
  - **`dbBool` 其实有六份、两种写法**：三份带 `typeof` 闸门，另三份直接 `String(value)` 比对。对库里真实出现的值（0/1、true/false、"0"/"1"/"true"/"false"、null、空串）结果完全一致，只在传进一个自定义 `toString` 的对象时才分岔 —— 那种值不会从数据库列里来，所以按同一份合了，并在注释里写明这个判断依据。
  - **`isFreshHeartbeat` / `isFreshHostHeartbeat` 两份** → `hostHeartbeatPolicy`。库里那个 `isOnline` 是上次写入的结论，心跳停了它不会自己变，所以读出来还要拿时间戳再验一次。两份漂了就会出现「主机页说在线、Token 页说离线」，而这两页说的是同一台机器。
  - **`linkHostSearchParts` / `forwardGroupHostSearchParts` 两份** → `hostSearchParts`。这份清单是「能不能搜到」的定义：两页各存一份，就会出现在链路管理里用 DDNS 域名搜得到、到转发组里同一个词搜不到。
  - **`pricePerGbMilliCents` 两份** → `shared/trafficBillingPrice.ts`。老数据价钱存在 `pricePerGbCents`（分）、新字段是毫分，两份各自判「用哪个」，漂了就是同一条配置在两页显示成两个价钱 —— 这是正在收钱的数字。
  - **`normalizeTargetValue` / `normalizeRawValue` 两份**（库切换 / 运行时）→ 一份。迁移时写进新库的值必须和面板平时写进去的走同一套转换，不然同一条数据迁完之后跟原来长得不一样。
  - **账单页和流量计费页的统计卡**改用全站那一份 `StatCard`。原来那两份是同一段复制的样式、且**没有窄屏适配**（固定 `p-4` + `text-2xl`）；换成共用的之后手机上数字会自适应、图标收起。真面板在 390px 和 1280px 两个宽度各看了一遍，横向不溢出。
  - `hostGeo` 那四合一另配了测试和反向对照（越界坐标改成夹边界、气泡不转义单引号、经度不按环形算 —— 三个都红）。
  - **一处差点改坏的地方，被 tsc 拦下了**：整体改名 `pricePerGbMilliCents` → `pricePerGbMilliCentsOf` 时，连保存单价那段里的局部变量和**提交给接口的字段名**一起改了。字段名一改，保存时就会把单价当成未知属性丢掉 —— 一次静默的「改了价钱没生效」。已改回，保存路径按原样。

- **三处「为了一个数字，把整张表读回来」改成窄查询**：
  - 自助加机器的配额检查（校验时一次、界面显示「2/10」时又一次）原来是 `(await getHosts(userId)).length` —— 为了得到一个台数，把这个人名下每台机器的每一列（agentToken、DDNS 配置、端口区间那一堆）全查回来再扔掉。换成 `countHostsByUserId`，一条 `COUNT(*)`。
  - 主机分组保存时校验「这些主机在不在」，原来整表读 `hosts` 再在内存里建 Set。换成 `findExistingHostIds`，只问那几个 id。
  - 按量计费的授权对账扫描原来走 `getAllUsers()` —— 那条查询为了列表展示会 leftJoin 一份按用户聚合的计费用量，再把每个用户的每一列（密码哈希、Telegram 绑定、整套配额）读回来，而调用方只是拿 id 去逐个对账。换成 `getNonAdminUserIds()`，管理员直接在 SQL 里滤掉。
  - 三处都拿**老写法当场算一遍再对**，不是看着像就算数（`server/narrowedTableReads.test.ts`）。反向对照四个都红了：去掉 `COUNT(*)` 的 WHERE、让 id 校验原样回传不查库、去掉 role 过滤、以及把 `role <> 'admin'` 写成 `role = 'user'`（后者会漏掉 role 是空串的老数据 —— 对账漏一个人，等于这个人的转发权限对不上账还没人发现）。
  - 真面板复核（把管理员降成租户、上限设成 4）：`selfServiceQuota` 回 `used 4 / limit 4 / canAdd false`，再加一台被拦下并给出「已达上限（4/4）」。

- **删掉 22 个无效的缓存失效调用**。`utils.users.list.invalidate()` 散在四个文件里，而 `users.list` 这个查询**全站没有任何地方去取** —— 分页化之后列表早就走 `users.listPage` 了。所幸每一处后面都跟着一句 `listPage.invalidate()`，所以没造成「改完不刷新」，纯属留着占地方、让人以为还有个全量列表在用。

- **删掉 64 个没人调用的导出**，37 个文件、486 行。全部是一路堆上来的残留，不改任何行为：
  - **改名后留下的兼容别名**：`getRuleStatusSnapshot`/`setRuleStatusSnapshot` 那一组（5 个）指向 `readRuleStatusSnapshot`、`multiavatarSeedFromValue` 指向 `avatarSeedFromValue`、`subscribePresenceCapableHostOffline` 指向 `subscribeAgentFastLivenessOffline` —— 新名字早就全站换完了，旧名字没有一处引用，留着只会让人以为是两个东西。
  - **被同一批删掉的调用方带走的**：`timeParam`、`compareTime`、`quoteIdentifiers`、`countDistinct` 等，删完再扫一遍才暴露出来，所以是反复扫到收敛为零。
  - **没接线的常量和分页/展示助手**：`MAX_LATENCY_CHART_MS`、`ONE_YEAR_MS`、`AXIOS_TIMEOUT_MS`、`TUNNEL_RELAY_MODE_LABELS`、`usePersistentPagination`、`hostAddressLines`。
  - **刻意留下的两处**：`server/pluginApi.ts` 的 `registerPluginEventHandler`/`emitPluginEvent`/`createPluginRuntimeContext` 和 `shared/pluginTypes.ts` —— 仓库里确实没有调用方，但它们是**插件按名字调的对外接口**，删了受影响的是装在外面的第三方插件，仓库内的静态扫描看不见；`stopTelegramBot` 同理留着，它看起来是个没接上的生命周期钩子，删掉等于把问题盖住而不是解决。
  - 验收是硬指标：`tsc --noEmit` 干净、1316 条服务端测试过 1315（剩下那条 `setupFlow` 超时是容器里本来就有的，CI 上是绿的）、`pnpm build` 通过、真面板十个路由逐个打开看主体内容都在。


- **打开面板少下 44% 的代码**。原来 26 个页面全是静态导入，打出来的主包 **3477 KB（gzip 960 KB）** —— 一个只想看「我的套餐」的租户，在手机上要先把 Settings（6460 行）、Rules（8778 行）、Plugins 的全部代码下完，才能看到第一屏。现在除登录页和仪表盘外按路由拆包：**主包 1832 KB（gzip 540 KB）**，页面代码点到才下（Rules 265 KB、Settings 167 KB 各自独立）。
  - 登录页和仪表盘**刻意留同步**：它们是所有人的入口，拆了会在最常见的那两屏上多一次往返、闪一下占位，省下的字节不划算。
  - 占位**延迟 250ms 才出现**。页面包是小文件，缓存命中时几十毫秒就到，那时候闪一下转圈比什么都不显示更糟 —— 屏幕会抖。
  - **Suspense 边界贴着页面放，不能包在整个路由表外面**。包在外面的话，页面代码还在下载时整棵树都挂起，权限守卫会跟着重渲染，而守卫在渲染期会返回 `<Redirect>`，那会同步改地址、进而更新所有 `useLocation` 订阅者（侧边栏就是一个）—— 「渲染 A 时更新了 B」，几个来回就撞上 React 的更新深度上限，整页掉进 ErrorBoundary。第一版我就是包在外面的，从侧边栏点「套餐管理」直接白屏报错；边界下移到守卫内部之后，挂起只影响页面那一小块。
  - 顺带：真面板上跑同一份冒烟，控制台告警从 **27 条降到 19 条** —— 重渲染轮次少了，本来就存在的那条「渲染期 setState」警告被触发得更少（那条警告 main 上原本就有，不是这次引入的，也没在这次修掉）。


### 版本

- 面板与 APK Release `2.3.362`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.361] - 2026-09-14

### 修复

- **拨「转发」开关时，面板报的是它自己发出去的值，不是真的落成了什么**。给一个**超额**的用户拨开启：服务端记下管理员的意图（`manualCanAddRules`），随后重算生效值 —— 仍然是关。可接口回的是写死的 `{ success: true }`，客户端照着自己刚发的那个值 patch 缓存并弹「用户转发已开启」。
  - 真面板复现：toast 说「已开启」，开关还是灰的，刷新一次原样；库里 `canAddRules=0`、`forwardAccessPauseReason='traffic_limit'`。管理员以为开好了，租户那边一条转发都跑不起来 —— 最后来问你。
  - 现在服务端把**生效值和被拦的原因**一起回来，界面据此回填、据此说话：开不成就说「没能开启：流量已超额，先重置流量统计或调高额度」，并且只说原因不够 —— 得给下一步。
  - **管理员的意图不回滚**：额度一放开就该自动生效，那正是 manual 那一列存在的意义。这里只是如实汇报。

- **乐观开关有个隐含假设：「请求什么，服务端就会变成什么」**。队列跑完时，它只在外部值和刚发的值相等时才回同步，不相等就继续显示刚发的那个 —— 而不相等恰恰是最该说出来的情形。上面那个「开关一直亮着但其实是关的」就是它造成的，而且**全站每一个乐观开关都有这个毛病**。现在跑完一轮一律以外部值为准。

- **`?tab=xxx` 这种深链接从来没生效过**（既有问题，不是这一版引入的）。判定 tab 的那段代码从 wouter 的 `useLocation()` 里 `split("?")` 取查询串 —— 而那个值**只有路径**，不含问号后面的部分，于是它永远是空，默默回落到默认 tab。不报错，只是你发出去的链接落在了错的地方。全站七个带 tab 的页面（主机 / 转发规则 / 链路 / 用户 / 支付 / 账单 / 商店）都受这个影响。
  - 判定逻辑抽成了 `pickTabValue` 单独测：地址栏 > 上次存的 > 默认。地址栏优先是因为它是**别人刚发给你的意图**，比这台浏览器上次停在哪要新。

- **套餐管理上那张「按量计费」卡片一直在说谎**。它读的查询带着 `enabled: activeTab === "billing"` —— 默认 tab 是「套餐」，查询根本不发，于是那张卡**永远显示「已关闭」**，哪怕库里开着。真面板复现：`trafficBillingEnabled = 'true'`，卡片写「已关闭」，开关是灰的。
  - 两面都错，而且都是关于钱的：你以为没在收钱，其实在收；你想关掉它，看到「已关闭」就根本不会去动那个开关。
  - 现在查询无条件发，那张卡只读说真话，开关收进旁边的「流量计费」tab 统一管 —— 一处配置、一处展示，不再有两个能碰的开关各说各话。

- **流量计费的总开关点不到**。`/traffic-billing` 这一页有路由、有总开关，但**全站没有一个链接指向它**，侧边栏里也没有，只能手敲 URL。中途我还在主机的计费弹窗里写过一句「要去『系统设置 → 流量计费』先打开」—— 那条路压根不存在（系统设置里没有这一节）。现在它在「套餐管理 → 流量计费」里，指路也改对了。


- **「每月自动清零」这个开关是死的**（上个版本刚加的，我自己的漏）。设置存得下、界面上一切正常，但调度器压根没去读这张表 —— 开关拨了、日子到了、数字纹丝不动。设置存了却不生效比没有这个开关更糟：人会以为已经安排好了，然后在某个月底被机房停机。现在四路（用户、主机、落地节点、落地端口）都接上了，并且有一条用例真的把调度那一段跑起来验，而不是只验「算不算到期」—— 只测算法测不出漏接。

- **自建的落地端口跑了多少，面板一直没在记**。上报进来的字节数只加到租户的套餐配额上，端口自己的已用量**永远是 0** —— 于是「这台落地机的套餐」那一整套（额度、到量提醒）对自建的端口完全不响。真库跑真实的 `/api/agent/traffic` 复现：自建端口和粘贴节点各上报 5GB，租户配额收到 10GB，粘贴节点记下 5GB，自建端口 **0GB**。
  - 方向正好是反的：**自建的那台机器才是你自己买的**，才有机房账单、超量才会被停机；粘贴进来的通常是别人家的，你反而管不着。
  - 记在**入站（端口）**上而不是派生节点上：Agent 的计数链装在监听端口上，一个多用户入站派生出好几个节点、共用这一个端口，字节数分不到人头（sing-box 官方二进制没有 per-user 统计）。所以「跑了多少」天然是端口的属性。派生节点上刻意不重复记 —— 记了再求和就会翻倍。
  - 计量归属的那几条守卫在端口这一侧同样守住：别的机器报你的端口号、停用的端口、负数上报，一个字节都不算。

### 新增

- **套餐计费和流量计费收进同一页：套餐管理下的两个 tab**。两者是同一件事的两种卖法（包月吃额度 / 按 GB 扣余额），「我这个月怎么收钱」不该分两个地方问。原来要把按量收费配起来，得跑四个地方，而且没有任何一处告诉你缺哪一环：总开关在一个点不到的页面里，资源定价在套餐管理的一个 tab 下（那个 tab 又刻意不渲染总开关），授权在用户管理的用户编辑弹窗里，余额在账单与兑换。四处都对了才真的收得到钱，错一处就是静悄悄不生效。
  - 「流量计费」这个 tab 回答一句话：**「我这套配好了没有，还差什么」**。顶上一份四步清单，每一步给的是能拿来做判断的数而不是一个勾 —— 「2 个资源在计费（公开 0 个、需授权 2 个）」和「都要授权但一个人都没授权过」是完全不同的处境，缩成 ok/not ok 等于没说。缺哪一环就在那一行给一个跳转。
  - **授权只数启用中资源上的**。停用资源上的旧授权不代表谁现在用得上，拿它去说「已经授权了 2 个人」是在报一个假的就绪状态。
  - **总开关关着时，清单第 2 步改口**说「2 个资源配了价，但总开关关着，现在一分钱都不扣」，而不是继续说「在计费」—— 上面的警告条已经那么说了，这一句再说反就是自相矛盾。
  - 顶上保留一张只读的按量计费状态卡，「去设置」直接切到旁边那个 tab。老地址 `/traffic-billing` 保留成跳转，存过书签的人不吃 404。

- **租户那一侧：「我的套餐」现在也讲按量计费**。一个纯按量计费的租户在这一页上原来是一片空白，还被劝「当前账户还没有套餐记录，可以去商店自助下单」—— 而他的「还剩多少」根本不是流量额度、是余额，在账单中心那一页。他真正要知道的三件事（在按什么价用、花了多少、余额还够不够）一件都看不到。
  - 现在多一张「按量计费」卡：**你有 N 条转发按 GB 扣余额，¥X/GB** · 当前余额 · 累计已扣 · 已计费流量，右上角直接「去充值」。几条转发单价不一样时给区间（「¥0.5–2.8/GB」），不编一个平均价。
  - **单价给他看**。主机卡片上对非管理员藏价钱是因为那是商家给别人机器的定价；这里是他自己在付的钱，藏起来才不对。
  - 余额是 0 的时候那张卡直接报红：**扣不动的时候会停掉你名下全部转发**，不只是按量的这几条。
  - 空状态跟着改口：他在按量计费的话，不再劝他去买套餐，而是说清「你走的是按量计费，不需要套餐也能用」。
  - 只在他真的有转发在按量扣钱时才出现 —— 纯套餐用户看到「余额」只会以为自己还得再掏一笔。


- **每台机器可以整台按量计费了**。原来计费只能按**转发组 / 隧道**配 —— 可商家是按台买机器、机房也是按台出账单的，「这台一律按 ¥2/GB」得给这台上的每个转发组各配一遍，漏一个就有一批流量悄悄不计费。现在主机管理的卡片菜单里直接有「按量计费」，填个价就行；「流量计费管理」那一页的资源类型里也重新有了「整台主机」。
  - 底层**没有新东西**：转发找计费配置本来就是 转发组 → 隧道 → **主机** 三档，主机那一档一直在跑，只是界面上被标成「历史主机 / 旧版本主机计费资源」并禁用了新建。这次是把那一档重新接出来，顺手改叫「整台主机」。
  - **它是兜底，不是覆盖**。这台上的转发，凡是被它所属的转发组 / 隧道单独计价过的，仍然走它们自己的价。这一条在弹窗里、在卡片的悬停里都写着 —— 不说的话，人改了整台的价却发现某几条的账没变，只会以为面板算错了。
  - **「需要单独授权」默认开着**。关掉等于把这台机器摆进商店：面板上任何有余额的用户都能在你这台机器上开转发。一个转发组这么配还行，一台机器不行，所以这里的默认和「流量计费管理」那一页反过来。
  - **总开关关着时弹窗会明说**「这里配了也一分钱都不会扣」，而不是让人配完了等账单。
  - **改计费资源会牵动授权**：靠这个资源才用得上某台机器的用户，可能因此失去访问、他的转发被停。这件事原来只写进服务端日志，现在保存/取消之后直接弹一句「顺带停掉了 N 条失去资源授权的转发」—— 挨停的是别人的业务。
  - 主机没有「链路倍率」这一列（隧道和转发组才有），所以那一栏照实写「1x（主机没有链路倍率）」，不再写会让人误会的「沿用旧配置」。
  - **保存前先算清楚会影响谁**。兜底价接管的是这台机器上**所有**没被单独计价的转发 —— 包括走套餐的租户的，因为计费那条路的判断里根本没有「这个人是套餐户还是计费户」（面板里没这个字段）。而套餐户通常余额是 0，余额 ≤ 0 会被**停掉名下全部转发**，不只是这台机器上的。所以弹窗里直接摆出来：「14/14 条会改成按 GB 扣余额 · 其中 2 位是有套餐额度的租户 · **2 人余额为 0，会被停掉名下全部 4 条转发**」，并给出两条出路（先充值，或者改成按转发组 / 隧道单独配）。不摆的话，你只有在租户来问「我的转发怎么全停了」的时候才会知道。
    - 预检算的是**不含兜底价那一档**的解析结果，所以在一台已经配过价的机器上重新打开，它说的仍然是「这 3 条正靠兜底价计费」，而不是「0 条受影响」。
    - 「会被停掉几条」按**人头**汇总而不是按这台机器 —— 停的本来就是这个人名下全部的转发，只报这台上的会把后果说小，而这正是人拿来做决定的那个数。

- **节点多了一个「对外标注」，分享给租户时对方看得到**。之前分享出去的节点，租户那边一律标着「直连」—— 而备注（「家宽」）是**刻意抹掉**的：弹窗里写着「只给你自己看」，因为备注也可能是「给张三的」「便宜线」「机房账号 xxx」这种不该外流的话。
  - 抹干净是对的，但抹完之后收方那一屏就只剩一个没有信息量的「直连」，而「这条是家宽还是 IEPL」恰恰是他最想知道、又只有你说得出的一件事。
  - 所以另开一个字段：**备注继续只给自己看**（承诺不变），**对外标注跟着节点走**。合成一个字段的话两种用途只能二选一 —— 要么泄露账本，要么租户永远看到「直连」。
  - 刻意**不拿备注给它兜底**：那等于把账本换个名字泄出去。没填就照实没有。
  - 已经写在备注里的字不会自动搬过去（同样的理由），要给租户看见得在新字段里再填一次。

- **自建节点终于能设带宽和总流量了，而且是两层**。原来「这台落地机的套餐」那一段只在粘贴节点的编辑弹窗里，自建节点走的是入站编辑，那里根本没有这几个字段。现在两层都给：
  - **这个端口的额度**（入站编辑里）—— 带宽、总流量、已用、每月自动清零。只数**面板经手的这一个端口**。
  - **这台机器的机房额度**（主机管理里本来就有，这次接到订阅管理页上）—— 自建节点行上直接显示「整机已用 780G / 1T（78%）· 机房口径 · 双向」。用的是 Agent 报的**系统级网卡计数**：订阅里的直连条目、这台机器上跑的别的服务，全都在里面。机房就是按网卡算的，而「还剩多少」报小了等于没报 —— 人以为还有余量，机房那边已经停机了。
  - 两个数**必须能一眼分开**，所以行上写「本端口 …」和「整机已用 …（机房口径）」，两层各自到量各自变色（取更严重的那个：机器超了而端口没超，一样要被停机）。不点破区别的话，人拿端口那个数去对机房账单永远对不上，然后两个数都不敢信。

### 变化

- **用户管理在手机上改成一人一行，点开看详情**。原来每个用户在窄屏上是一整张卡：套餐、按量、余额、到期、Telegram、邮箱、四个配额、两个开关、三个按钮 —— **一屏一个人**。可管理员在这一页干得最多的事是**找人**（「张三那个号还在不在」「谁超额了」），而找人只需要名字和状态；剩下那十几项是找到之后才看的。把它们平铺在列表里，等于每找一个人都要滑过别人的全部家当。
  - 行上只留用来认人和判断要不要点进去的：头像、用户名、角色、`#id · 备注 · 已用/额度`，外加**异常徽章**（禁用 / 超额 / 已到期）—— 这三样正是扫这一屏在找的东西，藏进详情就得一个个点开找。
  - 其余的原样搬进详情弹窗，**一项没少**，开关和「更多」菜单都照旧能用。
  - 「编辑」会先关掉详情再开设置弹窗：两层弹窗叠在一起时，关掉上面那个会连下面一起关，人会以为自己的改动没保存。
  - 弹窗里的数据按 id 从当前列表现取而不是存一份快照 —— 充完值、拨完开关，弹窗里的数字跟着变，而这一屏正是拿来看这些数的。
  - 宽屏不变，仍然是表格。


- **主机卡片上直接写出「这台机器怎么计费」**。同一条转发，扣的是**余额**还是**套餐流量额度**，两条路互斥，而配置原来只藏在弹窗里，列表上一个字都没有。想知道哪台在计费，得一台台点进去看，而记错账的代价是真金白银。现在每张卡上一行：在计费的标琥珀色「按量计费 · ¥2.8/GB」，没有的写「走套餐流量」，悬停里说清楚「扣余额、不记进套餐额度、余额扣完自动停转发」。
  - **这个结论不能问主机要**。计费配置挂在**转发组 / 隧道**上 —— 主机那一档是旧版遗留（界面里写着「历史主机」并且已禁用新建），运行时只当最后一档兜底。我第一版就只查了主机那一档，于是**新部署上每台都会显示「走套餐流量」**，哪怕上面的转发正按组计费。摆一个关于钱的结论在显眼处，就得是真的：现在顺着这台机器上实际在跑的转发去问，每条按 转发组 → 隧道 → 主机 找配置，和真正扣费那条路用的是同一个函数。
  - **一台机器上未必整台一个样**，所以标的是条数：「1/13 条按量计费」。写成整台非黑即白的话，那 12 条走套餐的就被这句话盖掉了。同一台上有几种单价时只说「按量计费」，不编一个平均价出来。
  - **总开关关着就一个字都不说**。总开关一关，扣费那条路直接不走（`agentReportRoutes` 里是同一个判断）—— 卡片上还挂着「按量计费 ¥2.8/GB」就是在骗人。
  - **单价只给管理员**：租户只需要知道这台上的转发在按量计费，价钱是商家的事，服务端对非管理员根本不下发这个字段。

- **主机管理这一页对租户开放了**。他能自助加机器，就得有个地方看这些机器到底什么情况 —— 原来只有「我的机器」里一个在线小圆点，而「在线」回答不了「这机器还撑得住吗」。现在整页给他：CPU、内存、磁盘、流量、延迟、运行时长，和管理员看到的是同一套卡片。
  - **看得到什么，以授权为准**：自己加的机器，加上管理员显式授权给他的那些。没授权的机器在他那儿从头到尾不存在。
  - **别人的机器只读**：`hosts.update` / `hosts.delete` 服务端本来就按「是不是主人」挡着，界面据此不渲染改名和删除的入口 —— 留着按钮只会让人点下去吃一句「无权操作此主机」，那不是提示，是绊脚石。
  - **管理员专属的入口一律不渲染**：分组管理、Token 管理两个 tab，检查 Agent 更新、一键升级 Agent、「N 台发现新版本」、卡片菜单里的升级 Agent、重置流量、用量修正。逐条按租户身份在真面板里点过一遍。
  - 顺带发现并修了两处：卡片菜单里的「升级 Agent」原来是渲染出来再 disabled，对租户就是个永远灰着的死项；「添加主机」按钮点了没反应 —— 那个按钮走的是「Token 管理」组件的信号，而那个组件只在管理员那边挂着。加机器的弹窗抽成了 `AddSelfServiceHostDialog`，两个入口共用一份。

- **每个租户能自助加几台机器，现在可以单独设**。原来只有系统设置里一个全局数，想给某个人多开两台就得把全体调高。用户编辑里多了「能自助加几台机器」，按钮上直接写着「添加主机 5/10」，到量了按钮就点不动。
  - **留空 = 跟随全局，填 0 = 一台都不许加**。特意分成两种而不是学旁边几个「0 = 不限」：这是个数量字段，人填 0 就是想说「一台都不给他」。当成「不限」就把他放开了，当成「跟随全局」就等于根本没法卡死某一个人 —— 两种都和填的人想做的事相反。
  - 为此「不限」在内部改用 `null` 表示：老的判断是 `limit <= 0 就放行`（那时候只有全局设置、0 就是不限），照那个算，把人卡到 0 反而是把他放开。

- **租户面板上「我的节点」那条工具条不再空一截**。挤不下时这一组会被撑成整行宽，而它写的是 `justify-end`，于是每一行都往右推：第一行「自动」左边空出一截，第二行只剩「新建」孤零零贴在右边。改成 `justify-start` —— 没换行时这一组是内容宽，外层本来就把它推到右边，所以只影响换行那一种情况。也没写成 `sm:justify-end`：宽屏一样会换行（窗口拖窄、侧栏展开），那个洞又会回来。

- **租户能看到自己机器的状况了**。租户可以自助加机器，但「我的机器」原来只有一个在线的小圆点 —— 而「在线」只回答了「Agent 连上没有」，回答不了「这机器还撑得住吗」。自己掏钱买的机器，内存满了、磁盘满了、刚重启过，他都该看得见，否则只能来问你。现在行上直接给出 **CPU / 内存 / 磁盘 / 已经跑了多久**；离线时标成「最后一次」，不把冻住的数字说成此刻的读数。
  - 走的是已有的 `latestMetricsSummary`（本来就逐台过 `requireHostAccess`），**没有开新接口，也没有把主机管理那一整页放给租户** —— 那里还有分组、令牌、Agent 升级和删任意主机，不是这个需求要的东西。

- **「每月自动清零」下面那句说明去掉了**，直接填日期。

- **主机列表上标出「这台机器是谁的」**。租户可以自助加机器，加完就出现在管理员的主机管理里 —— 那是对的（面板是管理员在跑，出了事要能查、要能删），但列表上原来一个字都没说这是谁的，管理员看到的是一台凭空多出来的陌生机器：不知道能不能动它，也不知道该找谁。现在卡片上多一行「归属：张三」。
  - **只给管理员**。普通用户能看见的除了自己的，还有被授权用、或套餐附带的别人的机器 —— 在那儿标出主人等于把另一个租户的身份透给他。这是隐私边界，不是显示偏好，所以单独测。
  - **自己建的不标**：满屏都是自己的名字等于没标，还会把真正该注意的那几台淹掉。
  - **主人注销了也要说**（「已注销用户 #N」），不能静悄悄当成自己的 —— 一台没人认领的机器恰恰最该被看见。
  - 名字按用到的 id 查，不整表读用户表。

- **离线的机器不再把旧数字说成「当前」**。速率是拿最后两次采样算的，运行时间也是最后一次上报里的值 —— 机器一掉线它们就冻住，可标题还写着「当前瞬时流量」。于是同一张卡上，红色的「离线」和「当前 4.09 KB/s」并排放着，看的人没法判断这机器是真在跑，还是这串数字是三天前的化石。现在离线时改叫**「最后一次」**和**「最后运行时长」**，悬停里补上上报时间。数字照留 —— 掉线前跑到哪儿是有用的线索，抹掉更糟。

- **每月自动清零的日期放开到 31 号**。原来卡在 1-28，理由是 29/30/31 在二月不存在、落在那几天会整月不触发。担心是对的，但**该修的是触发判断，不是不让人填** —— 机房按月末结算太常见了。现在填 29/30/31 就是「月末」：二月自动落到 28 号（闰年 29 号），四月落到 30 号。
  - 触发那一侧同步改掉了。原来节点这一路有条 `重置日 <= 今天几号` 的 SQL 预筛，放开到 31 之后它会漏：二月 28 号那天 `31 <= 28` 不成立，设成每月 31 号的整个二月都不重置，而用户看到的只是「设了自动清零却从来没清过」。改成只按开关取、日期交给 `billingMonthlyBoundary` 夹当月天数 —— 主机那一路一直就是这么做的，两边算法这下一致了。
  - 用户、主机、落地节点、落地端口四路的上限一并放开，`MONTHLY_RESET_MAX_DAY` 是唯一一份定义。

- **「高级」平时一律收起来**。原来是「设过非默认值就自动展开」，出发点是别让人以为配置丢了 —— 结果变成「谁配过谁的弹窗就永远长一截」，而改名字、换链接这种事天天做，选路和前置代理配一次就不动了。
  - 现在一律收着，**折叠条上直接写出里头设了什么**（「高级 · 主备切换 · 经由 新加坡中转」）。看得见就不会以为丢了，这比整段摊开便宜得多。
  - 自建端口的 REALITY 那两项同样收起来（它们留空就有默认值），折叠条上写「按默认值」或已填的域名。ACME 和 TLS 那两段**刻意不收**：域名、证书路径是必须填的，收起来等于让人存下一个跑不起来的配置。可收的只有「不填也对」的那一类。

- **展开那一行改成「已用」排最前**。原来是「带宽 2G · 总流量 100G · 已用 92G（92%）」，手机上放不下会被截断 —— 正好把唯一要紧的那个数切掉。现在是「已用 92G / 100G（92%）· 带宽 2G」，截断了也先看见它。顺带不再用 `2G / 100G / 92G` 那种紧凑式：那三个数里第一个是 Gbps、后两个是 GB，挤在一起读起来像同一种单位。

### 说明

- 差点重复造轮子：动手加 hosts 的额度字段时才发现**主机层早就有了**（`trafficLimit`、`trafficMeasureMode`、月度自动清零、Telegram 到量告警，tRPC 也接好了），只是没出现在订阅管理这一页上。已经加的那份撤掉了，改成把现成的接过来。
- 顺手合并了三处重复：GB↔字节的换算（`shared/trafficGb.ts`，1000 而不是 1024，两个弹窗填同一个数不能显示成两个值）、主机流量口径的换算（`shared/hostTrafficQuota.ts`，原来在主机卡片和主机监控里各写了一遍 —— 这个数决定「我是不是快被停机了」，两个页面给出不同答案比给错答案更让人不敢信）、节点用量单元格（`ProxyNodeQuotaCells.tsx`，粘贴与自建两路共用）。
- 顺手补了一处：入站编辑弹窗打开时，安全层和传输方式先按协议收敛一次再进表单。服务端保存时本来就会收敛，所以正常存下来的行不会不合法；但旧版本、迁移数据或直接改库仍可能留下「协议是 Shadowsocks、安全层却写着 reality」这种组合，那时候弹窗会凭空显示一段这个协议根本用不上的设置（本地就撞见了）。界面不该比服务端更容易信一个不合法的值。
- 新增真库回归用例覆盖端口计量（并发累加、跨机器上报、停用端口、负数、派生节点不重复记），两处反向验证过：拿掉累加会红，把累加挪到归属校验之前也会红。另有 `shared/hostTrafficQuota` 与用量排序的单测。

### 版本

- 面板与 APK Release `2.3.361`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。
## [2.3.360] - 2026-09-14

### 修复

- **「已拉取 N 次」这个数是假的：二十个客户端同时拉，只记一次**。一条订阅地址同时被好几个客户端拉是常态 —— 一个人手机、电脑、路由器各刷各的，客户端默认十二小时一轮，起床时间又差不多，几路请求撞在同一秒里很正常。而记这个数的写法是先把当前次数读出来、加一、再写回去：几路请求读到同一个旧值，写回同一个新值。真库复现，**二十次并发拉取，数字只涨到 1**。
  - 商家看这个数就是为了回答「客户端到底拉没拉过、拉得勤不勤」—— 被吞掉的恰恰是最该看见的那一部分，而且**越活跃的租户吞得越狠**：拉得越勤，撞车越多，数字越假。这个数是 v2.3.358「客户说订阅更新不了」那一整套的地基，地基是歪的。
  - 加法交给数据库自己做，不再先读后写。
  - 顺带查了别处：用户流量累加、余额扣减都已经是原子写或者带行锁的事务（真库并发验证过余额那条，二十笔同时扣，一分不多一分不少），只有这一处是先读后写。

- **面板里「订阅内容」那一屏把整份订阅组装了两遍**。这一屏要两样东西：算出来的 plan（谁进了、谁没进、为什么），和渲染出来的 document（策略组长什么样）—— 上个版本我把重复查询合掉的时候，只合了库里那一层，**漏了这个调用方**，它仍然分两次去要，于是打库次数正好翻倍。
  - 慢一倍还是次要的。真正的问题是那是**两次独立读库**：中间只要有人删掉一条转发，这一屏就会自相矛盾 —— 策略组里列着一个节点，底下的节点清单里却没有它。而这一屏存在的全部意义就是回答「我的订阅里到底有什么」，它自己前后不一致，比慢一点严重得多。
  - 合成一次 `getProxySubscriptionPreviewForUser`，两样东西来自同一次读库。

### 变化

- **一个落地节点都还没有时，「差一个落地节点」不再摆几十个选不了的下拉**。本地按真实数据看了一眼：33 条待绑定的转发，每条后面一个禁用的下拉，写着同一句「请先添加落地节点」—— 重复 33 遍，把真正该做的那一件事（去加个节点）挤到滚动条外面看不见。现在这种情况下只列名字，一屏就看完，那句话改成「先加一个节点，它们就能一起认上来」：**要动的手只有一次，不是几十次**。有节点时逐行下拉照旧 —— 那时候它是对的。

### 说明

- 两处都有真库回归用例并反向验证过。其中订阅预览那道守卫我第一版写成「预览的打库条数应等于渲染文档的条数」，反向验证时不变红 —— 因为渲染文档如今本来就走预览的实现，拿它当基准，两边只会一起变，等于没订。改成按**绝对条数**订（每张表至多一次、总数不超过 5 条）才真正拦得住。这是同一类错误第三次出现：**守卫要订在会独立变化的量上，订在会跟着一起动的量上就是订了个寂寞**。

### 版本

- 面板与 APK Release `2.3.360`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.359] - 2026-09-14

### 变化

- **组装一份订阅从 9 条查询降到 5 条**。订阅地址是**没有登录态、按客户端节奏来**的一条路：默认十二小时刷一次不觉得，可刷新间隔是用户自己填的，一个商家几百个租户里总有人填几分钟。这条路上每多跑一条查询，就是几百倍地多跑。原来白跑两处：
  - **节点模板查了两遍** —— plan 里查一次，外面渲染文档时又查一次，而它们本来就是一次能查完的（每遍三条：自己的、别人分享的、独立凭据的）。合成一个 `buildProxySubscriptionContextForUser`，一次查完两边共用。
  - **主机整表读** —— `select * from hosts` 不带任何条件，而实际用到的只有「这些转发的入口机」加「这些自建节点所在的机器」。改成按用到的 id 查。
  - 实测（40 次拉取取平均）：50 台机器 / 40 条转发 / 20 个节点 **4.0ms → 2.8ms**；500 条转发 / 200 个节点 **14.3ms → 10.0ms**；1000 台机器时 **6.1ms → 2.9ms**（整表读的那一刀在机器多时最明显）。

### 说明

- 先量后改：一开始我以为瓶颈是那个整表读，量下来 20 台到 1000 台只从 4.7ms 涨到 6.1ms —— **猜错了**，真正随规模涨的是转发数和节点数（那是真活）。最大的一笔浪费其实是重复查模板。
- 新增一组**打库次数**的守卫用例：钩住 sqlite 的 `prepare`，数一次订阅组装到底打了几次库、有没有不带条件的整表读。不测「快不快」（那会变成看机器脸色的脆测试），只测形状。这道守卫我第一版订成了「≤3 条」，反向验证时把重复查询加回去竟然照过 —— 阈值订在了空处；看过真实语句后改成「每张表各一次」，才真正拦得住。

### 版本

- 面板与 APK Release `2.3.359`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.358] - 2026-09-13

### 新增

- **「客户说订阅更新不了」，面板现在答得上来了**。订阅地址被拉取时原来**只记成功**：令牌行上写着「已拉取 3 次」，全是几天前的。于是四种完全不同的情况长得一模一样 —— 客户根本没试（地址填错、客户端没刷新）／这条地址被停用了／账号到期或超流量没资格了／服务端生成订阅时出错了。第三种要做的是去续费，第一种是让客户重新导入，判错方向就是白折腾一晚上。
  - 现在被拒的那一次也记下来，并记下**为什么**。令牌行上直接写「最近一次没拉到：账号没有订阅资格（到期、超流量或权限被收回）」。
  - 给客户端的回应**一个字都没变**：仍然一律是含糊的 404，不泄露「这个令牌存在但没资格」。要含糊的是给外面的回应，不是给商家自己的记录。
  - 拉成功了就把上一次被拒清掉 —— 「现在到底行不行」是这一行要回答的唯一问题，客户已经拿到了就别再挂着红字。
  - 有一种**记不了**，界面上也不假装能分辨：令牌本身不存在（地址被改过、被重置过）时，请求落不到任何一行上。

### 说明

- 「该说成功还是失败」抽成 `shared/proxySubTokenStatus.ts` 单独测（失败在后就说失败、成功之后不再挂红字、认不出的原因当没有、每个原因都得是人话不能是英文代码）。另有一组真库用例直接起 HTTP 服务打那条路由，逐条走过停用／过期／没资格／成功／令牌不存在五种情况。三处守卫反向验证过。另外在本地真实面板里用 curl 模拟了一次客户端拉取，确认令牌行上真的显示出来了。
- `accessCount` 只算成功，被拒不计 —— 否则「已拉取 N 次」会把被挡回去的也算进去，那个数就不再是「客户拿到过几次」。

### 版本

- 面板与 APK Release `2.3.358`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.357] - 2026-09-13

### 修复

- **机器的 Agent 还没装，节点照样发进订阅**。租户自己加了机器、在上面开了落地端口 —— 但那台机器的 Agent 从没连上过，配置根本下发不下去，sing-box 压根没跑。可面板把这个端口当成一条好线路直接发进订阅，客户端拉到手连不上，而这一页上一个字都没提过。真库复现：`direct:新开的节点@203.0.113.50:34567`，`skipped` 和 `warnings` 都是空的。
  - **仍然照发**，只是说清楚。机器可能下一分钟就连上了，而把人刚建好的节点悄悄拿掉比发出去更糟 —— 他会以为没建成功，再建一个。
  - 「订阅内容」里多一段**「机器还没连上面板」**，写明是哪台机器、下一步去哪拿安装命令；节点行上直接写**「机器的 Agent 还没连上」**。
  - 刻意只认「**从没连上过**」，不认「掉线」：掉线是暂时的（重启、网络抖一下都会掉），行首那个状态点已经在说了，拿它去告警就是天天报。判据是心跳 —— 收过一次，就说明 Agent 曾经装好过。

### 说明

- 判据抽成 `hostNeverConnected` 并单测（没心跳=没装、有心跳就不报、时间戳认不出来时宁可漏说也不对着好机器喊）。真库用例覆盖三条：没连上过要报、补一次心跳后不许再报、粘来的节点没有机器所以任何时候都不报。三处守卫逐条反向验证 —— 其中「照样要发出去」那条第一次的反向验证是无效的（改错了位置，数组早已拷贝），重做一次才真正变红。

### 版本

- 面板与 APK Release `2.3.357`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.356] - 2026-09-13

### 修复

- **租户的「我的节点」工具条会溢出卡片**。租户那边比管理员多一个「我的机器」按钮，四个控件加起来 426px，而手机上卡片只有 390px —— 那一组被写成了 `shrink-0`，既不能缩也不能换行，于是整条从卡片右边冲出去 49px 被裁掉：左边的「自动」下拉被切掉半个，右边的按钮贴着屏幕边。本地按租户的条件复现并量了尺寸，改完重新量：364px，收在卡片里了。

### 变化

- **节点按来源分色**。自建、粘贴、别人分享来的三类合成一个列表之后，「这条能不能改、凭据归谁」成了最常要分辨的事，而原来只写在第二行的灰字里，一眼扫过去全一样。现在每行左边一道很淡的色条：**自建青**（和「我的节点」那段的色章、「落地节点」概览卡同色）、**粘贴天蓝**、**分享紫**。只做色条不染文字 —— 颜色是辅助，文字仍然是说清楚的那一个；色盲、截图转灰、深色模式下对比度不够，这一行都还读得了。
- **节点行改说后果，不只说机制**。原来只写「无转发绑定」—— 那是机制，而且和「客户端看不到它」并不等价：粘来的节点开了直连，没有转发照样在订阅里。现在真正谁都没用它时，行上直接写**「不在订阅里」**。判断来自服务端组装出的那份订阅内容（它才知道谁被引用了），和「订阅内容」弹窗里那一段是同一个结论 —— 只是也落到了要动手的那一行上。

### 版本

- 面板与 APK Release `2.3.356`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.355] - 2026-09-13

### 变化

- **订阅管理换成和仪表盘一样的框架**。原来一进这一页就是两张白卡片，得逐个展开才知道「我现在到底有几条线路、有没有东西要处理」。
  - 顶上补了一排概览卡：**客户端线路**（中转 / 直连各几条）、**落地节点**（在线 / 离线 / 待探测）、**订阅链接**、**待处理**。这几个数就是这一页的全部问题，摆在最前面，一眼看完。
  - 「待处理」把散在预览弹窗三段里的事合成一个数（差落地节点的转发 + 指向已变的 + 没进订阅的节点）—— 进这一页时脑子里其实只有「我有没有东西要管」这一个问题。没有就写「都对上了」。
  - 卡片统一换成仪表盘那套玻璃卡 + 顶部一道高光；页头排版也对齐，同一个面板里不该有两种气质。
  - 概览卡组件从仪表盘里挪出来共用（`components/StatCard`），不是抄一份 —— 抄一份的代价是以后调圆角、调悬停要记得改两处，漏改的那处会显出两种气质。
  - **颜色从概览卡一路带到下面的分区**：「订阅链接」在概览里是紫的，下面那一段的小色章也是紫的；「落地节点」是青的，「我的节点」那一段也是青的。图标散着放也能认，但认的是图标；带上颜色认的是「这一块」，上下两处能对上。
  - **讲订阅内容的两张卡可以点**：「客户端线路」和「待处理」说的是同一件事的两面 —— 订阅里有什么、还差什么 —— 点一下直接打开「订阅内容」。写着「你有 N 件事要处理」的卡片本来就该是点进去处理的入口；让人看完数字再自己去别处找那几件事，等于把已经走完的一半路又还给他。可点的卡片标题后跟一个很轻的 `›`，不喊「点我」，但让人知道后面还有东西。
  - **手机上把卡片撑宽了**：原来三层内边距叠着吃宽度（外层 12px + 卡片 24px + 每行自己的 12px），414 的屏幕先去掉 72，内容被挤成窄窄一条。卡片这一层在手机上收到 12px，桌面端照旧。

### 修复

- **「离线数为 0」被当成了「全都在线」**。刚建好、一次都没探测过的节点既不在线也不离线，可页面据此写出「节点全在线」，概览卡上还会出现「9 个节点：0 在线 · 0 离线」这种自相矛盾的话。现在没探测过就如实说「N 待探测」，全部探测过且都通才敢挂「节点全在线」的徽标 —— 不知道的时候徽标干脆不出现。

### 说明

- 「怎么说在线情况」抽成 `summarizeProxyNodeHealthCounts` 并单测（没探测过不算在线、有一个没探测就不能说全在线、有离线就直说几个、一个节点都没有时不谈在线、数对不上不写负数）。这一版的界面改动是**在本地真实面板里跑起来看过的**：桌面端、手机端、以及仪表盘本身（确认抽出组件后没走样），不是只过了编译。

### 版本

- 面板与 APK Release `2.3.355`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.354] - 2026-09-13

### 修复

- **攒版本发布时，装 Agent 必然失败**。安装脚本把下载地址钉在面板版本上，那一版资产还没就绪时会回退到「上一版本」—— 而这个上一版本是**算出来的**：补丁号减一。它默认每个补丁号都发过版，可攒十几个版本发一次是常态（v2.3.341 → v2.3.353 中间那些从来没有 release），算出来的版本根本不存在，回退地址一样 404，装机就地失败且毫无线索。上一版发布时就踩了这一脚：v2.3.353 的兜底指向从未发布过的 v2.3.352。
  - 现在真要回退时去查仓库里**真正发布过**的最新版本；查不到（网络不通、API 被挡）才退回那个猜测值，总比什么兜底都没有强；用户自己用 `FALLBACK_RELEASE_VERSION` 指定过的，谁都不覆盖。
  - 只在回退这条路上查一次，正常安装不会多一次网络请求。

### 变化

- **配周期不用再自己填天数了**。原来是一个「加一档」按钮，点出来一行空的、要手填「120」这种天数 —— 可商家心里想的从来不是「120 天」，是「月付、季付、年付」。让人把脑子里的词翻译成天数，等于把我们的存储格式摊给他看。
  - 现在是一排可点的周期：**一个月 · 三个月 · 半年 · 一年 · 两年**，点一下就开卖，再点一下就下架，只剩「填个价」一件事。
  - 确实有人卖 45 天、100 天这种，所以留了「其他周期」，点开才出现天数输入框 —— 少数人的需求不该挡在多数人前面。已经配了非预设天数的套餐照旧显示成可编辑的天数行，不用动。
  - 每一行后面直接折成「约 ¥X/月」，商家自己也要一眼看出这几档之间划不划算；这个换算和商店卡片共用一份，免得同一档在两处显示成两个数。
  - 文案跟着改：「购买周期（选填）」→「卖哪几种周期」，说明改成「点一下就开卖，再点一下就下架。一种都不选，就只卖上面那一档」。

### 修复

- **开了一档却忘了填价，会按 0 元卖出去**。管理端看上去一切正常，商店里那一档直接白送。现在保存时拦下来并点名是哪一档（「一年」还没填价格）。0 仍然是合法价 —— 真要送就填 0，只拦「空着」。

### 新增

- **落地节点的流量到量提醒**。节点上那个「总流量」原来纯是个仪表盘：填了 500G，用到 700G 也照常服务，除了界面上一个红色小图标什么都不会发生。而这个数字通常来自机房 —— 跑超之后是**机房**把机器停掉，客户端里这条线路直接断，商家往往等客户找上门才知道。
  - 现在到量了发邮件和 Telegram（两个渠道各发一次，互不顶掉）。**不做硬上限**：把它当机房规格填的人不该因为一次升级突然少几条线路。
  - 阈值直接复用界面那个仪表盘的 —— 图标变黄的同一刻发提醒。两套阈值迟早会对不上，那时候「界面说没事、邮件说快满了」比不提醒更让人困惑。
  - 「快满了」和「已经跑满」是两条不同的提醒：只按节点去重的话，先发了 80% 那封，当天真跑满时更要紧的那封就发不出去了。

### 说明

- 兜底版本那段 shell 直接抠出来在 bash 里跑测试，curl 用桩替掉 —— 只比字符串没有意义，这段的价值全在「查得到用查到的、查不到退回猜的、用户指定过谁都别覆盖、重复调用不重查」这几条分支上。节点提醒的判定是纯函数（6 条单测），另有一组真库用例确认「没设上限的不查、没到量的不报、快满与已满是两把去重钥匙」。五处守卫逐条做过反向验证。

### 版本

- 面板与 APK Release `2.3.354`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.353] - 2026-09-13

### 修复

- **绑定会过期，而过期的绑定会把落地凭据递给别的机器**。「这条转发通向那个落地节点」只在保存那一刻成立过 —— 之后目标地址能改、节点自己的地址端口也能改，而绑定一直留着。留错了不只是订阅里多一条连不上的线路：订阅里那条节点**带的是这个落地的凭据**（uuid、Reality 公钥、SNI），地址写的却是转发入口；入口现在通向别处，客户端就会把这套凭据递给那台别的机器。真库复现：把一条已绑定转发的目标改到一台无关机器上，订阅照旧发出「入口 → 我的落地」，凭据一字不差。
  - **保存时能判定的就修正**：原来是字面相符的绑定（也就是面板自己认出来的那种），改了目标之后重新对一次 —— 新目标正好是另一个节点就**改绑**，谁都不是就**解绑**。两种都会明说，尤其解绑：不说的话，他只改了个目标，客户端里一条线路就这么没了。
  - **判不定的一个字都不动**：原本就不是字面相符的（串两跳、一边写 IP 一边写 DDNS 域名）是他自己搭的拓扑，面板没有判断权。
  - **判不定但看起来不对的，照发 + 告警**：老数据、手改库、或者节点自己换了地址端口 —— 这些保存路径管不到。订阅里那条**仍然发出去**（静默少一条节点是这套面板反复踩过的坑），但订阅管理的「订阅内容」里多一段「指向已经变了」，写清楚绑的是谁、现在指向哪；转发页那一行的「订阅」角标变成警示色的**「订阅·指向已变」**，鼠标放上去说清后果 —— 要改目标或换节点的人就在那一页。
  - 告警刻意会认「串两跳」：目标指向自己另一条转发的入口是正常拓扑，不报警。判不准时一律不下结论（有一边是域名就不判），宁可漏报也不要把一条好线路标成坏的。
- **一个节点可以谁都没用它，而页面上看不出来**。没开「直连也放进订阅」、又没有任何转发绑到它 —— 这个节点在客户端里根本不存在，可「我的节点」里它看着好好的（在线绿点、流量、备注一应俱全）。这就是当初「转发不绑节点就不进订阅、而转发页上看不出少了这一步」那个坑，从节点这一侧再犯一次。现在「订阅内容」里会列出「这些节点还没进订阅」，并说清两条出路：给它建一条转发（目标填它的地址端口，会自动认出来），或者打开「直连也放进订阅」。停用的节点不算 —— 那是他自己关的。

### 说明

- 判定逻辑是纯函数，和写库分开：`proxyNodeBindingTruth`（相符 / 不符 / 判不准）和 `planProxyNodeBinding`（不动 / 认一次 / 改绑 / 解绑），共 12 条单测。另有一组真库用例走服务端同一套决策与写库动作：改到另一个节点要改绑、改到无关机器要解绑、再改回来要自己认回去、串两跳一个字不动、绕过保存路径改坏时订阅照发但必须告警且不误报串联、没人用的节点要报而开了直连和有转发指向的不报、停用的不报。五处守卫逐条做过反向验证。

### 版本

- 面板与 APK Release `2.3.353`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.352] - 2026-09-13

### 新增

- **兜底的「有 N 项数据没读到」横幅**。上一版给要紧的列表各自接了失败态，但一页上挂着十几个查询，不可能每个都单独接一遍 —— 而面板重启或者网络抖动时它们会一起失败。现在页面顶部会说清楚「这一页显示的可能不全」，带「全部重试」。
  - 它刻意做得不吵：连续失败 4 秒才出现（轮询抖一下不闪）、数据一恢复立刻收起、手动关掉后同一批不再弹、但**又坏了新的一项就还要说**（关一次不等于永久静音）。只数当前页面真的在用的查询，缓存里躺着的旧查询不算。
  - 登录失效那一类不走这里：那种情况本来就直接跳登录页，再提示一遍是多余的。

### 修复

- 空状态说实话这件事铺完了余下的要紧页面：
  - **管理端收款**：支付订单（手机卡片和表格两处）、账单流水、余额流水 —— 读不到时说「暂无」，等于告诉店主「没人付过钱」。
  - **套餐管理**：卡片和表格两处的「还没有套餐」。读失败时照着它去重建，会建出一套重复的套餐。
  - **用户管理**：「暂无其他用户」。一屏的租户不会一起消失。
  - **链路与转发组**：「暂无隧道」「暂无端口转发 / 转发组 / 转发链 / 入口组」。这类假空最贵 —— 照着它重建会和还在跑的那些撞端口。

### 说明

- 横幅的显示时机是纯函数 `client/src/lib/queryFailureBanner.ts`，单独测：没有失败不提示、刚失败不提示、持续 4 秒才提示、关掉后同一批不再弹、失败变多要再弹、计数出偏差也不会写出「有 0 项」。

### 版本

- 面板与 APK Release `2.3.352`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.351] - 2026-09-13

### 修复

- **读取失败被画成了「你没有」**。前端普遍是「查询给个空数组默认值 + 只判 isLoading」，于是一次请求失败（面板重启、会话过期、网络抖一下）之后，页面照常渲染空状态：钱包说「暂无支付流水」，商店说「管理员还没上架」，我的套餐说「当前账户还没有套餐记录」，转发页说「暂无转发规则」。这些都是**结论**，而失败的时候面板并不知道结论成不成立 —— 刚付过钱的人会以为订单丢了，看到「暂无转发规则」的人第一反应是重建。
  - 加了一对和 `DataSectionLoading` 成对的失败态：`DataSectionError` 与表格里的 `DataTableErrorRow`，一句「没读到 X」+ 认得出来的原因（登录过期 / 没权限 / 没连上）+ 报错原文一行 + 重试按钮。
  - 已接上：钱包（余额、账单流水、支付流水）、商店（商店状态、套餐、按量计费资源）、我的套餐、订阅管理（订阅链接、节点列表、订阅内容）、转发规则列表、主机列表。
  - 余额这一处单独说一句：读不到时**不再显示 ¥0.00**。这一页每个数字都是钱，把「没取到」画成「你没有钱」，看到的人下一步就是再充一次。
  - 商店状态读不到时也不再替商店宣布「暂未开启」—— 那句话会让客户以为是管理员关的，然后去找一个什么都没做的人。

### 说明

- 失败原因的措辞抽成 `client/src/lib/queryErrorMessage.ts` 单独测：原文压成一行并截短、没有原文就不显示那一行、认得出的失败给人话、**认不出的不猜**（不编一个听起来很懂的解释）。

### 版本

- 面板与 APK Release `2.3.351`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.350] - 2026-09-13

### 修复

- **别的机器能把流量记到你头上**。落地入站的流量上报走的是 Agent token，认的是**机器**；而入站号是连号的，猜得到。面板收到上报后只查了「这个入站是谁的」，没查「它是不是开在这台机器上」—— 于是任何一台装了 Agent 的机器（包括租户自助加的那台）都能报别人机器上的入站号，字节数记到那个租户的配额上。配额一记满，面板会自动停掉他名下**所有**转发。转发那条路早就按 `accountingHostIds` 比过一次了，落地这条是漏的，现在补上，非本机的上报按「不是这台机器的流量」丢弃并记一条日志。
  - 顺带补上停用校验：停用的入站不该还在跑，还在报说明是停用前的存量，同样不计费（转发那条路本来就跳过停用规则）。
- **一次坏上报能把账户废掉**。Agent 报的是两次采样的差值，机器重启、计数器回绕都可能让它变成负数或者天文数字。入库那一层早就洗过（负数归零、上限截到安全整数），可**计费和配额**这一路用的是没洗过的原始值：同一次上报，历史明细里记 9 PB，用户的已用流量却写成 1e21 —— 两个数对不上，而且这个租户的配额从此永远是超的。洗值的规矩抽成 `shared/trafficCounterBytes.ts`，上报进来先洗一次，明细、配额、计费、日志共用同一个数。

### 说明

- 新增 1 组落地流量归属的真库用例（本机照常计费、别的机器一个字节都不算、停用不算、无主不算、负数不倒扣、天文数字和明细截成同一个数）和 4 条洗值用例。逐条做过反向验证：拿掉本机校验、拿掉停用校验、不洗字节数，对应的断言分别变红。

### 版本

- 面板与 APK Release `2.3.350`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.349] - 2026-09-13

### 新增

- **反过来那一半：新建落地节点时，把已经指向它的转发一并接进订阅**。先有转发、后加节点是很常见的顺序 —— 机器先跑起来，过几天才想起「这条其实可以进订阅」。只有正向自动绑定的话，这些早就存在的转发永远不会自己进来，要一条条去预览弹窗里手动绑。这个方向比正向更安全：节点是**刚建的**，在它存在之前谁也没机会手动解绑，所以不存在「他解绑了、面板又给他绑回去」。接上了几条会直接告诉你。

### 修复

- **主机的流量告警和续费提醒，没用 Telegram 的商家一条都收不到**。这两件事原来只走 TG，而界面上那两个开关又被**绑死在「TG 机器人已配置」**上：没配 TG 的人根本打不开。机房流量跑超、机器到期停机 —— 他名下所有转发和落地节点会一起断，却没有任何人告诉他。
  - 邮件那一路补上了这两类提醒；开关不再要求 TG，它只表示「这台机器要不要提醒」，走哪个渠道看你配了什么，说明文字也按实际配置说话。
  - 「该不该提醒、还剩多少」抽成 `shared/hostReminder.ts` 由两个渠道共用 —— 各写一份迟早会算出两个结论。日标记前缀分开，两边各发一次，不会互相顶掉。

### 变化

- 订阅管理的文字收了一遍：
  - 标题下面补一句「你有哪些线路，以及怎么把它们带进客户端」—— 一句话说清这一页是什么。
  - 没订阅权限的空状态原来只说「没有权限，联系管理员」，等于把人挡在门外还不说门后是什么；现在说清开通之后能拿到什么。
  - 预览里那两段原来叫「还没加入订阅的转发」和「未进入订阅的转发」，两个名字几乎一样、说的却是两件事。改成**「差一个落地节点」**（选一个就能进来）和**「这些进不了订阅」**（进不来，附原因），「已隐藏」改成「被你关掉的」。
  - 修掉一处上一版 IA 合并后遗留的假话：空状态还写着「在下面把转发加进来」，而预览早就收成弹窗了。

### 说明

- 新增 2 条反向匹配用例、6 条主机提醒判定用例（到阈值才提醒、没填总量不提醒、阈值离谱退回默认、过了停机日不再念、没填日期没这回事）。

### 版本

- 面板与 APK Release `2.3.349`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.348] - 2026-09-13

### 新增

- **建转发时自动认出「这条通往我自己的落地节点」，直接加进订阅**。订阅和转发本来就是同一件事的两面：一条转发把入口机的端口接到落地机上，订阅里那条中转描述的就是这件事。可原来这两件事要分两处做 —— 转发页建规则，再去订阅页的预览弹窗里绑。**不绑就不进订阅，而转发页上完全看不出少了这一步**：转发跑得好好的，客户端里却没有这条线路。
  - 现在目标地址端口正好等于自己（或别人分享给自己）某个启用中的落地节点时，保存即入订阅。
  - 认的规矩是**宁可不认**：地址端口要完全相同、停用的不认、**认出多个一个都不认**（猜错一次就是订阅里指向了错的落地机）、自己的节点优先于分享来的。
  - 只在**新建**、以及**改了目标之后仍然没绑**时认。手动解绑过的人不会顺手改目标，所以不会被面板又绑回去；已经绑着别的节点的更不动，那是他明确选过的。
  - **自动做了就说出来**：保存后直接告诉你「已顺手加进订阅：HK 落地」，并说明不想要就去预览订阅里关掉。面板替人多做一步却不吭声，下次他在客户端里看到一条没印象的线路，只会以为是别处出了错。

### 变化

- **转发列表的行上现在标得出「订阅」**。一条转发被订阅引用着，原来在转发页看不出来 —— 改目标、停用、删掉都会让别人客户端里那条线路跟着变，而操作的人毫不知情。绑了但设成不进订阅的标「订阅已隐藏」；没绑的什么都不显示（大多数转发跟订阅无关，每行挂个「未加入」只是噪音）。鼠标停上去会说清楚改动会影响什么。

### 说明

- 7 条纯逻辑用例（认得出、大小写与 IPv6 方括号、停用不认、多个不认、自己的优先、非法输入、什么时机才认）+ 6 条真 sqlite 用例（别人的节点不会被认成我的、分享来的算数、同地址两个一个都不认……）。

### 版本

- 面板与 APK Release `2.3.348`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.347] - 2026-09-13

### 修复

- **提醒用的「一天只做一次」标记写进设置表之后从来没人删，面板会越用越慢**。到期提醒、流量提醒、主机流量告警、主机续费提醒、余额自动续费 —— 六类去重标记每次都往 `system_settings` 里写一行 `<前缀>:<...>:<日期>`。一个五百人的面板跑一年能攒下十万行，而 `getAllSettings()` 是**整表读**、几十处在调：这些垃圾每次缓存过期都要重新加载一遍，慢得毫无线索可查。
  - 每小时那次清理里顺手删掉 7 天前的日标记（去重窗口只有一天，留 7 天纯属保险：跨时区、面板停机几天再回来）。
  - 日标记不再混进 `getAllSettings()` —— 它们只会被 `getSetting(精确键)` 读，去重逻辑照旧。
  - 清理最怕误删：**日期解析不出来的键一律不动**，宁可留着垃圾也不能删掉一条真设置；名字里带日期但不是这几个前缀的键也不碰。

### 说明

- 4 条新用例（过期的清掉今天的留着、真设置一根汗毛不动、解析不出日期就不猜、日标记不进 getAllSettings 但精确键仍读得到）。两条反向验证按预期变红。
- 这一轮还系统扫了一遍权限边界：把所有 `protectedProcedure` 里「拿 input 的 id 直接查」的过了一遍，30 个初筛全部落到 `assertOwned* / requireHostAccess / visibleXQueryScope / actor.role` 这些校验上，没有发现越权读写。

### 版本

- 面板与 APK Release `2.3.347`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.346] - 2026-09-13

### 修复

- **删掉主机之后，那台机器上的落地节点全都留着**。入站行指向一台已经不存在的主机（「新建节点」那一段还会把它列出来，地址解析不出来），派生节点继续待在订阅里 —— 租户的客户端里多一条永远连不上的线路，分享记录也还在，而管理端看上去一切正常。删主机时本来就会清掉转发规则、隧道出口、权限、计费配置这些，唯独漏了落地节点这一路。
  - 走 `deleteProxyInbound` 而不是直接删行：它会顺带删掉派生节点，而删节点那一步又会解绑引用它的转发规则、清掉分享记录 —— 三件事漏一件都会留下一处对不上。

### 说明

- 1 条新用例（删主机后入站 / 派生节点 / 分享 / 租户订阅四处都归零），反向验证短接后立刻变红。
- 这一轮另外核过、确认没问题的：sing-box 配置下发是「每次心跳整份重算 + 按内容签名去重」，所以推送时 Agent 不在线不要紧，下次心跳自愈；改凭据、分享、取消分享都会改变配置内容，因而签名必变、必重下发 —— 这条链上没有「面板说生效了但机器上没有」的缝。转发/端口那一块本来就有一整排纯逻辑用例（端口策略、端口预留、规则配额、资源授权、隧道出口与 mimic 端口分配），覆盖是密的。

### 版本

- 面板与 APK Release `2.3.346`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.345] - 2026-09-13

### 修复

- **落地节点占的端口，转发规则那边完全不算数**。同一台机器上已经有 sing-box 在听 443，用户再建一条源端口 443 的转发 —— 面板一路放行，到了机器上后起的那个 bind 失败，而界面上两边都显示正常，要人上机器看日志才知道为什么。
  - 转发规则和落地入站是两个进程，各自 bind 各自的端口，谁也不知道对方存在；**面板是唯一同时知道两边的人**，它不拦就没人拦。
  - 反过来那半边本来就是对的（给落地节点挑端口时会躲开转发规则和隧道端口），所以这是个「A 躲 B、B 不躲 A」的单向漏洞。现在两个方向都躲，并且各有用例钉着。
  - 不按协议放行：入站那边一个端口 TCP/UDP 都可能用（Hysteria2、TUIC 是 UDP，VLESS 是 TCP，XHTTP 两个都要），分不清就一律当占用 —— 少给一个端口是小事，撞上了要人上机器排查是大事。
  - 停用的入站不挡路：它没在听，挡着反而让人以为端口被莫名占了，而界面上那条入站明明是灰的。

### 说明

- 5 条新用例（算作占用、不按协议放行、停用的不挡、只挡自己那台机器、反方向也躲）。反向验证：短接掉这一层，5 条里 3 条立刻红。

### 版本

- 面板与 APK Release `2.3.345`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.344] - 2026-09-13

### 修复

- **套餐里填的「自建落地节点数」和「订阅地址条数」两个上限完全不生效 —— 等于不限**。这两列既没被算权益时那条查询查出来，也没冻进套餐快照，于是配额合并那边拿到的是 `undefined`，而在那套规矩里 **0 就是「不限」**。后果很直接：卖「2 个自建节点」的套餐，租户能开无限个；卖「1 条订阅地址」的，他能签发到手软。其余配额（端口数、规则数、连接数、单 IP 数）本来就是对的，只有这两项漏了。
  - 修了三处才算齐：算权益的查询要查出这两列、套餐快照要冻住它们、快照解析在**老快照里没有这两个键**时要退回套餐当前值而不是默认成 0。第三条是给你面板上现有订阅用的 —— 它们的快照全是升级前的样子。
- **订阅地址在账号到期或被停用之后还能拉到节点**。这条路只看了「有没有订阅权限」，不看账号停没停用、到没到期 —— 而落地机那边早就按同一个人的资格把凭据拿掉了。于是到期的租户拉到一份列着全部节点的订阅（页头还老老实实写着他已经过期），连上去一个都不通，界面上看不出为什么。现在订阅地址和落地机用**同一把尺子**（`proxyCredentialRecipientActive`）：停用、到期、没权限一律 404，与令牌无效同样处理，不泄露「这个令牌存在但没资格」。

### 说明

- 5 条新用例：两个上限真的落到用户身上、冻结快照的订阅冻住这两个上限、老快照退回套餐当前值、账号到期/停用后订阅地址立刻 404（续期与恢复之后要自己回来）。三条反向验证全部按预期变红。
- 这一轮另外验过没问题的：按量计费按整 GB 扣（零头不重复扣）、余额扣穿之后由上报路径停服、套餐到期后各项配额一起收回并停服、端口段按 portCount 分配。

### 版本

- 面板与 APK Release `2.3.344`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.343] - 2026-09-13

### 修复

- **删账号时收凭据这件事，焊进了 `deleteUser` 本身**。原来只有「用户管理」那条删人路由记得先收：凭据是按 `sharedUserId` 找的，人删了就再也找不着 —— 那些凭据会永远留在各个端口上，界面上没有任何入口能收回，而它照样能连。目前只有那一条路径，所以还没出事，但这是个雷：任何一条别的删人路径（自助注销、批量清理）漏掉这一步都会留下活凭据。现在收凭据、收专属端口都在函数里，漏不掉。
- **后台手动分配套餐能按档给了**。上一版给套餐加了多周期之后，这里漏了：分配对话框只认「月付套餐可以改周期」这一条老规则，于是卖月付 / 年付的套餐，管理员想手动给一个年付都给不了，界面上还写着「如需其他周期，请先编辑套餐」。现在这个套餐挂着的档位都能选（外加「永久」），选套餐时默认落在它自己的默认档 —— 原来写死「一个月」，一个只卖年付的套餐会默认分配成 30 天，而下拉里根本没有这一档。

### 说明

这一轮是拿真数据库把「面板承诺了什么」逐条跑了一遍，下面这些**验过没问题**，记在这里免得下次重复查：

- 买套餐自动发凭据、到期清扫收凭据、超流量停凭据、落地机配置里同步增减 —— 四条都对得上。
- 独享端口模式：买了开端口、续期不会重复开、到期收端口、套餐改绑节点后收端口、专属端口不占自建配额。
- 折扣码打在**选中那一档**的价格上（年付 9600 打九折扣 8640，不是按月付价算）。
- 兑换码按自己写的天数发，不被套餐档位改写。
- 同一个节点分给两个人各发一份凭据、取消其中一个不影响另一个、删入站时分享行和派生节点一起清干净。

新增 2 条用例（按档手动分配、兑换码不受档位影响）。

### 版本

- 面板与 APK Release `2.3.343`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.342] - 2026-09-13

### 修复

- **分享节点时，没生效的那些现在会当场说出来**。原来挑到不能分享的节点（最常见的是**挑到了他自己的节点**）是**静默丢弃**：界面照样提示「已保存」，而库里一条都没写进去 —— 管理员只能等租户来说「我这儿没有」才发现。现在保存后会明确告诉你有几个没生效、为什么（他自己的节点 / 节点已不存在 / 发不出凭据）。
  - 自己的节点确实不该分享：它本来就在他订阅里，再分享一遍客户端里会出现两条一模一样的线路。问题从来不是这条规矩，而是**执行了却不说**。
- **换入站归属之后，新主人的订阅里会出现两条同一个端口的节点**。把一个端口先分给某个租户、之后又把这个端口整个转给他，那份当初发给他的共享凭据没有被收回：一条是端口本身的（现在归他了），一条是当租户时分到的那份 —— 客户端里就是两条只有凭据不同的线路，界面上看不出哪条该留。现在换归属时会连同那份凭据和分享记录一起收掉。
- **分享给没有客户端订阅权限的用户时会警告**。分享本身写得进去，但那个人打不开订阅管理、也拿不到订阅地址，等于没分享 —— 而管理员从这个弹窗上完全看不出来。

### 变化

- 分享说明里补了一句**租户在哪儿看**：「订阅管理 → 我的节点」，标着「管理员分享」；「我的套餐」那一页只管套餐、不列节点。找不到东西在哪儿，是这个功能最容易被当成「没生效」的地方。

### 说明

- 4 条新用例跑在真 sqlite 上：挑到自己的节点要报上来、节点已删要报上来、全部生效时不能报假警、换归属后同一个端口只剩一条且分享行清干净。反向验证过两条：短接「换归属收凭据」→ 1 条红；短接「报告没生效」→ 1 条红。

### 版本

- 面板与 APK Release `2.3.342`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.341] - 2026-09-13

### 新增

- **一个套餐可以挂多个购买周期，客户自己选月付 / 季付 / 年付**。原来一个套餐只有一个周期，要卖三种周期就得建三个套餐 —— 商店里同一个产品占三张卡，客户得自己比对哪几张其实是一回事；改一次配置要改三遍，改漏一个就是三张卡说的话不一致；年付便宜多少还得客户拿计算器按。
  - 管理端「套餐设置」里多了一张**购买周期表**（周期 + 价格，最多 6 档，「加一档」自动挑下一个没被占用的常用周期）。留空 = 只卖原来那一档。
  - 填了之后上面的「价格 / 有效期」变成只读，由**总价最低那一档**回填 —— 两处各填一遍再互相打架，是这种表最容易出的错。
  - 商店卡片上出现周期切换，长周期自动标出**省了百分之几**（按每天单价算，基准是最短那一档 —— 客户心里的参照物是「按月买要多少钱」），并把总价换算成「约 ¥X / 月」。
  - **默认选总价最低那一档**，不是每天最划算的那一档。后者通常是年付，一进商店就默认选中金额最大的那个，点错一下就是一年的钱 —— 这种默认值不该由面板替客户做。
  - 周期白名单同时放开了：原来服务端硬卡 30/90/180/365/730 五档，现在 1–3650 天都收，7 天体验、14 天促销不用再绕路。

### 修复

- **续费按当初买的那一档，不再按套餐默认档**。这是多周期带出来的必修项：买年付的人点「续费」，原来会按默认档（通常是月付）扣钱和续期 —— 而弹窗上显示的还是年付的价。现在订阅上记着买的档（`user_subscriptions.durationDays`），续费金额和天数都由服务端按它算好（`renewPriceCents` / `renewDurationDays`），前端只管显示，不重算。
- **自动续费同理**：按月付的人不会被扣成年付，按年付的人也不会被悄悄降成月付。那一档被管理员下架了才退回默认档，并写一行日志 —— 否则「为什么这次扣的钱不一样」查无对证。
- **认不出的周期一律拒掉**，不拿默认档顶上。下单和收款之间隔着一次跳转，管理员完全可能在这中间改了套餐；顶上默认档就成了「我买的是年付，开出来是月付」。
- **网关支付记住买的是哪一档**（`payment_orders.planDurationDays`）：不存下来的话，回调回来只知道「买了这个套餐」，开出来的必然是默认档。老订单这一列是 null，走默认档，与改动前一致。

### 说明

- 存量套餐**一行都不用迁移**：没配周期表的套餐退回它自己那一档，界面和行为跟以前一模一样。有专门的用例盯着这条 —— 这次改动不能把所有老套餐变成买不了的卡片。
- 11 条纯逻辑用例（`shared/planPricing.ts`：折扣怎么算、默认选哪档、认不出的档位怎么办）+ 11 条跑在真 sqlite 上的用例（扣的钱和开的天数对不对得上、续费档位、余额不足、删套餐清理）。反向验证过两条要命的路径：短接「按选中档扣钱」→ 2 条红；短接「续上次那一档」→ 2 条红。
- 周期名称和常用档位收敛到 `shared/planPricing.ts` 一份，商店和管理端不再各写一遍。

### 版本

- 面板与 APK Release `2.3.341`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.340] - 2026-09-13

### 新增

- **自建节点可以写备注了**。「名称」是客户端里显示的节点名、会随订阅发出去，所以它写不了「给张三的」「测试用」这种只该自己看的话 —— 而一台机器上开五个端口分给不同人时，最需要的恰恰是这一句。粘贴进来的节点早就有备注框，自建这边一直没有。
  - 库表和接口**本来就支持**（`proxy_inbounds.remark`、`proxyInbounds.create/update` 都认这个字段），缺的只是表单上那个框 —— 等于放着一个填不进去的字段。
  - 备注跟着派生节点走：不带过去的话，入站上填了备注、订阅内容里那一条却还标着「直连」，填了等于没填。派生节点在界面上没有编辑入口（「编辑」是按「换一条节点链接」设计的，自建节点没有链接），所以这一列只可能来自入站，覆盖是安全的。
  - 节点行上备注排在地址前面：一排端口里，认出「哪个是给谁的」比看地址更常用。
  - 名称框下面补了一句说明「客户端里显示的节点名，会随订阅发出去」，备注框下面是「只给你自己看，不进订阅」—— 两个框长得一样，不说清楚就会有人把备注写进名称，然后客户端里冒出「给张三的」。

### 说明

- 4 条断言跑在真 sqlite 上：填了要落到派生节点、改了要跟着改、清空要真的清掉（不能留上一次的字样）、名称不受影响。反向验证过：短接掉备注传递那一行，这条用例立刻失败。

### 版本

- 面板与 APK Release `2.3.340`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.339] - 2026-09-12

### 修复

- **发布说明现在覆盖「自上一次发布以来」的全部版本，不再只写最后那一节**。发这一版时才发现的真事：上一个 GitHub Release 是 `v2.3.320`，而 main 已经到 `2.3.338` —— 中间十八个版本（自助加机器、独立凭据、到期真断服、套餐发节点、按人计量、到期提醒、余额自动续费、支付主动查单……）在发布说明里**一句话都不会出现**，因为 `extract-release-notes.mjs` 只取 CHANGELOG 里当前版本那一节。看 Release 页面的人会以为这一版只改了它最后碰的那一处。
  - 连着合几个 PR 才发一次版是常态，所以这不是一次偶发，而是每次都会再犯一遍。
  - 现在发布流程先查出**上一个正式 Release**（跳过草稿、预发布，也跳过重跑时自己刚建好的那一个），把它传给脚本；脚本取这中间的每一版，各带版本号小标题，最前面一行写清范围。
  - 取不到上一个 Release、或者它比这次还新（回滚、重跑旧 tag）时退回只取一节，并且**正常退出** —— 发布说明不完整是小事，为它挡住一次发布是大事。
  - 「忘了写 CHANGELOG」仍然是硬失败：那一道闸留着。

### 说明

- 8 条用例跑在一份固定的 CHANGELOG 上（`FORWARDX_CHANGELOG_PATH` 只为可测而存在），否则期望值会跟着仓库真实版本号一起漂，每发一版都得改一次。反向验证过：短接掉多节那一支，8 条里 2 条立刻失败。

### 版本

- 面板与 APK Release `2.3.339`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.338] - 2026-09-12

### 变化

- **「订阅管理」五张卡合成两张，三类节点合成一个列表**。这一页原来是：订阅链接、我的机器、新建节点（自建）、落地节点（粘贴的）、订阅内容 —— 五张平级的卡，从上滚到下才摸得清自己有什么。可是「自建的」「粘进来的」「别人分享来的」是我们的实现细节，租户脑子里只有一件事：**我有哪些线路**。按来源切三张卡，等于把实现漏到了界面上。现在：
  - 上面一张 **「订阅链接」**：要复制走的东西。
  - 下面一张 **「我的节点」**：三类节点在同一个列表里，行上标出来源（自建 / 粘贴 / 管理员分享），分组（按协议 / 自动）作用在整张合并列表上。
  - **「我的机器」**降级成这张卡上的一个按钮 —— 它是自建节点的前置条件，不是跟节点平级的一段；**「预览订阅」**（原「订阅内容」）也是一个按钮，它本来就是「客户端会看到什么」的预览，看一眼就走，不该常驻。
- **自建节点行现在也有在线状态点**。合并之后一半行有绿点一半没有，看起来像自建节点永远查不出状态。探测结果挂在派生节点上：一个端口可以有多份凭据、派生多个节点，任一可达就算这个端口活着（在线 > 离线 > 未知）。
- **卡头的「N 在线 / N 离线」现在数的是整张列表**，不再只数粘贴那一半 —— 原来会出现卡头写着「12 个 · 2 在线」，而另外十行自建的明明都亮着绿点。
- **「还没加入订阅的转发」这个提醒挪到了「预览订阅」按钮的角标上**。它原来长在「订阅内容」卡里，收进弹窗就等于藏掉了：转发建好了却没进订阅，用户只会以为订阅坏了。

### 说明

- 三类行的开关和动作写的是**不同字段**（自建写 `includeDirect`，粘贴的写 `isEnabled`），所以合并的做法是：**每类由各自的 owner 组装成 `ProxyNodeRowSpec` 再传进列表**，列表只管分组和排版，不按类型分支。直接在列表里 `if (type === ...)` 的写法迟早会把某一类的开关接到另一类的字段上，那种 bug 从界面上看不出来 —— 点一下没反应，或者更糟，关掉了别的东西。
- 纯前端改动，接口、库表、订阅内容一律没动；客户端拉到的订阅和改之前逐字节一致。

### 版本

- 面板与 APK Release `2.3.338`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.337] - 2026-09-12

### 新增

- **支付主动查单：把「钱付了但回调没到」的订单自己捞回来**。整条收款链路原本只靠网关回调 —— 回调丢一次（网关那边网络抖、面板正好在重启、反代把那个 POST 拦了），订单就一直挂在 pending 直到过期，而客户那边钱已经扣了。这是收款系统最常见的一类事故（俗称掉单）。现在面板每轮维护会主动问网关一句「这单付了没」：
  - 已支持**易支付**（`api.php?act=order`）与 **Stripe**（Checkout Session 查询）。gmpay 等暂时没有可用的查单接口，宁可不查，也不拿一个猜出来的「已付」去发货。
  - 查到已付走的是**回调那条既有路径**，那条路本来就是幂等的（processing / completed 都有判断）—— 回调随后又到了也不会发两次货，有用例专门盯这个。
  - **主动查单排在关闭过期订单之前**。顺序反了的话，一笔「付了但回调没到」的订单会先被判过期关掉：钱收了、服务没发，而系统里看起来一切正常。
  - 三条自我约束：太新的不问（用户可能还停在收银台）、过期的不问（那时该关单）、每轮最多问 50 单。网关挂了就安静跳过，不动订单状态 —— 一次网络抖动不该改变任何一单的结局。

### 说明

- 判断「付没付」的解析单独拆成 `shared/paymentReconcile.ts` 测：判错的后果是两种事故 —— 认成已付是白送服务，认成未付是收了钱不发货。用例几乎全在写「什么情况下**不能**算已付」，比如易支付的 `code=1` 只是「查询成功」而不是「付款成功」（混淆的话每查一次就白送一次），Stripe 的 `status=complete` 也可能配 `payment_status=unpaid`（异步付款方式）。
- 另有 6 条跑在真数据库上的用例，用假 fetch 顶掉网关那一半，验的是入账、幂等、查无此单就地关单、网关挂了不动状态。反向验证过：短接掉「查到已付就入账」，那条立刻失败。

### 版本

- 面板与 APK Release `2.3.337`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.336] - 2026-09-12

### 新增

- **余额自动续费**。「到期 → 断服 → 客户发现 → 手工去付 → 等回调」这一串每一步都在掉人，而余额本来就躺在账上。在「我的套餐」里每条订阅旁边多一个「自动续费」开关，开了之后到期前一天用余额自动续一期。
  - 自动续费跑在**到期清扫之前** —— 顺序反了的话，余额够的人会先被断一下服再续回来，中间那几分钟他的客户端全是红的，而他什么也没做错。
  - 三条自我约束，各有用例：**只在到期前一天内扣**（提前十天把钱划走，客户会觉得是乱扣）；**余额不够就安静跳过、照常到期**，绝不扣成负数；**同一天只试一次**（调度器几分钟一轮，不去重会在余额不足时每轮撞一次数据库）。套餐已下架的也不续 —— 下架的东西不该继续卖。
  - 开关放在订阅卡片上而不是设置页：决定要不要自动扣钱的时候，人正看着这条订阅的价格和到期时间。

### 修复

- **主人自己到期 / 被停用 / 超流量之后，他名下的落地端口还在监听**。v2.3.332 只处理了「分享出去的凭据」，而套餐给的专属端口是**归租户自己**的 —— 那条路绕过了检查，等于按人计费的东西停不掉。现在算下发配置时，入站主人也要过同一道资格检查（管理员不受限），下一次心跳端口就消失，恢复账号自动恢复。

### 说明

- 用例 7 条（自动续费）+ 2 条（主人资格）跑在真 sqlite 上。自动续费那组做了两次反向验证：短接掉扣款，「自动扣款续一期」立刻失败；拆掉「只在到期前一天内」那道门槛，「离到期还早的不提前扣」立刻失败 —— 确认防超扣的那几条不是摆设。

### 版本

- 面板与 APK Release `2.3.336`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.335] - 2026-09-12

### 新增

- **按租户计量：套餐可以给每人单开一个端口**。套餐里勾了节点之后多一个开关「给每人单开一个端口」：
  - 开：面板照着那个节点在同一台机器上克隆一个入站，**端口直接归租户名下** —— 现有的按端口计费链路于是自然把流量算到他头上，也能给他单独限速。到期、取消、换套餐、删账号，端口连同节点一起收掉。
  - 关（默认）：维持现状，大家共用原端口、各发一份凭据，省端口，但流量按端口统计、分不开。
  - 克隆出来的端口凭据全部重新生成，REALITY 也另生成一对密钥 —— 私钥漏一处就是所有端口一起漏。端口在 20000-30000 里挑一个没被别的入站、转发规则或隧道出口占用的。
  - 这些端口是面板托管的：在「新建节点」里标成「套餐附带 · 面板托管」，不给编辑和删除（删了下次权益重算又会建回来，中间那段他自己连不上，而界面上看不出是谁干的），也**不占他的自建配额**。

### 说明

- **为什么是「一人一个端口」而不是读 sing-box 的 per-user 统计**：这条路我拿真二进制试过，走不通。官方发布的 sing-box 1.14.0 没有编进 v2ray API（`sing-box check` 直接报 `v2ray api is not included in this build, rebuild with -tags with_v2ray_api`），而 v2ray API 才是那个能给出每用户上下行字节数的接口；Clash API 这边，我起了一个真 sing-box、用另一个 sing-box 当客户端跑通 VLESS、把连接挂住再查 `/connections`，连接元数据里只有入站标签（`vless/in-1`），**没有任何用户字段**，而且连接一断就从列表里消失。所以在共享端口上根本拿不到分人的数字。而这套面板的落地流量本来就是按监听端口数的（Agent 在端口上挂计数链），一人一个端口是目前唯一能分账的做法，且不需要任何新机制。
- 修一个刚写出来就被用例逮住的 bug：克隆端口上那份凭据一开始被标成了「分享给某人」，于是被 v2.3.332 那条「为别人发的凭据不进主人自己的订阅」过滤掉 —— 端口开好了、节点也建了，他的订阅里却是空的。
- 用例 15 条跑在真 sqlite 上，反向验证过：短接掉独享端口这一步，15 条里 4 条立刻失败。

### 版本

- 面板与 APK Release `2.3.335`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.334] - 2026-09-12

### 改进

- **到期提醒：提前几天可配，而且不再依赖绑定**。
  - 原来邮件和 Telegram 两条路各自写死「剩 3 天以内每天发一次」。3 天太晚 —— 按月付的人多半到期当天才发现断了，而续费还要走一遍支付；每天发一次也不对，剩 3、2、1 天各来一封，三封说的是同一件事。现在是「在这几个整天数上各发一次」，默认第 7、3、1 天，系统设置里可改（填 0 表示当天也发）。
  - **面板顶上加了一条到期横幅**。邮件和 Telegram 都要求用户先绑定，没绑的人（多数）在到期前收不到任何消息。这条谁都看得见，旁边就是「去续费」。
  - 三处共用同一份配置与解析（`shared/expiryReminder.ts`）——邮件一套阈值、TG 一套、横幅再一套的话，同一个用户会在不同的日子收到互相矛盾的说法。
  - 脏配置退回默认档位，而不是解析成空数组：空数组等于一声不吭地关掉提醒，而管理员从界面上看不出自己填错了。想关有单独的开关。

### 版本

- 面板与 APK Release `2.3.334`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.333] - 2026-09-12

### 新增

- **套餐可以附带落地节点，买了自动发凭据，到期自动收回**。上架时在套餐里勾好节点，之后整条链路不再有任何一步手工操作：
  - 用户下单 / 管理员分配 → 面板在这些节点上给他**各发一份独立凭据**，节点直接出现在他的订阅里。
  - 到期、取消、超流量被停、管理员收回订阅权限 → 凭据自动收回，他连不上，同一个端口上别人照旧。
  - 套餐改挂别的节点 → 保存并同步一次，旧的收、新的发。
  - 挂钩点选在「重算这个人的权益」那一个函数上 —— 下单、后台分配、改套餐、到期清扫本来都会走到它，所以不必每条路各记一遍，也就不会漏。
- **开通即可用：买了套餐还没有订阅地址的话，自动开一条**。「买完 → 进面板 → 还得自己点一下新建链接 → 才拿得到地址」，中间那一步对用户没有意义。只挂在开通这一刻，不挂在权益重算上 —— 否则用户自己删掉的地址会被一次次加回来，删都删不掉（这条是写用例时发现的，第一版实现就是错的）。
- 商店卡片和套餐列表都显示「附带落地节点 N 个」。买之前看不到，买完才发现订阅里有节点，等于白送了个卖点。

### 修复

- **管理员在用户页看「分享了哪些节点」时，选择框是空的**（v2.3.331 引入）。他实际拿到的是**自己那条**派生节点，而选项列表里放的是代表整个端口的那条 —— 两边对不上，界面显示成一个都没选，管理员随手一保存就把他的凭据静默收走了。现在选择框查的是「该勾哪几条」，并且原样保存一次不会换凭据。

### 说明

- 手工分享和套餐带的分开记（`proxy_node_shares.source`）：套餐同步不会冲掉管理员手工分的，撤套餐也不会带走手工那份；同一个节点两边都给的情况下，撤掉其中一路不会让他断线。
- 节点授权按套餐**当前**配置走，不随「不同步已购买用户」的快照冻结 —— 冻结的是流量、端口那些额度。
- 用例 9 条跑在真 sqlite 上，并做过反向验证：短接掉自动发凭据那一步，9 条里 6 条立刻失败。

### 版本

- 面板与 APK Release `2.3.333`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.332] - 2026-09-12

### 修复

- **到期、停用、收回订阅权限，都拦不住已经发出去的凭据**。面板那边拦住的只是订阅地址 —— 客户端里存下的那份配置照连不误，因为凭据落在落地机的 sing-box 上，没人去删。「到期就停服」这句话对分享出去的凭据一直是假的。现在算配置时会把已经没资格的收件人剔掉（账号停用 / 到期 / 没有订阅权限 / 账号已删），下一次心跳他就连不上；恢复账号就自动恢复。主人自己那份不受影响 —— 端口本来就是他的。
- **删账号会留下一份谁也管不着的活凭据**。原来只删 `proxy_node_shares`，凭据还留在各个端口上；而人已经从用户列表里消失，界面上再没有入口能收回它。现在删人之前先收回他手上所有凭据，并催相关落地机重下发。
- **自助加机器没有上限**。v2.3.330 把 `hosts.create` 摆上了界面（在那之前它虽然也允许普通用户调用，但没有入口）。机器行本身不吃资源，可它会进管理员的主机列表和仪表盘统计，灌几千条就把那些页面淹了。现在默认每人 10 台，管理员可用系统设置 `selfServiceHostLimit` 调整（0 = 不限），额度直接显示在「我的机器」标题上（`2/10 台`），到顶时按钮变灰并说明原因 —— 而不是等人填完表单才弹一句错误。

### 改进

- **落地节点列表不再一行一次查询**。入站列表原来每行单独查一次用户清单，管理员那边是全量入站，五十个端口就是五十次往返 —— 页面越用越慢，而慢在哪里从界面上看不出来。改成一次查完。
- **订阅链接挪到「订阅管理」最上面**。回头客来这一页十次有九次只干一件事：把链接复制走或重新导入，而它原来压在五张卡片的最后，每次都要滚到底。第一次来的人也不吃亏 —— 这时它是空的，空状态里直接写「先在下面加节点，再回来建链接」，等于把顺序讲了一遍。

### 版本

- 面板与 APK Release `2.3.332`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.331] - 2026-09-12

### 新增

- **分享给谁，就给谁单独发一份凭据**（VLESS / VMess / Trojan / Hysteria2 / TUIC / AnyTLS）。原来分享是把主人自己那份凭据抄给对方，取消分享只是删掉一条记录 —— 凭据还在端口上活着，对方把订阅存下来照样能连。**那句「取消分享他就连不上」原本是假的**，现在是真的：
  - 选中某人 → 面板在那个端口上给他单独开一份凭据，派生出一条只属于他的节点进他的订阅。
  - 取消 → 删掉他那份凭据。他连不上，同一个端口上别人照旧。
  - 再点一次保存不会重发凭据（重发等于让对方白重拉一次订阅）。
- 新增 `proxy_inbound_users.sharedUserId` 记录「这份凭据是给谁的」，0 = 管理员手工加的。库表变更由 `ensureDatabaseSchema` 自动补列，无需手工迁移。

### 修复

- **保存入站不会再顺手删掉分享发出去的凭据**。入站弹窗提交的是全量用户清单，而自动发的凭据不在里面 —— 照单全收的话，管理员随手改个名字保存一下，租户就集体连不上，界面上什么都看不出来。现在这类凭据在清单里缺席时保留，并且收件人不能被表单改写。
- **为别人发的凭据不进主人自己的订阅**。它归属确实是主人，但只为某个租户而存在；留着的话主人客户端里每多一个租户就多一条只有凭据不同的重复线路。
- 派生节点与凭据的对应关系改成显式返回（`derivedNodes` 带 `inboundUserId`）。原来界面靠「id 数组的下标」跟用户清单对齐，而那个查询根本没保证顺序 —— 凭据一增一删就会对错人，**而对错的后果是把 A 的链接当成 B 的发出去**。

### 改进

- 分享弹窗按协议说实话：多凭据协议写「每人一份独立凭据，取消只吊销他那一份」；Shadowsocks / Snell 写「这个端口只有一份凭据，分给几个人就是几个人共用，想收回只能换掉它，届时所有人都要重拉订阅」。
- 多凭据协议下，节点这边的分享改成按**端口**选人（原来是每份凭据各选各的人 —— 那是把「谁拿到了哪一份」交给管理员手工维护，现在这件事由分享自己管）。
- 入站弹窗里，分享自动发的凭据显示为只读并标「分享发出」：在那里删掉它并不会取消分享，只会让对方莫名其妙连不上。
- 已经属于某人的凭据不再出现在「可分享节点」清单里 —— 再分给第二个人等于两人共用一份，取消一个会把另一个也断掉。要给第二个人，挑原来那条节点即可。

### 说明

- **Shadowsocks / Snell 维持原样**：一个端口只有一份 PSK。硬开多用户会让那个入站**已经发出去的所有配置立刻失效**（实测 sing-box 1.14.0：`users` 数组一存在，顶层 `password` 两种加密族都会被拒），那需要你点头，不该由分享偷偷做。
- 分享/取消都会立刻催那台落地机重下发，不用等下一次心跳。
- 用例 12 条，跑在真 sqlite 上。做过一次反向验证：把「发独立凭据」这一步短接掉，12 条里 9 条立刻失败 —— 确认它们咬的是这件事本身，不是摆设。

### 版本

- 面板与 APK Release `2.3.331`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.330] - 2026-09-12

### 新增

- **租户可以自己加机器了**：订阅管理里多了一段「我的机器」——加一台、拿 Agent 安装命令、看在不在线、删掉，四件事，就这些。
  - 没开放整页「主机管理」是故意的：那页三千多行、近二十个接口，分组 / DDNS / 批量升级 / 流量校准好几个都是 `adminProcedure`，整页放开只会让租户到处撞权限错。
  - 后端本来就支持：`hosts.create` 是 `protectedProcedure`，建出来的主机 `userId` 就是他自己，管理员那些字段（流量告警、DDNS、封禁开关）对非管理员一律落成默认值。**缺的只是一个入口，和一条他拿得到的安装命令**。
  - 能开几个节点仍由 `maxProxyInbounds` 管着。机器行本身不消耗任何东西 —— Agent 没连上的行就是条死记录。
- 新增 `hosts.agentInstallCommand`：只有主人和管理员能取。**被授权用某台主机不等于能拿它的令牌** —— 命令里带着 `agentToken`，谁拿到谁就能把一台机器接进面板顶着这个身份上报，所以这条判定跟「能不能在这台机器上开节点」是分开的两件事，各自有用例。

### 改进

- Agent 安装命令的拼装抽到 `shared/agentInstallCommand.ts`，前端「主机管理」和服务端这条新接口共用一份。原来只有前端会拼，而拼它要读 `panelPublicUrl`、GitHub 加速、`agentPreferPanelInstall` 三个管理员才能读的设置 —— 租户拿不到，只能服务端拼；两边各抄一份的话，改了一边忘了另一边，要等有人真去装机器才会发现。
- 顺手补上 token 的引号。现在的 token 是 `nanoid(32)`，只有字母数字和 `_-`，引不引都一样；但这条命令现在由服务端拼给租户，token 一旦换了生成方式，没引号的那份会从外层单引号里跑出去，变成粘进 SSH 就执行的另一条命令。
- 「新建节点」没有可用主机时的提示跟着改：租户现在看到的是「可以在上面的『我的机器』里加一台自己的，也可以让管理员授权一台」—— v2.3.325 那句「请联系管理员给你授权一台」这一版起就不完整了。

### 说明

- 面板没配公开地址时，取命令会明说「装上的 Agent 不知道该回连到哪里，请联系管理员在系统设置里配好」，而不是给一条装不上的命令。
- 命令的引号是用真的 bash 验的：把开头的 `bash -c` 换成 `printf %s` 跑一遍，看它实际会执行的那条管线里，带引号的 token 有没有原样落进去。

### 版本

- 面板与 APK Release `2.3.330`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.329] - 2026-09-12

### 改进

- **「新建节点」里一个「用户」有两个意思**。同一个弹窗上边是「归属用户」（面板账号，决定节点进谁的订阅、流量扣谁的套餐），下边是「用户（2）／加一个」（sing-box 入站上的凭据槽，跟面板账号毫无关系）。同一个词指两样东西，看不懂是应该的。
- 下边那一段改叫**「凭据」**，按钮改成「再发一份」，输入框的提示改成「给谁用，例如 小王 / 备用机」，列表上的角标从「2 个用户」改成「2 份凭据」。
- 说明也写全了：同一个端口、同一份配置，只是凭据不同；一份凭据在订阅里是一条单独的节点，名字是「入站名 · 标签」；删掉一份只有拿那份的人连不上；**标签只是给自己认人用的，跟「归属用户」和面板账号没有绑定**；**流量按端口统计，分不到每一份头上**。
- 单用户协议（Shadowsocks / Snell）那句「只支持单用户」也说清后果：这个端口只能发一份凭据，谁拿到都一样，想一人一份、能单独吊销就换 VLESS / VMess / Trojan / Hysteria2 / TUIC / AnyTLS。

### 版本

- 面板与 APK Release `2.3.329`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.328] - 2026-09-12

### 改进

- **「我的套餐」空着的时候，得有地方去**。原来只在商店开着时才在空状态卡片里给一个「去商店购买」按钮，别的情况就是一句「当前账户还没有套餐记录。」然后没有下一步了。
  - 商店开着：页头常驻一个「去商店」按钮（不用等到一个套餐都没有才出现，想加购也走这里），空状态的按钮改叫「去商店下单」。
  - 商店关着：空状态不再只说「还没有套餐记录」，补上「商店暂未开放，请联系管理员为你分配套餐」——不放假链接，但告诉他找谁。
- 商店页里「暂无可购买套餐」原来的说明是「暂无可用套餐。」，等于把标题重说一遍。改成「管理员还没有把套餐放上商店，需要的话可以联系他分配。」

### 版本

- 面板与 APK Release `2.3.328`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.327] - 2026-09-12

### 修复

- **套餐上架了，租户面板里却没有商店**。这不是 bug，是面板把三个名字相近的开关摆在一起、又不肯说实话：全局「商店状态」、套餐「状态」、套餐「购买入口」。总开关关着的时候，套餐里那句「开启后普通用户可在商店自助购买」是假的 —— 开关照开，用户那边照样看不到。
- 现在这三处都说实话：
  - 总开关关着且已有套餐开了购买入口，套餐管理页顶上直接横一条提示，写清楚有几个套餐受影响，旁边一个「开启商店」按钮，一下点开，不用自己去找。
  - 「商店状态」卡片关着时不再写「开启后用户可自助购买」，改成告诉你现在拦下了几个套餐。
  - 套餐弹窗里的「购买入口」，总开关关着时说明文字换成「商店总开关是关的，这里开了用户也看不到」。

### 说明

- 服务端那条链子查过了，是好的：`storeList` 先看 `storeEnabled`，再要求套餐 `isActive` 且 `isStoreVisible`；`getSetting` 不走缓存，`setSetting` 的三种数据库 upsert 都对。缺的一直只是「为什么看不到」这句话。
- 套餐统计里加了 `storeVisibleItems`，横幅上那个数字是全库统计，不是当前分页的。

### 版本

- 面板与 APK Release `2.3.327`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.326] - 2026-09-12

### 修复

- **长弹窗的页头页脚会压在正文上**。「新增套餐」里标签行盖住说明文字、「保存」按钮压在「购买入口」那一行上，都是同一个原因：`DialogContent` 是 flex 列，`DialogHeader` / `DialogFooter` 没有 `shrink-0`，内容一超高浏览器就连它们一起压扁 —— 压到比自身内容还矮时，里面的东西溢出自己的盒子，画到相邻内容上。
- 修在弹窗原语里，**全站长弹窗一起好**，不是给这一个贴补丁。套餐弹窗里的标签行也一并钉住：它和可滚区域是同一列的兄弟，一样会被压。

### 说明

- 这跟前几版给节点弹窗加 `shrink-0` 是同一件事的两次遭遇。这次找到了共同的根，所以放回原语里，省得下一个长弹窗再来一遍。
- 一开始怀疑是 `cn()` 没做 tailwind-merge、导致 `flex` 压不过基类的 `grid`。查了，`cn` 用的就是 `twMerge`，这条排除了 —— 真正的原因是 flex 的收缩规则。

### 版本

- 面板与 APK Release `2.3.326`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.325] - 2026-09-12

### 修复

- **对租户说了一句他做不到的话**。「新建节点」在没有可用主机时提示「先去『主机管理』装一台」—— 但那一页对普通用户是关着的：侧边栏藏了（`hiddenNormalUserMainPaths`），路由也是 `AdminRoute`，输网址也进不去。现在按角色分开说，租户看到的是「请联系管理员给你授权一台」。

### 说明

- 主机页自己的空状态本来就有这一档（`user?.role === "admin" ? "点击添加主机生成 Agent 安装命令" : "请联系管理员添加主机"`），是「新建节点」这一段没跟上。
- 上一版放开的是另一件事：**自己名下的主机**能开落地节点了。两者不矛盾 —— 管理员把主机授权给租户之后，租户就能在上面开；他自己加机器仍然走不通，因为那一页对他不开放。

### 版本

- 面板与 APK Release `2.3.325`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.324] - 2026-09-12

### 修复

- **租户加了自己的机器，却不能在上面开落地节点**。「主机管理」对租户本来就是开着的，`hosts.create` 也不是管理员专属 —— 他能加自己的 VPS、能在列表里看见它、能装上 Agent，一点「新建节点」却撞上「无权在该主机上开落地节点」。而那一段的空状态还写着「先去『主机管理』装一台」，等于把人往墙上引。

### 说明

- 原来那条判定只查「被授权的主机」（管理员手动授权 + 套餐附带），**漏了「自己就是主人」这一档**。同一个仓库里 `helpers.ts` 的 `requireHostAccess` 是先认主人再查授权的，这里跟它对齐了。
- 放开它没有多出风险：机器是他自己的，Agent 装在他自己那台上，`maxProxyInbounds` 配额照样管着他能开几个；别人的机器仍然要授权才行。
- 判定抽成了可导出的纯函数 `canOpenInboundOnHost`，路由和用例共用同一份 —— 这条错向任一边都很难看，不该靠肉眼守。用例里连「数据库回来的 userId 是字符串」这种情况都钉住了：用 `===` 比的话，自己的机器会被判成别人的。

### 版本

- 面板与 APK Release `2.3.324`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.323] - 2026-09-12

### 新增

- **节点可以写备注**，「订阅内容」里那个「直连」标签由它顶掉。原来每条直连条目都标「直连」—— 那一段本来就叫「直连」，每条再写一遍等于没说，还占着一行里唯一能放信息的位置。现在填了备注就显示备注（落地 / 家宽 / 备用，随你写），没填才回落到「直连」。

### 说明

- 备注只在面板上显示，**不进订阅** —— 订阅里节点叫什么由「节点名称」决定，两者分开。
- `proxy_nodes.remark` 这一列本来就有，只是编辑弹窗从没给过输入框，接口那边早就通了。
- 落地节点行的第二行也带上备注，就在地址后面 —— 在哪填就在哪看得见。

### 版本

- 面板与 APK Release `2.3.323`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.322] - 2026-09-12

### 修复

- **租户能看到管理员的邮箱**。分享进来的节点在租户面板上标着「xxx@example.com 分享」—— 那是节点主人的显示名，而显示名没设时会退到 username，这个面板的 username 就是邮箱。现在只标「管理员分享」，不带是谁。

### 说明

- 租户拿这个身份也做不了任何事，本来就不该发出去。服务端不再把主人的名字放进返回值，标签由界面固定写死。

### 版本

- 面板与 APK Release `2.3.322`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。
## [2.3.321] - 2026-09-11

### 修复

- **链接弹窗里的复制图标点了没反应**。上一版给 iOS 加的分支自己是坏的：把 `contentEditable` 设在了 `<textarea>` 上，然后用 `Range.selectNodeContents` 去选 —— 而 textarea 的内容在 `value` 里、没有子节点，这么选出来的**选区是空的**。Chromium 上还照样返回 `true`（其实复制了个空），iOS 直接失败。
- 现在弹窗里的复制按钮**直接选中屏幕上那段链接文字**再复制，等同于用户自己长按选中，不依赖任何隐藏元素的技巧。复制不成时选区留着不清，用系统菜单接着复制就行。
- 通用的兜底路径也一并改对：换成带真实文本节点的 `contenteditable` div。

### 说明

- 两条路都用真实模块在非安全上下文（`http://192.0.2.2:8899`）下用 Chromium 实测过：`copyTextFromElement` 选中的是可见的链接文字并返回 `true`，重写后的 `copyTextToClipboard` 也返回 `true`。
- 隐藏元素的几条规矩写进注释了：`display:none` / `visibility:hidden` 的元素选不中；不用 `opacity:0` 和负 `z-index`（iOS 上会让选中失败）；字号给 16px，否则 Safari 会因为小字号可编辑元素获得焦点而把整页放大。

### 版本

- 面板与 APK Release `2.3.321`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.320] - 2026-09-11

### 修复

- **iOS 上「复制」按钮点了没反应**。兜底用的是 `readonly` textarea + `select()`，这套组合在 iOS 上选不中内容，`execCommand` 直接返回 false。iOS 需要元素可编辑、并用 `Range` 选中内容。顺带把那个临时元素从「不可见」改成「1px 透明但真实渲染」——`opacity:0` 加负 z-index 在 iOS 上同样会让选中失败，字号给 16px 是避免 Safari 因为小字号输入框而把整页放大。

### 说明

- **上一版把原因说错了**。当时写的是「http 访问下浏览器不允许网页写剪贴板，没有办法绕过去」。实测不成立：用 Chromium 开一个 `http://192.0.2.2:8899` 的非安全上下文页面，`document.execCommand("copy")` 返回 `true`，连在 `await fetch` 之后调用也是 `true`。
- 准确的说法是：`navigator.clipboard` 确实只在 HTTPS / localhost 下存在，但退回 `execCommand` 之后能不能成，取决于浏览器实现——Chromium 可以，iOS 挑剔。所以这是个可修的兼容性问题，不是环境死路。
- 上一版加的退路（复制不成就把链接弹窗打开、链接整条可选）保留：它对任何失败原因都管用。

### 版本

- 面板与 APK Release `2.3.320`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.319] - 2026-09-11

### 修复

- **「复制链接」失败时给的提示做不到**。原来弹一句「请长按选中链接复制」，可链接压根没显示在屏幕上，让人长按什么。现在复制不成就直接把链接弹窗打开，链接摆在那儿，长按即可选中。
- 复制失败的提示**说清原因**：面板跑在 `http://IP:端口` 上时，浏览器不允许网页写剪贴板，这不是面板坏了，也没有办法绕过去。不说原因的话用户只会反复点。

### 说明

- 浏览器只在**安全上下文**（HTTPS 或 localhost）下开放剪贴板写入。`execCommand` 那条老路还得在用户手势里当场调用，而取链接要先发一次网络请求，手势早过期了 —— 所以 http 访问下一键复制必然失败，没有补救余地。能做的是把内容摆出来让人自己选。
- 弹窗里的链接改成整条可选（`select-all` + 断行），原来是截断显示的，就算长按也选不全。
- 这条只影响 http 访问。HTTPS 或 localhost 下仍然是点一下直接进剪贴板，不弹窗。

### 版本

- 面板与 APK Release `2.3.319`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.318] - 2026-09-11

### 变更

- **协议挪回节点名后面**，第二行让给地址。上一版把协议压到第二行，结果它和安全层、归属一起把 IP 端口挤成了省略号 —— 而地址才是这一行最常要看的东西。
- 「新建节点」第二行里**地址排到最前面**，安全层和归属往后让。

### 说明

- 第一行现在的规矩写死了：**名字 + 最多一个徽章**，而且必须是长度有上限的（协议名最长是 Shadowsocks，约 70px）。徽章本身不是上一版那个 bug 的原因，数量不封顶才是 —— 只要这行还能往里加东西，下次就会再加一个，名字又会被挤没。要再加第三样东西，加到第二行去。

### 版本

- 面板与 APK Release `2.3.318`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.317] - 2026-09-11

### 修复

- **弹窗底部的按钮和正文糊在一起**。可滚的正文和按钮之间没有分隔，滚到底时最后一行字正好贴在「保存」下面，看着像被压住了。现在按钮区自己占住位置、与正文之间有一条分隔线。
- **手机上竖排的弹窗按钮之间没有间距**。`DialogFooter` 原来只给了桌面端的横向间距，竖排时两个按钮糊成一整块——改在组件本身，全站弹窗一起好。

### 变更

- 「多中转时的选路方式」改叫 **「自动选路」**，和「前置代理」一起收进**「高级」**折叠区，默认收起。这两项多数人用不上，平铺着会把「改个名字、换条链接」这种日常操作推到第二屏。

### 说明

- **折叠不会让人怀疑配置丢了**：只要选路方式不是默认值、或者设过前置代理，打开弹窗时「高级」自动展开。折叠可以省地方，不能省到让人以为自己设过的东西没了。
- 选路方式的默认值提取成 `PROXY_NODE_DEFAULT_AUTO_GROUP`，新建时的初值和「改过没有」的判断用同一份定义 —— 两处各写一个字面量的话，将来改默认值会漏掉一处，表现是新建的节点一打开就被当成「改过」。
- 「自动选路」的说明补了一句前提：只有多条中转指向同一个落地时才会生成选路组。原来的说明直接讲三种模式的区别，没说这东西什么时候才起作用。

### 版本

- 面板与 APK Release `2.3.317`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.316] - 2026-09-11

### 变更

- **「订阅」这个词不再指两件事**。面板里它同时被用在「节点订阅」（客户端拉的那条链接）和「计费套餐」上，侧边栏里甚至并排摆着「订阅管理」和「我的订阅」，点进去一个是节点链接、一个是买的套餐。现在计费那一侧统一叫**套餐**，「订阅」只留给节点订阅。
  - 侧边栏「我的订阅」→「我的套餐」，页面标题同步
  - 用户管理「用户订阅管理」→「用户套餐」，空状态同步
  - 套餐管理与商店的「订阅套餐」tab →「套餐」（与「按量计费」成对，去掉「订阅」不丢信息）
- **建用户弹窗里「客户端订阅」的说明改成一句话**。原文是把版本说明的口气直接搬了进去，手机上占三行还断句奇怪。

### 说明

- 路由不动（`/subscriptions` 保持原样），老书签不会断。
- 改的是少数派：套餐页里绝大多数文案本来就在说「套餐」（套餐管理、套餐列表、新增套餐、还没有套餐），跑偏的只有 tab 名和侧边栏那两处。
- 账单页的「订阅记录」「取消订阅」没动 —— 那些在纯计费语境里，读作「订阅了一个套餐」是通顺的，旁边也没有节点订阅可混。
- 顺手修了一处指路失效：套餐页里「用户在『我的订阅』内余额购买」指向的菜单项已经改名。

### 版本

- 面板与 APK Release `2.3.316`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.315] - 2026-09-11

### 修复

- **节点名被挤没了**：手机上那几行只剩一个字。上一版加的分享按钮是压垮它的最后一根 —— 行内已经有开关加三到五个图标，名字是唯一能压缩的元素，动作每多一个，被吃掉的全是名字。「新建节点」那行更严重，名字完全不见，「停用」还被开关压掉一半。

### 变更

- **节点行统一成一个组件**，「新建节点」和「落地节点」两段共用：第一行只放名字，协议、地址、端口、停用、分享人数全部压到第二行的一句话里，超长就截断。
- **行内动作收进 ⋯ 菜单**，只留最常用的「用量」和开关。以后再加功能也只是菜单里多一项，不会再挤到名字。
- **分组多了「自动」这一档，并成为默认**：节点少于 7 个不分组，多了且协议不止一种时按协议分。

### 说明

- **为什么第一行只留名字**：原来是「名字 + 协议徽章 + 安全徽章 + 停用徽章」，徽章都是 shrink-0，只有名字会被压。这不是调一次宽度能解决的 —— 只要还有东西和名字抢同一行，下次加个按钮就会重演。
- **停用不再单独占一个徽章**，整行压暗，状态在第二行的文字里。
- **菜单项推迟到菜单关完再执行**：菜单和对话框都是浮层，同一拍里一开一关会抢焦点，表现是对话框弹出来点不动、甚至整页失去响应。
- **清一色同协议时「自动」不分组**：十个全是 Shadowsocks 的节点分出一个组，标题下面就是原来的整张列表，多一行标题而已。

### 版本

- 面板与 APK Release `2.3.315`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.314] - 2026-09-11

### 新增

- **节点这边也能发起分享**：节点行上多了一个分享按钮，直接勾人。此前只能从用户的权限页反过来挑节点 —— 站在节点这边想把它租出去，得先绕到用户页把人一个个找出来。

### 说明

- 多用户入站派生出好几行节点，每一行是一份独立凭据，所以是**按凭据分别选人**：把两份凭据混成一个列表就分不清谁拿到了哪一份。单凭据时不显示这一层，省掉一个没用的小标题。
- 选人列表里标出「无订阅权限」和「已停用」：分给一个没有订阅权限的人，他照样拿不到节点，事先看得见比事后排查强。
- 两个入口写的是同一张表，方向不同：从用户那边保存是「这个人拿到哪些节点」，从节点这边保存是「这个节点给了哪些人」。两个全量替换的范围不一样，用例盯着这一点。

### 版本

- 面板与 APK Release `2.3.314`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.313] - 2026-09-11

### 新增

- **节点分享**：在用户的权限里可以挑几个节点，直接出现在他的订阅里。节点仍然是原主人的，他改不了也删不掉。只租一两个落地、不值得为他单开端口时用这个。

### 说明

- **和「归属用户」的分工**：归属是把那一份凭据整个转给对方 —— 对方的订阅、对方的配额、对方能改；分享是同一份凭据同时出现在几个人的订阅里，节点还是我的。要按人分开计量就用归属（一人一个端口），只是让人能连就用分享。
- **流量记在节点主人名下**：分享出去的是同一个端口，面板按端口计量，拆不开。这一点写在选择框下面，不要事后才发现。
- 分享进来的节点一律以直连形态进订阅 —— 收方名下没有绑着它的转发，直连是唯一出口。主人自己有没有开「直连也放进订阅」不影响分享出去的那一份。
- 分享出去的那一份不带前置代理引用：它指向主人自己的另一行，而 id 是全表自增的，收方名下恰好有同号节点时会接到一台毫不相干的机器上。
- 收方看不到节点的带宽/总流量/已用量和备注 —— 那是主人的账单口径。
- 节点删掉或用户删掉，分享记录跟着清，不会留下一条指向空处的分享。

### 版本

- 面板与 APK Release `2.3.313`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.312] - 2026-09-11

### 变更

- **转发权限与客户端订阅权限解绑了**，现在可以任意组合：只给转发、只给订阅、两个都给、都不给。此前订阅被绑在转发上——建用户时那个开关是灰的，转发一关订阅就跟着关。

### 新增

- 用户与套餐里各多一个「最大订阅地址数」，0 表示不限。超了在生成前就拦住。

### 说明

- **为什么现在可以解绑**：以前绑着是对的 —— 订阅只能由转发规则汇聚而成，没转发就是一张空订阅。有了自建落地节点之后这条不成立了：一个用户可以零转发，订阅里是他自己主机上的落地节点，直连条目根本不经过中转。那个绑定是上一代的遗留。
- 所以「租两个落地节点给某个用户」现在是完整成立的一档：给订阅权限、不给转发权限、落地节点上限设 2、订阅地址上限设 1。
- **订阅地址为什么也要限**：每条地址都是一份完整凭据，发出去就只能靠吊销那一条收回。不限的话一个租户可以生成几十条分发出去，而你从「有几个用户」上完全看不出来。
- 两个上限都按**客户端订阅权限**开闸，不是按转发权限 —— 只卖转发的套餐不该顺带给出订阅侧的额度。
- 流量用尽或账号被停时订阅权限仍然自动收回，这条没变。

### 版本

- 面板与 APK Release `2.3.312`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.311] - 2026-09-11

### 新增

- 用户可以限制**自建落地节点的数量**。用户管理里多一个「最大落地节点数」，套餐里也多一个，0 表示不限。
- 超了在保存前就拦住，报错里写清「已有几个 / 上限几个」，以及该删一个还是找管理员调高。

### 说明

- **补的是一个真缺口**：此前只校验主机授权。给了一个用户主机授权，他就能在那台机器上开任意多个入站、占任意多个端口 —— 转发规则那边有 `maxRules` / `maxPorts`，落地节点这边一条都没有。要么完全不给自建权限，要么给了就没有刹车。
- **按归属者计数，不是按创建者**。管理员替租户开节点时占的是租户的份额；按创建者算的话，管理员替十个人各开一个会被记成自己开了十个，配额就形同虚设。
- **套餐那一侧按「客户端订阅」权限开闸，不是按转发权限** —— 这是刻意跟 `maxRules` 不一样的地方。落地节点是订阅那一套的配额，一个只卖转发、没开订阅的套餐不该顺带给出落地额度。
- 换归属时也查新主人的额度，否则「建不了就先建给自己再转过去」能绕开上限。没换归属时不查 —— 那会让一个已经超额的用户连改名字都改不了。
- 管理员开给自己的不受限：他要是想开满，配额也拦不住他，拦了反而碍事。
- 合并规矩沿用其他配额：手动与套餐取大，任一为「不限」则不限。

### 版本

- 面板与 APK Release `2.3.311`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.310] - 2026-09-11

### 新增

- 「新建节点」每行多一个链接按钮，点一下复制这个自建节点的分享链接，粘进客户端就能用。多用户入站会先让你挑一个用户 —— 一个用户一条链接，各自是各自的凭据。

### 说明

- 链接是**按需**拉的，不跟着列表走：里面带着完整凭据，而列表每次进页面都会拉、还在轮询。凭据只在你主动要的时候才发出去。
- 主机还没有可用地址时直接报错而不是给一条链接 —— 那种链接里的地址是空的，复制走了也连不上。
- 挑用户的弹窗里写着「链接里带着这个用户的完整凭据，发给谁，谁就能用这个节点」。

### 版本

- 面板与 APK Release `2.3.310`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.309] - 2026-09-11

### 修复

- **自建节点建完不会出现在订阅里。** 派生出来的客户端节点「加进订阅」默认是关的，而它又没绑转发，于是订阅里一条都没有 —— 而这一段的说明写的正是「在自己的主机上开节点，自动进订阅」。现在新派生的节点默认就在订阅里。
- 「落地节点」那一段不再重复列出自建节点派生的那些。它们在「新建节点」里管着，两处都列就是同一个节点出现两次；而且那一段的「编辑」是按「换一条节点链接」设计的，派生节点没有链接，点进去只会得到一句「请粘贴落地机的节点链接」—— 一个必然失败的入口。

### 新增

- 「新建节点」每行多一个开关，控制这个自建节点在不在订阅里。派生节点不在「落地节点」那段列出之后，这是改它的唯一入口。多用户入站的几条派生节点一起改。

### 说明

- 默认值只在**新建时**给，更新时不动：用户手动关掉之后，再改端口、改协议都不会把它重新打开。有测试守着这条 —— 否则表现是「我明明关了，怎么又开了」，而且看不出是谁打开的。
- 为什么粘进来的落地节点默认不进订阅、自建的却进：粘进来的是租来的机器，落地 IP 要藏在中转后面；自建节点开在自己的主机上，直连本来就是它的用法。
- 派生节点仍然留在绑转发和前置代理的下拉里 —— 「自建落地 + 中转」是正常用法，只是不在「落地节点」列表里重复出现。

### 版本

- 面板与 APK Release `2.3.309`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.308] - 2026-09-11

### 变更

- 订阅管理页整体收拾了一遍，目标是读起来像一套东西而不是几块拼起来的。
  - 「订阅内容」里原本有五个子块，各用一种样式（彩色框、虚线框、灰底框、纯标题）。样式不同会让人以为它们是不同性质的东西，其实都只是「订阅里的一部分」。现在统一成同一条细横线小标题，并各自带上条数。
  - 自动选路组不再用高亮底色；跟组类型重复的那句说明（「选这个组，客户端自动挑最快的中转」）去掉了 —— 组名旁边已经写着「自动选最快」。
  - 「还没加入订阅的转发」标题里的括号说明去掉 —— 旁边的下拉自己就写着「选择落地节点」。那一行的高度也收到和其他行一致。
  - 订阅令牌展开后原本是「卡片 → 令牌框 → 展开框 → 标签条」四层边框套在一起，去掉中间那层，改用一条分隔线。
- 「关掉开关只是不进订阅，转发照常运行」从常驻说明改成开关自己的悬停说明。

### 版本

- 面板与 APK Release `2.3.308`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.307] - 2026-09-11

### 变更

- 套餐展开行改成只给数字：`1G / 10T / 33.2M · 0%`。原来带「带宽」「总流量」这些标签，一行二十几个字，手机上会被截断 —— 而截掉的正好是最后的已用量，恰恰是最想看的那个数。

### 说明

- 顺序固定为 带宽 / 总流量 / 已用，缺的那一段用破折号占位，三个数的位置才对得齐。
- 标签没有丢，移到了悬停说明里：桌面端鼠标一停就知道哪个是哪个，手机上靠固定顺序认。

### 版本

- 面板与 APK Release `2.3.307`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.306] - 2026-09-11

### 新增

- 传输方式加上 **HTTPUpgrade**。VLESS / VMess / Trojan 建节点时可选，比 WebSocket 少一次握手往返，过 CDN 时更省事。粘别处的 HTTPUpgrade 节点进来中转也认（`type=httpupgrade`，`httpu` 简写也收）。

### 说明

- 订阅里 base64、Clash（mihomo）、sing-box 三种格式支持它；Loon / Surge / QX 没有这个传输，会跳过并说明原因，而不是渲染出一个协议对、传输错的死节点。
- 中转改写地址时 Host 头保留原落地域名 —— 和 WebSocket 同理，不留的话发出去的 Host 是中转 IP，对端按 Host 路由就找不到人。
- 两端都拿 sing-box 1.14.0 的真二进制验过：入站配置 `check` 通过，订阅渲染出的 sing-box 出站也 `check` 通过，链接来回读写一致。
- 注意 `host` 在 httpupgrade 里是单个字符串，不是 HTTP/2 那样的数组 —— 写成数组 sing-box 直接拒绝配置。
- **伪装网站没做**：sing-box 根本没有 `fallback` 字段（实测报 `unknown field "fallback"`），那是 Xray 的机制。sing-box 给出的答案是 REALITY，而我们建节点默认就是它。要真正的伪装站点得另起 nginx，不属于节点管理这一层。
- BBR 也没做：那是装机时做一次的系统调优，跟节点管理不是一回事。

### 版本

- 面板与 APK Release `2.3.306`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.305] - 2026-09-11

### 新增

- Shadowsocks 的加密方式加上了 `aes-128-gcm` 与 `aes-256-gcm`。现在五选一：SS2022 那三种，加这两种老式 AEAD。默认仍是 `2022-blake3-aes-128-gcm`。

### 说明

- 两类算法的**密码规则完全不同**，面板按算法自动走对应的生成方式：SS2022 是定长密钥（16 或 32 字节的 base64），老 AEAD 是任意口令。给老 AEAD 按密钥长度生成没有意义，给 SS2022 长度填错则 sing-box 报 `bad key` 并拒绝加载整份配置。
- 选中老式 AEAD 时界面会提示：这类有已知的主动探测手段，中间设备能把流量识别出来。只在对端客户端太旧、不支持 SS2022 时才用。
- 五种算法的配置与订阅链接都拿 sing-box 1.14.0 的真二进制验过：一起 `check` 通过，链接是标准的 `ss://base64(method:password)@host:port`。
- 仍然不放出来的：`chacha20-ietf-poly1305`、`aes-192-gcm`、`rc4-md5` 这些。没放出来的算法在保存前就被拦住，不会下发到落地机。

### 版本

- 面板与 APK Release `2.3.305`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.304] - 2026-09-11

### 变更

- 「新建节点」那一段的整段说明文字去掉了。手机上占四行，而里面讲的事（归属用户、按端口计数）在新建弹窗里本来就写着。空列表提示也压成一行。
- 补完上一版漏掉的改名：这一段里的弹窗标题、提示和删除确认还叫着「落地节点」，而那已经是下面另一段的名字 —— 站在「新建节点」里却弹出「删除落地节点？」是会让人以为点错了段的。

### 说明

- 「还没有可用主机」那句留着 —— 它解释了「新建」按钮为什么是灰的，去掉就成了一个没有出路的死角。措辞跟着改成「自建节点要靠 Agent 下发配置」。

### 版本

- 面板与 APK Release `2.3.304`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.303] - 2026-09-11

### 新增

- 新建 Shadowsocks 落地节点时可以选加密方式了，三选一：`2022-blake3-aes-128-gcm`（默认）、`2022-blake3-aes-256-gcm`、`2022-blake3-chacha20-poly1305`。此前固定用 AES-128，界面上看不出来用的是哪个。

### 修复

- 认不出来的加密方式现在在保存前就被拦住。以前只检查「非空」，一个拼错的或老式的算法能存进去、下发到落地机，然后 sing-box 拒绝加载**整份**配置 —— 同一台机器上其他入站跟着一起停，而报错跟「刚改了加密方式」看不出关联。

### 说明

- 只放 SS2022 这三种。老的 `aes-256-gcm`、`chacha20-ietf-poly1305` 有已知的主动探测手段，新开的节点没理由用一个一开始就可被识别的算法。要接别人给的老节点，走「落地节点」粘链接那条路。
- 默认 AES-128 而不是 256：128 位密钥没有任何可行攻击，实际安全性不比 256 弱，但更快 —— 在没有 AES 硬件加速的小机器上差别明显。这也是 SS2022 作者推荐的默认。
- **改加密方式会重新生成密码**，所有客户端要重拉一次订阅。三种算法的密钥长度不一样（128 是 16 字节，另外两种是 32 字节），沿用旧密码会让 sing-box 直接报 `bad key` 并拒绝整份配置。
- 客户端不用手选算法 —— 订阅链接里带着 `method`，导入时自动对上。
- 三种算法生成的配置都拿 sing-box 1.14.0 的真二进制 `check` 过；长度不匹配的反面用例也验过确实会被拒。

### 版本

- 面板与 APK Release `2.3.303`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.302] - 2026-09-11

### 变更

- 节点行上的套餐用量改成**点开才看**：行末多一个小图标，点一下在下面展开一行「带宽 500M · 总流量 1T · 已用 367G（37%）」。IP 端口不再被截断。
- 展开的那一行带标签写清三个数各自是什么 —— 折起来时的 `500M/1T/367G` 得先知道顺序才读得懂。桌面端把鼠标停在图标上也能直接看到，不必点。

### 说明

- **图标本身带颜色**：用到 80% 变黄、超额变红。把数字藏起来的代价本来是「快超额了却要逐个点开才发现」，颜色留在外面就还是一眼能看出哪个节点要满了。
- 没填带宽也没有用量的节点不显示这个图标 —— 点开是空的，那个入口没有意义。
- 为什么不常驻显示：试过放第一行（挤掉节点名）和放第二行（截断 IP 端口）两版，手机上那一行就这么宽，地址和套餐只能二选一常驻，而地址是每次都要看的那个。

### 版本

- 面板与 APK Release `2.3.302`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.301] - 2026-09-11

### 修复

- 填了带宽和总流量之后，节点的名称与 IP 端口在手机上被挤没了。套餐那一栏和名称抢同一行的宽度，屏幕一窄名称就被压成零。现在套餐挪到第二行末尾，名称独占第一行；地址太长时截断的是地址，套餐数字一直看得见。
- 不到 1M 的用量不再四舍五入成「1M」，显示成「<1M」；用量为 0 显示「0」而不是「0G」。套餐按 G 卖，几百字节写成 1M 会让人以为真跑了 1MB。

### 变更

- 「客户端订阅」改名为「订阅管理」，原来的「落地节点」整页并了进来，成为其中的「新建节点」区块 —— 自建节点、粘进来的落地节点、订阅内容、订阅令牌本来就是一件事的四个环节，分两页要来回跳。侧边栏去掉独立入口，`/proxy-inbounds` 自动跳转，老书签不会 404。
- 「订阅内容」也能折叠了，并按「中转」和「直连」分成两组，每组可单独折叠，标题上给出各自的条数。

### 说明

- 中转组排在直连组前面：直连条目只有开了「加进订阅」的节点才有，通常只一两条，而且落地 IP 直接暴露在订阅地址里，放后面不容易被误当成常规入口。
- 用户管理里的「客户端订阅」权限名没有跟着改 —— 它管的是「能不能把转发汇聚成订阅」这个能力，不是某一个页面。

### 版本

- 面板与 APK Release `2.3.301`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.300] - 2026-09-11

### 变更

- 节点行上的流量从「近 72 小时」改成了这台落地机的套餐口径：**带宽/总流量/已用流量**，例如 `500M/1000G/367G`。用到 80% 变黄，超额变红。
- 带宽与总流量在「编辑」里填（面板无从得知你买的是什么套餐）；已用流量由面板自己累加。

### 新增

- 已用流量支持手工校准与一键清零，用来跟机房账单对齐；校准之后仍然继续累加。
- 可以开每月自动清零，按机房的流量周期设日期。

### 说明

- **已用量是面板经手的口径，只会比机房账单小，不会大。** 面板只数得到经转发规则走过的流量 —— 订阅里的「直连」条目是客户端直连落地机的，中转机不在路径上；这台机器上跑的别的服务同理。界面上和悬停说明里都写了这一条，需要对齐就用手工校准。
- 累计量单独存了一列，没有去查 traffic_stats 求和：那张表只保留 72 小时，过期行会被清掉，「这个周期用了多少」事后是算不回来的。
- 累加用的是未乘计费倍率的原始字节 —— 机房按实际跑的量算，倍率是面板向用户计费用的，两回事。
- 流量按 1000 进制换算，满 1T 写成 `1T`。用 1000 而不是 1024 是刻意的：按 1024 算，1000G 的套餐会显示成 931G，跟你买的东西对不上号，看着像面板算错了。
- 自动清零的日期只收 1-28：29/30/31 在二月不存在，那样设会整月不触发，而界面上查不出原因。
- 升级会自动补出新列，已有节点的带宽与总流量为空、已用量从 0 开始累加。

### 版本

- 面板与 APK Release `2.3.300`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.299] - 2026-09-11

### 变更

- 节点列表的行高收窄了近一半：内边距、字号、按钮都调小，「编辑」由文字按钮改成图标。落地节点、订阅内容、已隐藏三段，以及「落地节点」管理页，都用同一套尺寸。
- 「落地节点」整块可以折叠，标题上直接显示「N 个 · N 在线 · N 离线」，收起来就能翻到下面的订阅内容。

### 新增

- 节点可以分组：不分组 / 按协议 / 按状态，每组可单独折叠。分组方式和折叠状态记在本地，下次进来还是这个样子。
- 按状态分组时离线排在最前面 —— 按状态分组的用处就是找出问题节点，排在第一屏才有意义；「未知」那一组放最后，多半是没绑转发的节点，看了也没什么可做的。

### 说明

- 只有一个节点时不显示分组下拉，也一律不分组，免得出现一个改不掉的分组标题。
- 认不出来的协议兜底进「其他」组，不会让节点从列表里消失 —— 有测试守着这条。
- 折叠状态按分组键记，换个组名不会把折叠状态弄丢；浏览器不让写本地存储时也只是下次进来又展开，不影响使用。

### 版本

- 面板与 APK Release `2.3.299`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.298] - 2026-09-11

### 新增

- 客户端订阅页的落地节点行上多了在线状态小圆点：绿=可达，红=探测超时，灰=没有新鲜的探测结果。
- 节点行上显示近 72 小时经这个落地的流量（按绑定的转发汇总，上行 + 下行）。

### 变更

- 节点行上的「多中转时的选路方式」下拉撤掉了，位置让给流量。这个设置在「编辑」弹窗里本来就有一份，而且那份还带说明文字，行上那个是重复的 —— 功能没有减少。
- 节点列表改为 30 秒轮询：状态和流量都是会变的，不刷新的话那个圆点会一直停在进页面那一刻的颜色。

### 说明

- 在线状态来自**中转机**对转发目标做的 tcping，而转发目标就是这个落地的地址端口。所以它回答的是「中转连不连得上这个落地」，不是「你的客户端连不连得上」—— 悬停说明里写了这一条。
- 一个落地被多台中转指向时，**任一中转探得通就算在线**，并显示最快那条的延迟。按「全部可达才算在线」判的话，任意一个机房抖一下都会把落地误标成挂了。
- 没有转发绑定的节点没有任何东西在探它，显示灰色「未知」而不是红色 —— 标红会把一个好端端的节点冤枉成故障。
- 过期的探测（超过 6 分钟）一律退回未知，包括过期的超时。否则一条旧记录会让节点永远挂着红灯。
- 流量窗口是 72 小时，因为流量明细本来就只保留 72 小时。窗口和保留期由同一个常量约束，测试会拦下把两者改得不一致的改动。
- 顺带修掉一个 N+1：原来每个节点单独查一次「有几条转发在用」，节点多了会把一次列表请求变成几十条查询，现在一次查完。

### 版本

- 面板与 APK Release `2.3.298`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.297] - 2026-09-11

### 新增

- 落地节点可以按端口分租给不同用户：管理员建入站时多一个「归属用户」，一台机器上的多个端口可以分别开给不同的人。节点进归属用户的订阅，这个端口的流量扣他的套餐额度、超额自动停，每个人在面板上只看得到自己的那部分用量 —— 也就是 3x-ui 那种「一台机器多用户多端口」的用法。
- 管理员的落地节点列表改为全量，每行标出归属用户；普通用户仍然只看得到自己名下的入站。

### 说明

- 归属用户下拉里会把没有客户端订阅权限的用户灰掉：开给这种用户的话入站能建起来、流量也照扣，但他拉不到订阅，是一个建好了却用不上的节点。要先去「用户管理」里给他开通。
- 换归属会把派生出来的节点从原主人的订阅里移走、进到新主人的订阅里，原主人已经导入的客户端会少掉这个节点。改的是同一条节点记录，不会给原主人留下一条还能连的。
- 同一个入站里的多个用户仍然共用一份流量统计（按端口计数）。要分人算流量就一人一个端口 —— 这正是这次「归属用户」补上的用法。

### 版本

- 面板与 APK Release `2.3.297`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.296] - 2026-09-11

### 新增

- 落地节点支持多用户：一个入站可以给多个人各发一份独立凭据，每人派生一个节点进订阅，不必再为每个人单开一个入站占一个端口。删掉某个用户只有他连不上，其他人不受影响。
- 支持多用户的协议：VLESS、VMess、Trojan、Hysteria2、TUIC、AnyTLS。凭据仍由面板生成，前端不参与。

### 说明

- Shadowsocks 与 Snell 保持单用户，原因是拿 sing-box 1.14.0 的真二进制验出来的：SS2022 多用户的客户端密码要变成「服务端PSK:用户PSK」组合，而 `sing-box check` 对两种写法都放行，写错了配置层看不出来、只在连接时失败；Snell 多用户用「共享 PSK + 每用户 userkey」，而 Surge 与 mihomo 的节点配置里没有 userkey 这个位置，开了多用户除 sing-box 外全连不上。与其给一个「能保存、能下发、就是连不上」的功能，不如明确只支持单用户。
- 改用户名字不会换凭据；重新生成凭据会把该入站所有用户的凭据一起换掉，所有客户端都要重新拉订阅。
- 落地流量仍按端口计数，也就是同一个入站里多个用户共用一份流量统计 —— 多用户解决的是「各自一份凭据、可单独吊销」，不是「分人算流量」。

### 版本

- 面板与 APK Release `2.3.296`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.295] - 2026-09-11

### 新增

- 落地节点支持自动签证书：安全层选「TLS（自动签证书）」，填一个解析到这台落地机的域名和邮箱即可，证书由落地机自己向 Let's Encrypt 申请并自动续期。原来那种自备证书填路径的方式保留为「TLS（自备证书）」。
- 同一个域名上的多个入站共用一张证书，避免撞上 Let's Encrypt 按域名算的签发频率限制。

### 说明

- 面板不实现 ACME 客户端，用的是 sing-box 自带的证书提供者。好处是私钥一次都不经过面板，续期也不需要面板在线。
- 签证书需要落地机的 80 或 443 端口能从公网访问到。域名必须真的解析到这台机器，填 IP 会在保存时被挡下 —— ACME 失败在客户端只表现为握手失败，看不出原因。
- 用的是 sing-box 1.14 引入的 `certificate.providers`，不是 `tls.acme`：后者在 1.14 已标记废弃、1.16 移除。

### 版本

- 面板与 APK Release `2.3.295`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.294] - 2026-09-10

### 新增

- 落地节点的流量接进现有计费：按监听端口内核计数，和转发规则走同一个套餐额度，超额同样触发停用。**不需要升级 Agent**，已装的 Agent 直接就能报上来。

### 说明

- 经中转访问落地节点的流量此前就已经在计费了（走的是转发规则那条路）。本次补上的是客户端**直连**落地节点的那条路 —— 也就是节点上开了「加进订阅」之后多出来的那条，此前它绕过所有转发规则，一个字节都不计。
- 落地流量只累加用户配额，不写按规则的历史明细表（那张表按转发规则 id 组织，塞一个不存在的规则 id 会污染按规则看的报表）。主机维度的流量统计里本来就看得到。

### 版本

- 面板与 APK Release `2.3.294`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.293] - 2026-09-10

### 新增

- 落地节点：可以直接在自己管的主机上开节点，不必再去别处搭。侧边栏新增「落地节点」页，选主机、协议、端口即可，凭据与 REALITY 密钥由面板生成。保存后自动派生一个客户端节点进订阅，中转绑定与订阅渲染那一整套原样接上。八个协议都能开：VLESS / VMess / Trojan / Shadowsocks / Hysteria2 / TUIC / AnyTLS / Snell。
- 默认推荐 REALITY 组合：不需要域名，也不需要证书，新建即可用。要 TLS / WSS 的话需要自己先把证书放到落地机上并填路径。
- 运行时用 sing-box，由面板下发配置。配置装到落地机之前会先跑一遍 `sing-box check`，不通过就整份回滚 —— 一份配置里只要有一个入站不合法，sing-box 会拒绝加载整份配置，同机器上其他节点会跟着停，这道校验就是防这个。

### 说明

- 落地节点只能开在装了 Agent 的主机上，面板要靠 Agent 下发配置。租来的线路机装不了 Agent，那种落地仍然走「客户端订阅」里粘链接的老路，两条路是互补的。
- 改协议、安全层或加密方式会重新生成凭据，客户端需要重新拉一次订阅。沿用旧凭据会得到一份 sing-box 拒绝加载的配置（REALITY 密钥对与 TLS 证书不通用，Shadowsocks 换加密方式后密码长度要求也变了）。
- 删除落地节点会连同它派生的客户端节点一起移除，绑定过它的转发规则自动解绑，转发本身继续运行。

### 版本

- 面板与 APK Release `2.3.293`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.292] - 2026-09-10

### 新增

- 客户端节点支持 Snell（v1–v6）。Snell 没有分享链接，粘 Surge 的那行节点配置即可（`名字 = snell, 地址, 端口, psk=密钥, version=4`），Clash 条目与 sing-box 出站的 JSON 同样能认。各家支持的版本区间不同，渲染时逐家判断：Surge v1–v6，mihomo 到 v5，sing-box 只有 v4 与 v6（v5 与 v4 线格式一致，按 v4 发）。
- 支持 XHTTP 传输（VLESS 专用，Xray 用来取代 H2 的新传输）。目前只有 mihomo 跟进，通用 base64 订阅也能原样带出去；sing-box、Loon、Surge、Quantumult X 会跳过并说明。

### 修复

- REALITY 节点不再静默发给 Surge 与 Quantumult X。这两家的配置里根本没有 REALITY 这一层，此前会渲染成一个「普通 TLS」节点：能导入、能识别协议、握手必失败，而客户端只报一句连接失败。与 Loon 漏公钥是同一类静默失效，现在改为跳过并写明原因。
- 跳过节点时会把策略组里对它的引用一起清干净，空掉的组也一并移除。此前不存在会被跳过的分组格式，所以没有这条路径；留一个指向不存在节点的引用会让 Clash 拒绝整份配置，报的还是「订阅导入失败」这种毫无线索的错。

### 版本

- 面板与 APK Release `2.3.292`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.291] - 2026-09-10

### 新增

- 客户端节点支持 Hysteria2、TUIC v5、AnyTLS 三种协议，链接与 JSON 两种粘贴方式都认（`hysteria2://`、`hy2://`、`tuic://`、`anytls://`，以及 sing-box 出站与 mihomo 条目的 JSON）。加上原有的 VLESS / VMess / Trojan / Shadowsocks，覆盖了目前主流在用的协议。
- 转发规则只放行 TCP 时，不允许绑定 Hysteria2 / TUIC 这类跑在 QUIC 上的节点，订阅组装时也会把这类组合排除并在「订阅内容」里写明原因。QUIC 全程只用 UDP，这种组合能导入、能识别协议，握手却永远收不到回包，客户端只报一句超时。

### 改进

- 各家客户端缺哪种协议，订阅里逐个节点写明跳过原因，而不是让节点凭空消失：Quantumult X 没有 Hysteria2 / TUIC / AnyTLS，Loon 没有 TUIC，Surge 没有 VLESS。协议覆盖最全的是 Clash 与 sing-box 两种格式。
- Loon 的 Hysteria2 只有 `salamander-password` 一个混淆参数位，用 gecko 混淆的节点会被跳过并说明，而不是发出一个连不上的节点。
- Loon 的 `alpn` 改为带引号的逗号写法。此前多个 alpn 用冒号连接，节点行本身以逗号分隔，写法不一致会让 Loon 解析错位。

### 版本

- 面板与 APK Release `2.3.291`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.290] - 2026-09-10

### 修复

- Loon 格式的订阅漏掉了 VLESS Reality 的公钥与 short-id，导致这类节点在 Loon 中一律连接失败。Clash 与 sing-box 两种格式此前已正确输出，只有 Loon 这条路径遗漏。缺少公钥时握手无法完成，而客户端只显示一句连接失败，看不出缺的是参数而非网络。
- 节点链接现在也认 Shadowrocket 写法的 VLESS：整段为 base64、用 `tls=1` 而非 `security=tls`、Reality 不写 `security` 而靠 `pbk` 判断、名称在 `remarks` 参数里、流控写成 `xtls=N`。此前这类链接会直接报「格式无法识别」。

### 版本

- 面板与 APK Release `2.3.290`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.289] - 2026-09-10

### 修复

- 客户端节点的编辑弹窗在手机上滚不动。弹窗容器是固定最大高度加隐藏溢出，内容超出后直接被裁掉而不是出现滚动条，导致够不到下方的节点链接输入框和保存按钮。改为内容区独立滚动；二维码弹窗同样处理。

### 改进

- 精简「客户端订阅」页面的说明文字：删去页面顶部、「落地节点」和「订阅链接」三处介绍段落，其余说明只保留会影响操作的那一句。说明文字统一改用更小的字号，与面板其他页面一致。删去说明后，卡片标题与右侧按钮改为垂直居中对齐。
- 「把这个节点也放进订阅」开关更名为「加进订阅」。

### 版本

- 面板与 APK Release `2.3.289`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.288] - 2026-09-10

### 新增

- 客户端节点新增「把这个节点也放进订阅」开关。节点模板本来就是一个完整节点（地址、端口、凭据都全），平时只取凭据、把地址端口换成转发入口；开了这个开关就再原样产出一条。用途有二：一是落地机的直连地址进订阅，和各条中转并列进同一个选路组，客户端可以自己在「直连落地」和「走中转」之间挑快的；二是租来的、不做转发的节点（例如只给了节点链接的线路机）也能靠它进订阅。默认关闭且按节点单独控制 —— 开了之后该节点的地址会出现在每一条订阅地址里。
- 客户端节点新增「前置代理」，指向另一个节点，表示连接先经由它建立。适用于「客户端 → 租来的线路机 → 自己的落地」这种链路：中间没有自己的机器，四层转发插不进去，只能由客户端完成链式。订阅渲染时输出各家的链式字段（Clash 的 `dialer-proxy`、sing-box 的 `detour`、Surge 的 `underlying-proxy`），导入即生效。Loon 与 Quantumult X 的订阅格式没有这个位置（其代理链只存在于客户端界面中），这两种格式下节点名会标注「需手动接」，需要在客户端里自行连一次。被选为前置的节点会自动进订阅，无需再单独开启上一条的开关。
- 添加客户端节点时除了节点链接，也可以直接粘贴 JSON，按结构自动判断：sing-box 出站、Clash 节点条目、v2rayN 的 VMess JSON，以及完整的客户端或服务端配置文件。粘服务端配置时若其中没有公网地址（`listen` 通常是 `0.0.0.0`），会提示补填而不是猜一个 —— 猜错会生成一个连不上的节点，且在客户端侧看不出原因。
- 订阅内容里每个节点可以单独改显示名。转发派生的节点留空可恢复默认名；直连节点改的是节点模板本身的名字。

### 修复

- 客户端图标下载脚本会把图标下到面板读不到的目录。本地安装的面板数据目录在 `/opt/forwardx-panel/data`，路径写在 `.env` 里由面板启动时加载，而脚本是手动运行的、不会加载 `.env`，于是下到了 `/data`。结果是脚本显示全部成功、面板一张图标也看不到，两边都不报错。脚本现在会主动读取面板配置来定位目录，并在每次运行时打印解析出的路径及其来源；服务端的目录解析也补上了同样的回退。

### 版本

- 面板与 APK Release `2.3.288`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.287] - 2026-09-10

### 同步上游

- 合入上游 poouo/Forwardx v2.3.280：修复转发规则按链路两级筛选、以及批量管理打开时主列表消失的问题；优化延迟统计、隧道端口分配、转发组成员上限和 Agent 运行状态恢复；增强 Agent/面板诊断与流量统计。共 52 个文件、约 4900 行改动。
- 注意：本仓库与上游在 v2.3.279 之后各自使用过 `2.3.280` 这个版本号 —— 本仓库的 2.3.280 是「多前置带宽叠加」，上游的是上述隧道与规则修复，两者内容不同。上游那版的改动收在本版里。
- 顺带修复上游带过来的一处问题：其新增的 6 个测试文件未登记进 `test:server` 列表，在 CI 里不会执行，现已全部登记。

### 新增

- 添加用户时即可授予「客户端订阅」权限，不必先建用户再进编辑页开。转发总开关关闭时该开关不可用，关闭转发也会一并收回订阅 —— 转发都停了，订阅只会给出连不通的节点。默认关闭。
- 客户端图标下载脚本补齐至全部 13 个客户端，新增 NekoRay、v2rayN、Surfboard，并支持 `.ico` 格式（v2rayN 上游只提供 `.ico`，内含 256px）。

### 改进

- 新建订阅链接只需填写用途。此前还要选「默认格式」和「分流预设」：分流预设在展开的「一键订阅」面板里本就能改，重复询问；默认格式仅在客户端标识无法识别时才生效，而客户端图标现已固定格式、标识识别也覆盖了全部客户端，这一项几乎不再起作用。两项均已移到各自真正生效的位置就地调整。

### 修复

- 客户端图标下载脚本此前未包含在发布包中，安装后的面板上找不到该文件。已加入打包清单。

### 版本

- 面板与 APK Release `2.3.287`，Agent `2.2.195`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。
- **本次 Agent 有改动，需要升级**：上游 v2.3.280 改写了 Agent 的指标采集与 TCPing 上报（`agent/metrics.go` 等，约 500 行），面板升级后请一并升级各主机的 Agent，否则拿不到新的诊断与流量统计。ForwardX FXP runtime 无改动。
- 另需注意：Agent 版本号此前也与上游撞过号 —— 本仓库和上游都独立发布过 `2.2.194`，内容不同。本次升到 `2.2.195` 以区分，并确保已安装的 Agent 能收到升级提示（升级判定按版本号比较，号不变就不会提示）。

## [2.3.286] - 2026-09-10

### 新增

- 一键订阅的客户端从 7 个补到 13 个，新增 Hiddify、v2rayNG、Surfboard、NekoBox、NekoRay、v2rayN。其中 Hiddify 与 v2rayNG 有官方导入 scheme，点一下直接跳进客户端；Surfboard、NekoBox、NekoRay、v2rayN 官方没有导入 scheme（NekoBox 的还只是未实现的功能请求），这几格点开会给出该客户端对应格式的订阅地址、二维码和一句「粘到哪儿」的说明。订阅地址本身对任何客户端都有效，deep link 只是省一次粘贴 —— 没有 scheme 不等于不支持。原本的 7 格容易被误读成「只支持 7 个客户端」，实际是 7 个 URL scheme，其中一格 Clash 就覆盖了 Clash Verge Rev、ClashX、ClashX Meta、FlClash、Clash for Android、Clash Meta，现在把覆盖范围写进了悬停提示。
- 客户端图标按当前设备自动筛选。一键导入走的是 deep link，只有装了该客户端的设备点得动，在 Windows 上摆一格 Loon 就是个点了没反应的按钮。现在按浏览器所在系统筛（iOS 8 个、Android 6 个、macOS 与 Windows 各 5 个、Linux 3 个），识别不出来时展示全部，另有「显示全部」可随时翻出其他平台的，被筛掉的置灰并写明「本机没有」而不是凭空消失。iPadOS 的浏览器标识与桌面 Mac 完全一致，靠触摸点数区分，避免 iPad 上少掉只有 iOS 才有的那几个客户端。
- 新增 `scripts/fetch-client-logos.mjs`，一条命令把各客户端的官方图标下载到面板数据目录（`/data/clientLogos/`）。开源客户端取自各自上游仓库，闭源客户端取自 App Store 的公开接口（1024px 原图）。图标放在数据目录而不是打包进产物：换图不需要重新构建，升级也不会被覆盖。未放置图标时使用界面内置的图案。

### 修复

- 扫码弹窗在手机上被裁切：二维码只显示一半，标题、订阅地址和复制按钮跑出屏幕。根因是弹窗底部那条不可断行的订阅地址把内部栅格撑到比屏幕还宽，再被裁掉，三处症状是同一个原因。现在地址断行显示、二维码宽度跟着弹窗走，窄屏整体缩小而不是被裁；地址也不再截断 —— 手机上截断的地址等于没有。
- 订阅页的复制按钮在以 `http://` 访问的面板上必定失败。浏览器的剪贴板接口只在安全上下文中存在，此前直接调用它，非 HTTPS 面板上只会提示复制失败。改用面板已有的带回退实现。
- 修复一个格式协商问题：订阅格式的判定顺序是地址参数 → 客户端标识 → 令牌默认格式，而通用 base64 的地址刻意不带格式参数。于是当令牌默认格式被设为 Clash 时，从 Shadowrocket、Hiddify、v2rayNG 的图标点进去会收到一份它们读不懂的 Clash 配置。现在从客户端图标进入时固定格式，同时把这几个客户端的标识加入服务端识别，让手动复制的通用地址对它们也能协商正确。
- 修正 Surge 的平台标注：`surge:///install-config` 是 Surge 在 iOS/macOS 上的专属 scheme，安卓上的 Surfboard 虽然读同一套配置格式，但只有自己的导入界面、不认这个 scheme，此前误标为支持安卓。

### 版本

- 面板与 APK Release `2.3.286`，Agent `2.2.194`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.285] - 2026-09-10

### 新增

- 客户端订阅新增二维码。原来的一键导入图标只在「面板和客户端在同一台设备」时有用，在电脑上看面板、往手机里导入之前只能手抄地址。码里放的是不带 `format` 的订阅地址，扫码的客户端按自己的 User-Agent 协商格式，Loon 扫到 Loon 格式、Clash 扫到 Clash 格式；切到规则订阅再扫会自动带上 `rules=1`，两种订阅各扫各的，不必为此另发一条链接。需在客户端的「添加订阅」里扫，用系统相机只会在浏览器里打开这条地址。
- 客户端订阅改为按用户单独授权，不再是所有人默认可用。授权来自管理员手动开启或套餐附带，两者取或，管理员自身不受限制。这项权限刻意不跟着转发权限走：有套餐就能建转发，但订阅必须显式开启。用户被暂停或流量用尽时权限自动收回，此时其转发本已停用，订阅里只会是一堆死节点。无权限时公开订阅地址与令牌无效同样返回 404，不泄露「令牌存在但没权限」；权限收回后旧地址立即失效，无需吊销令牌。**升级后所有非管理员用户都需要重新授权才能继续使用订阅。**

### 改进

- 一键订阅改为折叠式图标网格。原来把两种订阅 × 六种格式全部平铺，一个订阅链接就是十几行地址，手机上要滚很久才找得到自己的客户端；现在默认折叠，点开后按客户端排成图标网格，点一下直接导入。每个图标下标注适用平台，方便判断自己能不能用。切到规则订阅时，表达不了规则的客户端置灰并写明原因，而不是直接隐藏。分流预设也移到了规则订阅那一段，紧挨着它作用的地址。
- 订阅令牌的「重置」按钮改为「重置地址」。原来只写「重置」、配一个刷新图标，在订阅面板的语境里容易被读成「重置流量」，但它实际做的是轮换订阅地址，误点会让所有已导入的客户端失效。现在补上了宾语、换成钥匙图标，确认框里也说明不会重置流量。

### 版本

- 面板与 APK Release `2.3.285`，Agent `2.2.194`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.284] - 2026-09-09

### 新增

- 新增「客户端订阅」：把转发规则自动汇聚成可直接导入客户端的订阅地址，之后新建的转发会自动进订阅，不用再手动维护节点列表。转发规则本身只存地址和端口、不含任何节点凭据（这是四层转发器的设计决定的，面板并不知道目标端口后面跑的是什么协议），因此需要把落地机的原始节点链接登记一次；模板按落地机算而不是按转发算，通常只需要一两个。支持 VLESS / VMess / Trojan / Shadowsocks 四种链接，改写时只替换地址和端口，UUID、密码、SNI、传输方式原样保留。
- 订阅支持 6 种客户端格式：通用 Base64、Clash/mihomo、sing-box、Loon、Surge/Surfboard、Quantumult X，按 `format` 参数或 User-Agent 自动选择。Surge 原生不支持 VLESS，这类节点会被跳过并在文件开头注明原因与替代格式，而不是产出它读不懂的配置。
- 同一个订阅链接同时提供两种地址：不带 `rules` 参数是节点订阅（全部格式可用，适合习惯自己配分流的客户端），带上是规则订阅（仅 Clash 与 sing-box 能表达规则）。换一种不必重新签发地址。
- 分流规则预设分精简 / 均衡 / 完整三档，引用 MetaCubeX 与 SagerNet 的上游规则集，每个分类各有策略组，可在客户端里单独改某一类的去向。局域网判断用两家的原生表达而不是外部规则集，少一个下载依赖。
- 多台中转指向同一个落地节点时，订阅里自动生成选路组，客户端会自己挑最快的那条中转，故障时自动切换；可按节点设为自动选最快、主备切换或不生成。只有一台中转时不生成，避免界面上多出无意义的分组。
- 订阅链接支持一键导入：Clash、Stash、sing-box、Loon、Surge/Surfboard、Quantumult X、Shadowrocket 各自的 deep link，点一下直接跳进客户端。
- 订阅响应带 `Subscription-Userinfo`，客户端可直接显示已用流量与到期时间。订阅令牌可建多个、可单独重置（设备丢失时只吊销那一个），并记录访问 IP 与 UA。
- 绑定到订阅的转发可以单独隐藏，隐藏后不出现在订阅里，转发本身照常运行。

### 修复

- iperf3 装不上不再阻断 Agent 安装。它只服务于 Looking Glass 的测速，Agent 侧本就有兜底，端口转发和隧道完全不依赖它，此前却因为被列为关键依赖而让整个 Agent 装不上。同时依赖安装的输出会写入日志文件，缺依赖时能看到包管理器的真实报错，而不只是一句「未能安装依赖」。

### 版本

- 面板与 APK Release `2.3.284`，Agent `2.2.194`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。Agent 与 FXP runtime 本次无改动，已安装的 Agent 无需升级。

## [2.3.283] - 2026-09-04

### 新增

- 转发组和入口组的成员健康检测新增 Ping 方式，与原有的 TCPing 并列。此前探测方法在生成任务时写死为 TCPing，目标不开放 TCP 端口、或只想看链路通断和延迟时没有可用方式。Ping 不需要端口，从 TCPing 切换过来时目标里残留的端口会被直接忽略。切换方式会重置成员已存的健康状态，避免沿用上一种方式的判定。Agent 侧本就支持该探测类型，已安装的 Agent 无需升级。

### 修复

- 隧道的「中转模式」在中转不足 2 台时不再整个隐藏，改为显示并置灰，同时说明还差几台中转、或该隧道类型是否支持。原先隐藏后界面没有任何线索表明这些模式存在，只能靠翻文档才知道需要凑够 4 台主机。隧道类型不支持带宽叠加时优先提示协议限制，而不是误导性地让用户去加中转。

### 版本

- 面板与 APK Release `2.3.283`，Agent `2.2.194`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。

## [2.3.282] - 2026-09-04

### 变更

- 入口组的「多前置带宽叠加聚合」改名为「多入口带宽加权分流」，策略「动态叠加」改为「动态调节」。原名里的「叠加」会被读成把多台机器的带宽叠到一条连接上，而该功能是按带宽比例把不同客户端分到不同入口，单条连接始终只走一台。开关描述中补充了这一边界，并指向隧道的「中转带宽叠加」——拆分单条连接的是那个功能。仅文案调整，字段名与数据库列不变，无需迁移。

### 修复

- 本地面板安装脚本的版本解析失败改为写入 stderr。这两处提示原本写到 stdout，而函数运行在命令替换里，提示会被调用方的变量吞掉，仓库尚未发布 Release 时操作者只能看到 curl 裸报的一行 404。同时补充说明 404 通常意味着仓库还没有 Release。

### 版本

- 面板与 APK Release `2.3.282`，Agent `2.2.194`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。

## [2.3.281] - 2026-09-04

### 新增

- ForwardX 隧道新增中转「带宽叠加」模式：单条连接被拆分到全部中转并行传输，由出口按序重组，单连接可用带宽接近各中转之和，不再受限于最慢的一条路径。
- 入口按序号给数据块编号并分发给最先空闲的中转，快的中转自然承担更多流量，无需手动配置权重；出口去重并按序交付，字节流与单路完全一致。
- 单条中转中断时其在途数据块交由其他中转重发，会话继续；全部中转中断才结束会话。中转本身无需改动，仍是透明转发。

### 版本

- 面板与 APK Release `2.3.281`，Agent `2.2.194`，ForwardX FXP runtime `2.2.118`，Android APP `2.3.97`。

## [2.3.280] - 2026-09-04

### 新增

- 入口组新增多前置 VPS 带宽叠加聚合：按每台前置声明的上行带宽分配入口解析份额，使整组可用带宽接近各台之和。
- 提供手动权重、均分、带宽比例和动态叠加四种聚合策略，动态叠加按实时剩余带宽把新连接导向更空闲的前置。
- 入口组编辑弹窗展示聚合可用带宽、实时已用带宽、占用率和每台前置的份额；健康前置低于下限时自动退回等量分配。

### 版本

- 面板与 APK Release `2.3.280`，Agent `2.2.193`，ForwardX FXP runtime `2.2.117`，Android APP `2.3.97`。

## [2.3.279] - 2026-09-03

### 修复与优化

- 增强面板、Agent 与 FXP 的心跳、流量、运行时恢复和诊断日志，降低异常状态下的 CPU、内存与事件循环压力。
- 为 SSE、支持包、日志、状态缓存和任务队列增加背压、超时、去重及容量保护，避免连接抖动造成资源持续增长。
- 优化 SQLite 指标查询与历史清理、Agent 注册/升级校验及 Realm 兼容资产回退，提升长时间运行稳定性。
- 修复旧版故障转移组升级后可能无提示继承转发工具或 PROXY 配置的问题，旧组保持原成员运行配置。

### 版本

- 面板与 APK Release `2.3.279`，Agent `2.2.193`，ForwardX FXP runtime `2.2.117`，Android APP `2.3.97`。

## [2.3.278] - 2026-08-30

### 修复与优化

- 修复 FXP 多出口故障转移、连接上限和隧道恢复，避免单一入口连接异常影响整条链路。
- 修复转发组成员继承转发工具与 PROXY 配置，并完善转发工具切换时的 nftables/iptables 旧规则清理。
- 完善 Agent/FXP、Realm/GOST/Nginx 资产与运行时安装升级校验，候选文件异常时保留现有可用版本。
- 停止生成 Realm 2.9.x 不兼容的旧传输参数，避免配置看似开启但实际不生效。
- 优化延迟详情统计、本地开发环境和 Agent/FXP 运行状态同步。

### 版本

- 面板与 APK Release `2.3.278`，Agent `2.2.192`，ForwardX FXP runtime `2.2.116`，Android APP `2.3.97`。

## [2.3.277] - 2026-08-23

### 修复与优化

- 修复本地开发面板端口代理、免登录和退出清理，避免开发环境出现代理断连噪声。
- 优化 Agent/FXP 运行状态、流量统计、连接统计和规则恢复流程。
- 修复白名单插件 nftables 规则加载失败时的错误处理，并完善登录人机验证与安装升级脚本。

### 版本

- 面板/APK Release `2.3.277`，Agent `2.2.191`，ForwardX FXP runtime `2.2.115`，Android APP `2.3.97`。

## [2.3.276] - 2026-08-14

### 新增

- 增加管理员密码本机与容器重置命令，重置后自动吊销已有会话。

### 修复与优化

- 加固禁用账号、双重验证、Telegram 登录、支付回调及计费和端口分配的并发安全。
- 优化端口策略、面板日志、状态缓存与 Agent/FXP 任务调度，降低高负载下的 CPU、内存和进程占用。
- 修复流量周期边界、多段端口策略、回环地址校验，以及自定义 HTML、Markdown 链接和容量显示问题。

### 版本

- 面板与 APK Release `2.3.276`，Agent `2.2.190`，ForwardX FXP runtime `2.2.114`，Android APP `2.3.97`。

## [2.3.275] - 2026-08-13

### 修复与优化

- 修复转发组故障转移按成员优先级恢复，成员恢复后自动回切到更高优先级成员。
- 修复规则离开页面后状态短暂变黄、普通用户入口域名展示不一致等问题，复用上次状态并平滑刷新。
- 增加 GitHub 加速站配置与安装/升级脚本支持，补充部署文档和升级校验。

### 版本

- 面板与 APK Release `2.3.275`，Agent `2.2.189`，ForwardX FXP runtime `2.2.113`，Android APP `2.3.97`。

## [2.3.274] - 2026-08-09

### 新增

- 支持用户同时拥有多个套餐，合并可用权益并分别展示套餐、手工额度和附加流量。

### 修复与优化

- 修复套餐到期、重置和续费后的资源回收、周期展示及 Agent 状态刷新。
- 修复 Agent/FXP 流量与连接统计，优化 UDP 缓冲回收和心跳离线重试，减少误报与内存占用。

### 版本

- 面板与 APK Release `2.3.274`，Agent `2.2.188`，ForwardX FXP runtime `2.2.113`，Android APP `2.3.97`。

## [2.3.273] - 2026-08-07

### 新增

- 端口转发、转发链和转发组增加资源限速，覆盖 GOST、ForwardX、iptables、nftables、Realm、Socat 和 Nginx。

### 修复

- 修复 Agent 离线判定和 DDNS 故障转移延迟，并为状态切换失败增加有限重试。
- 修复 ForwardX、mimic 和故障转移的监听切换、重启恢复及运行状态同步。
- 修复套餐月度重置日、下次流量周期和流量超限后规则恢复不一致。
- 修复主机探测延迟图长时间范围截断、时间轴偏移和首次打开状态错误。
- 修复 AI API Key 无法删除。

### 版本

- 面板与 APK Release `2.3.273`，Agent `2.2.187`，ForwardX FXP runtime `2.2.112`，Android APP `2.3.97`。

## [2.3.272] - 2026-08-04

### 修复

- 修复 WireGuard UDP 代理关闭时队列清理与写循环互等，避免 Agent 运行时卡死。
- 修复 nftables 旧版解析兼容、转发链放行和 MASQUERADE 失败时无回退导致的链路不通。

### 版本

- 面板与 APK Release `2.3.272`，Agent `2.2.186`，ForwardX FXP runtime `2.2.112`，Android APP `2.3.97`。

## [2.3.271] - 2026-08-04

### 修复

- 修复 Agent/FXP 流量上报在挂载路由下签名路径不完整导致持续 `401`，并修复繁忙探测任务的强制刷新丢失。
- 修复历史转发组子规则、成员引用和删除标记残留，避免规则不可见或阻塞主机删除；启动时自动修复可确认的历史数据。
- 修复隧道规则探测只收到出口结果时误报成功、首段缺失时显示旧结果，以及隧道段超时状态和总延迟展示不准确。
- 修复 WireGuard UDP 代理关闭时队列清理与写循环互等，避免 Agent 运行时卡死。
- 修复 nftables 旧版解析兼容、转发链放行和 MASQUERADE 失败时无回退导致的链路不通。

### 版本

- 面板与 APK Release `2.3.271`，Agent `2.2.186`，ForwardX FXP runtime `2.2.112`，Android APP `2.3.97`。

## [2.3.270] - 2026-08-04

### 修复

- 修复 Agent/FXP 升级或面板配置变化后复用旧进程，导致 ForwardX 隧道可转发但流量不上报。
- 修复 GOST 等进程转发短连接漏计，导致 24H 连接数长期为 `0`。
- 增加 FXP 流量上报限频诊断，并保证失败批次幂等重试。
- 修复 GOST/ForwardX UDP 会话提前关闭及重建后断流，降低高包速率开销并限制队列与分片内存。
- Agent 心跳失败采用单一有限重试队列，避免请求叠加，并在任意心跳成功后立即取消重试。
- 修复 Agent Token 删除时待清理规则误拦截，并在真实规则占用时显示所属用户。

### 版本

- 面板与 APK Release `2.3.270`，Agent `2.2.184`，ForwardX FXP runtime `2.2.112`，Android APP `2.3.97`。

## [2.3.269] - 2026-08-02

### 修复

- 修复普通用户转发组流量与连接数归属，以及 Agent/FXP 连接累计不准确。
- 修复可选访问限制和 TCP/UDP 计数链恢复导致的重复重启或计数中断。
- 修复批量导入、复制及跨组迁移的入口端口误判和并发分配冲突。
- 修复隧道转发延迟分段误显示“等待探测”和出口组文案错误。

### 版本

- 面板与 APK Release `2.3.269`，Agent `2.2.183`，ForwardX FXP runtime `2.2.111`，Android APP `2.3.97`。

## [2.3.268] - 2026-08-01

### 新增

- 增加套餐流量与按量计费流量分开统计及重置展示基线。
- 增加自定义菜单的面板路径、内嵌和新窗口打开方式。
- 补充隧道、多入口延迟分支详情和故障排查文档截图。

### 修复

- 修复 Agent/FXP 在重启、升级及 V1/V2、WireGuard、GOST 切换后的运行态恢复和监听校验。
- 修复 Agent 流量计数链恢复、规则修复时的重复清理和计数丢失，保留已有防火墙计数。
- 修复隧道、转发链和多入口探测的拓扑匹配、并发刷新、超时及旧结果串用。
- 修复延迟详情节点归属、普通用户流量展示和自定义菜单嵌入兼容性。

### 版本

- 面板与 APK Release `2.3.268`，Agent `2.2.182`，ForwardX FXP runtime `2.2.110`，Android APP `2.3.97`。

## [2.3.267] - 2026-08-01

### 修复

- 修复支付回调重新激活已关闭订单，并启用 PostgreSQL TLS 证书校验。

### 版本

- 面板与 APK Release `2.3.267`，Agent `2.2.181`，ForwardX FXP runtime `2.2.110`，Android APP `2.3.97`。

## [2.3.266] - 2026-07-30

### 新增

- 主机累计流量支持“用量修正”，可按实际用量覆盖当前统计值。

### 修复

- 修复 GOST 六种原生传输、单 UDP 规则协议和多跳中继鉴权配置。
- 修复 ForwardX FXP 切换 GOST 时旧运行态残留及认证失败，增加事务交接、失败回滚和运行态恢复。
- 修复普通用户规则列表按账号隔离，以及筛选无结果后无法返回的问题。

### 优化

- 调整主机卡片和列表快捷操作，取消瞬时流量动画，并明确 Agent Token 创建时间展示。

### 版本

- 面板与 APK Release `2.3.266`，Agent `2.2.181`，ForwardX FXP runtime `2.2.110`，Android APP `2.3.97`。

## [2.3.265] - 2026-07-30

### 修复

- 稳定 Agent 在 ForwardX V1/V2、WireGuard 与 GOST/Nginx 间切换时的运行态交接、监听就绪和失败恢复。
- 转发链支持各成员独立使用主机端口区间，并修复同步回滚、链路探测和流量归属。
- Docker 升级增加镜像与运行版本校验，避免旧实例仍在运行时误报升级成功。
- Docker 镜像改为 amd64/arm64 原生构建并验证后合并，避免 QEMU 异常导致 ARM64 镜像缺失。
- 修复隧道延迟刷新超时边界偶发重复查询。

### 版本

- 面板与 APK Release `2.3.265`，Agent `2.2.180`，ForwardX FXP runtime `2.2.110`，Android APP `2.3.96`。

## [2.3.264] - 2026-07-29

### 新增

- Nginx Stream 自定义证书支持拖放或多选上传 PEM 证书链和私钥。

### 修复

- 修复 ForwardX V1/V2 会话、队列、握手及规则替换造成的内存、连接和旧运行态回收问题。
- 修复 Nginx Stream TCP 长连接超时、入口证书处理和旧证书清理问题，补充会话诊断日志。

### 优化

- 优化大批量 ForwardX V1/V2 入口规则合并，降低 Agent 的 CPU 与 GC 压力。
- 优化 Agent Token 备注展示，并统一隧道探测超时文案。

### 验证

- 类型检查、前端构建、Agent/FXP 测试、竞态测试和 `go vet` 通过。

### 版本

- 面板与 APK Release `2.3.264`，Agent `2.2.179`，ForwardX FXP runtime `2.2.110`，Android APP `2.3.96`。

## [2.3.263] - 2026-07-29

### 修复

- 修复 ForwardX FXP 多入口及 V1/V2 混用时的运行态残留、误清理和 WireGuard peer 未就绪问题。
- 稳定转发组健康切换，避免短暂超时或面板通讯波动造成入口、DNS 和规则状态反复切换。
- 修复资源撤权、规则迁移和按量计费授权不一致的问题，并避免旧规则清理误删同端口新规则。
- 拆分端口转发与网络测试主机授权，普通用户不再看到未授权的底层主机。
- 修复域名到期或 DNS 失败引发的重复解析和运行态抖动，失败时保留最后有效地址。
- 修复 iptables、nftables、Realm、Socat、GOST、Nginx 与 ForwardX 的流量重复、串流或漏计问题。
- 减少大规则量下防火墙计数规则的扫描和重建，降低域名变化、Agent 重启时的 CPU 峰值，并兼容 Alpine/BusyBox。

### 验证

- 服务端 `427/427`、Agent/FXP 测试、类型检查、生产与文档构建均通过；未执行 Docker 构建。

### 版本

- 面板与 APK Release `2.3.263`，Agent `2.2.178`，ForwardX FXP runtime `2.2.109`，Android APP `2.3.96`。
