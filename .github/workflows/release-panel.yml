name: Release Panel

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write
  packages: write

concurrency:
  group: forwardx-release-panel-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  release-panel:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23.x"

      - name: Resolve release version
        id: release
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            VERSION="$GITHUB_REF_NAME"
          else
            VERSION="v$(node -p "require('./package.json').version")"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "version_no_v=${VERSION#v}" >> "$GITHUB_OUTPUT"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check release versions and tag
        env:
          FORWARDX_RELEASE_TAG: ${{ github.ref_type == 'tag' && github.ref_name || '' }}
        run: pnpm check:versions

      - name: Type check
        run: pnpm exec tsc --noEmit

      - name: Run panel tests
        run: pnpm test:server

      - name: Run Agent tests and vet
        shell: bash
        run: |
          (cd agent && go test ./... && go vet ./...)
          (cd forwardx-fxp && go test ./... && go vet ./...)

      - name: Resolve previously published release
        id: previous
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ steps.release.outputs.version }}
        run: |
          # 发布说明要覆盖「自上一次发布以来」的全部版本，而不是只有最后一次版本号
          # 里写了什么 —— 连着合两个 PR 才发一次版是常态。
          # 拿的是最新的正式 Release（跳过草稿、预发布，也跳过这次自己重跑时已经
          # 建好的那一个）。取不到就留空，脚本会退回只取一节，不会挡住发布。
          PREVIOUS="$(gh api "repos/${GITHUB_REPOSITORY}/releases?per_page=50" \
            --jq "[.[] | select(.draft == false) | select(.prerelease == false) | .tag_name] | map(select(. != \"${RELEASE_VERSION}\")) | .[0] // empty" \
            || true)"
          echo "previous=${PREVIOUS#v}" >> "$GITHUB_OUTPUT"
          echo "上一个已发布版本：${PREVIOUS:-(取不到)}"

      - name: Prepare release notes
        shell: bash
        run: node scripts/extract-release-notes.mjs "${{ steps.release.outputs.version_no_v }}" "${{ steps.previous.outputs.previous }}" > RELEASE_NOTES.md

      - name: Build panel assets
        run: pnpm build

      - name: Build bundled Agent assets
        run: bash scripts/build-agent-release.sh

      - name: Package panel runtime bundle
        shell: bash
        env:
          VERSION_NO_V: ${{ steps.release.outputs.version_no_v }}
        run: |
          mkdir -p .release dist/panel
          BUNDLE="forwardx-panel-v${VERSION_NO_V}.tar.gz"
          tar -czf ".release/${BUNDLE}" \
            dist \
            client/dist \
            drizzle \
            patches \
            package.json \
            pnpm-lock.yaml \
            pnpm-workspace.yaml \
            scripts/install-panel-local.sh \
            scripts/install-panel-docker.sh \
            scripts/migrate-agent-legacy.sh \
            scripts/fetch-client-logos.mjs
          cp ".release/${BUNDLE}" "dist/panel/${BUNDLE}"
          ls -lh "dist/panel/${BUNDLE}"

      - name: Upload panel bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: forwardx-panel-bundle
          path: dist/panel/*.tar.gz

      - name: Publish panel bundle to GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ steps.release.outputs.version }}
        run: |
          set -euo pipefail

          shopt -s nullglob
          files=(dist/panel/*.tar.gz)
          if [ "${#files[@]}" -eq 0 ]; then
            echo "::error::Panel release bundle was not generated"
            exit 1
          fi

          if gh release view "$RELEASE_VERSION" >/dev/null 2>&1; then
            gh release edit "$RELEASE_VERSION" --notes-file RELEASE_NOTES.md
          else
            gh release create "$RELEASE_VERSION" \
              --target "$GITHUB_SHA" \
              --notes-file RELEASE_NOTES.md
          fi

          attempt=1
          delay=8
          until gh release upload "$RELEASE_VERSION" "${files[@]}" --clobber; do
            if [ "$attempt" -ge 5 ]; then
              echo "::error::Panel bundle upload failed after ${attempt} attempts"
              exit 1
            fi
            echo "Panel bundle upload failed, retrying in ${delay}s (attempt ${attempt}/5)..."
            sleep "$delay"
            attempt=$((attempt + 1))
            delay=$((delay * 2))
          done

  docker-image:
    needs: release-panel
    uses: ./.github/workflows/docker-image.yml
    permissions:
      contents: read
      packages: write
